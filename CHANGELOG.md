# Changelog

Registro de cambios relevantes del plugin, en español. Versión `0.Y.Z`: `Y` sube al implementar una mecánica nueva (y `Z` vuelve a 1), `Z` sube al modificar una mecánica ya existente.

## 2026-07-12

### v0.0.1

- Sistema de entrada (`InputManager`):
  - Detección del botón de apuntar/lanzar/recuperar, configurable por INI (`Data/SKSE/Plugins/ThrowingWeapons.ini`, por defecto tecla R).
  - Restringido al arma arrojadiza única, identificada mediante una Keyword (`WAF_ThrowableWeapon`) equipada en la mano derecha.

### v0.1.1

- Ciclo de vida del arma (`WeaponState`/`WeaponManager`):
  - Máquina de estados: en mano / apuntando / lanzada / clavada / regresando.
  - El mismo botón apunta+lanza o recupera el arma, según el estado actual.
  - Lanzar/recuperar desequipa y reequipa el arma original (mano vacía → combate a puños real, según la mecánica). Transición instantánea por ahora; trayectoria y proyectil réplica quedan pendientes de `THROW`/`RETURN`/`PHYSICS`.

### v0.1.2

- Robustez del ciclo de vida del arma (`WeaponManager`/`EventManager`):
  - El arma comprometida con el ciclo se fija al empezar a apuntar (no al soltar el botón), para no lanzar por error un arma distinta si el jugador la cambia a mitad de apuntado.
  - Resincronización con la partida real al cargar/empezar (`kPostLoadGame`/`kNewGame`): el estado vuelve siempre a "en mano", evitando quedar desincronizado si se guardó con el arma fuera.
  - Autocorrección si se pierde el botón de soltar (p. ej. pantalla de carga a mitad de pulsación): una pulsación nueva estando ya "apuntando" reinicia el ciclo en vez de quedarse atascado.
  - Bloqueo de equipar cualquier otra arma mientras la arrojadiza está fuera de la mano (apuntando, lanzada, clavada o regresando), vía `RE::TESEquipEvent`.

### v0.1.3

- Recuperación automática del arma al cambiar de celda (`WeaponManager::OnLoadingScreenClosed`, enganchado al cierre de la pantalla de carga vía `RE::MenuOpenCloseEvent`/`RE::UI`): si el ciclo estaba en marcha (apuntando, lanzada, clavada o regresando) al entrar en un interior, salir de él, viajar rápido, etc., el arma se reequipa sola en vez de obligar a pulsar el botón de recuperar. Probado en el juego en ambas direcciones (interior↔exterior).
  - Se descartaron dos alternativas antes de llegar a esta: `TESCellAttachDetachEvent` filtrado al jugador nunca se dispara para su referencia (igual que en Papyrus, `OnCellAttach`/`OnCellDetach` tampoco lo hacen); `TESCellFullyLoadedEvent` solo salta cuando el motor tiene que cargar datos nuevos, así que no se disparaba al volver a una celda exterior ya visitada/en caché.
  - `EquipObject`/`UnequipObject` fuerzan aplicación inmediata sin cola; además, el reequipar se difiere un tick (tarea de SKSE) en vez de llamarse dentro del propio evento de cierre de carga — invocado en ese instante exacto, el juego aceptaba la orden (sonaba el sonido de equipar) pero nunca llegaba a equipar el arma de verdad.
  - No se toca nada al cargar una partida guardada (no hay forma fiable de saber si el arma sin equipar es por nuestro ciclo o por decisión del jugador tras reiniciar el proceso), se deja tal cual está en el guardado.

### v0.2.1

- Lanzamiento del proyectil réplica (`Throw::LaunchWeapon`, llamado desde `WeaponManager::ThrowWeapon` justo antes de desequipar el arma original):
  - Usa el sistema nativo `RE::Projectile` de Skyrim para dar gratis la trayectoria parabólica (punto 3 de la mecánica) y el clavado en superficie/enemigo (punto 6).
  - Origen del lanzamiento: el nodo `WEAPON` de la mano derecha (misma posición que el arma, punto 2), no la cámara.
  - Dirección: hacia donde apunta la cámara (decisión de diseño, no especificada por el documento), calculada a partir del vector de dirección de la cámara (no de `ToEulerAnglesXYZ`, que descompone en un orden distinto al que usa Skyrim y da ángulos incorrectos en cuanto se mezcla inclinación vertical con giro horizontal).
  - Decisión de arquitectura para el regreso (no implementado todavía, documentada en `CLAUDE.md`): la física nativa vale para la ida pero no para la vuelta (curva no balística, velocidad recalculada, homing a enemigo); el regreso se controlará a mano, sin Havok.

## 2026-07-13

### v0.2.2

- Lanzamiento del proyectil, de verdad funcional de punta a punta (probado en el juego contra paredes y NPCs):
  - Cambiado de tipo `Lobber`/`Missile` a **Arrow**, lanzado vía `RE::Projectile::LaunchArrow` con un formulario `Ammo` dedicado (`CAP_ThorMjolnir_Ammo`) en vez de construir el `LaunchData` a mano — es la misma ruta que usa el motor para flechas reales, y resultó ser necesaria para que el impacto contra actores se registre (daño) y para que el arma quede clavada en vez de destruirse al chocar.
  - `Lobber`/`Grenade` no servía: usa un modelo de trayectoria distinto (pensado para lanzar hacia un punto de impacto calculado) que no respondía al par dirección+velocidad que le pasábamos, dejando el proyectil flotando sin moverse.
  - La capa de colisión (`Collision Layer`) del formulario `Projectile` estaba vacía en la Creation Kit; sin ella asignada (a `L_PROJECTILE`, la misma que usan las flechas) el proyectil atravesaba paredes y actores sin registrar ningún impacto.
  - Desmarcado "Can be Picked Up" en el `Projectile`: al quedar clavado, se podía recoger como si fuera munición suelta (añadiendo el `Ammo` falso al inventario, no el arma real) dejando además un modelo fantasma en el mundo. La única forma prevista de recuperar el arma es el botón, no recogerla del suelo.

### v0.2.3

- Detección de impacto y distancia máxima (`Throw::TrackProjectile`, en `ThrowProjectile.cpp`), completando lo que quedaba pendiente de `THROW`, probado en el juego contra pared/árbol, NPC y lanzamiento al vacío:
  - `Throw::LaunchWeapon` ahora devuelve el `ProjectileHandle` del proyectil lanzado (antes se descartaba); `WeaponManager::ThrowWeapon` lo persiste en `WeaponState` y arranca su seguimiento.
  - Sondeo del proyectil cada ~50ms: solo `ImpactResult::kImpale`/`kStick` cuentan como "clavada" (punto 6) — `kBounce` (rebote contra una superficie sin quedar clavado, p. ej. una pared en ángulo) no debe darse por terminado, sigue en vuelo.
  - El sondeo **no** se reprograma llamando a `SKSE::GetTaskInterface()->AddTask` directamente desde dentro de sí mismo: si esa cola no está separada por fotogramas, encadenar comprobaciones así congela el juego por completo (comprobado). En su lugar, cada vez que toca seguir vigilando se lanza un hilo aparte que duerme un intervalo de tiempo real y solo entonces reprograma la comprobación en el hilo principal.
  - El golpe contra un actor no se refleja en `ImpactResult` (queda clavado visualmente pero el campo no cambia); se detecta aparte con `RE::TESHitEvent` (`Events::ProjectileHitWatcher`), filtrado por `a_event->source` (el arma causante) — `a_event->projectile` llega a 0 al lanzar vía `LaunchArrow` con un arma como `a_weap` en vez de un arco real, así que no sirve para filtrar.
  - Distancia máxima de lanzamiento (`Constants::kMaxThrowDistance`, 6000 unidades — valor no especificado por el documento, elegido como placeholder), si se supera sin impactar, dispara la recuperación automática (punto 5).
  - El sondeo se autocancela si el estado deja de ser "lanzada" por cualquier otro motivo (botón de recuperar, resincronización tras pantalla de carga) o si el proyectil deja de existir.

### v0.2.4

- Dos huecos detectados al repasar el sondeo de `THROW` antes de abordar `RETURN`:
  - `WeaponManager::RecallWeapon` ahora destruye (`Projectile::Kill`) la réplica en vuelo o clavada al recuperar el arma — proceso inverso al lanzamiento (punto 2). Sin esto, tanto el botón de recuperar como el auto-regreso (distancia máxima, pantalla de carga) dejaban el proyectil abandonado en el mundo para siempre como un arma fantasma.
  - Caída al agua (caso no cubierto por el documento): `ImpactResult` no cambia nunca al caer al agua, así que `Throw::TrackProjectile` no lo detectaba como impacto ni como distancia máxima, y el arma se quedaba flotando/hundiéndose indefinidamente. Se detecta aparte con `TESObjectREFR::IsInWater()` (comparación barata de altura de agua vs. posición Z, apta para el sondeo) y se trata igual que "no impactó": recuperación automática (`WeaponManager::OnProjectileEnteredWater`).
  - Probado en el juego: el `Kill()` al recuperar funciona contra pared/suelo y agua (el proyectil desaparece), pero **no** contra un actor — la réplica clavada en un NPC persiste tras recuperar. Investigado en detalle: al clavarse en un actor, ni el `ProjectileHandle` original, ni buscar en `RE::Projectile::Manager`, ni buscar por radio en el mundo (`RE::TES::ForEachReferenceInRange`) encuentran nada — el motor destruye la referencia al procesar el golpe (comprobado: el handle ya está muerto en el mismo instante del `TESHitEvent`, no es un problema de timing). Resuelto en v0.2.5.

### v0.2.5

- Localizado y arreglado el fantasma clavado en NPC (probado en el juego, desaparece correctamente al recuperar):
  - Diagnóstico: se volcó el árbol de nodos 3D del actor golpeado, primero en el instante del `TESHitEvent` (vacío, nuestro modelo no está ahí todavía) y luego con 1 segundo de retraso (entonces sí aparece). El "clavado" visual no es una referencia del motor en absoluto — es un nodo 3D nuevo, llamado `"Scene Root"` (nombre por defecto del NIF exportado, nunca renombrado), enganchado directamente al hueso donde impactó, con ~1 segundo de retraso respecto al golpe.
  - `Throw::DetachEmbeddedWeapon` busca ese nodo recursivamente en el árbol 3D del actor y lo desengancha (`NiNode::DetachChild`) — pura manipulación de grafo de escena, no hay ninguna referencia/`Projectile` que destruir en este caso.
  - `Events::ProjectileHitWatcher` guarda ahora el actor golpeado (`WeaponState::SetImpactTarget`, vía `a_event->target`) para que `WeaponManager::RecallWeapon` sepa dónde buscar cuando el `ProjectileHandle` no resuelve a nada.
  - Limitación conocida y aceptada: si se recupera el arma menos de ~1 segundo después del impacto, el nodo todavía no existe y no se encuentra (`DetachEmbeddedWeapon` lo registra con `logs::warn` en ese caso). No se ha añadido reintento con retraso porque es un caso de borde improbable en juego normal, y esta ruta entera es el placeholder de `RecallWeapon` que sustituirá `RETURN`.

## 2026-07-13 (RETURN, fase 1)

### v0.3.1

- Primera versión del regreso manual del arma (`Return::BeginReturn`, `WeaponManager::BeginReturn`): trayectoria en línea recta a velocidad constante hacia la mano del jugador, para validar el mecanismo antes de añadir curvatura/velocidad híbrida/homing/enderezado/temblor (puntos 7-12 de `Mecanica del arma.txt`, todavía pendientes).
  - Revisión de la arquitectura anotada en `CLAUDE.md`: en vez de destruir el `Projectile` nativo, se le pone `SetMotionType(hkpMotion::MotionType::kKeyframed)` — modo estándar de Havok para "movido por código" (sin fuerzas/gravedad, con colisión) — y se controla con `SetPosition`/`SetAngle` cada tick. Evita los tirones/clipping sin tener que destruir ni recrear la referencia.
  - Tres puntos de partida posibles al pulsar recuperar estando lanzada o clavada: proyectil todavía en vuelo, clavado en una superficie (ambos con `Projectile` vivo, se reutiliza tal cual), o clavado en un actor (sin `Projectile` vivo — se lanza uno nuevo con `Throw::SpawnProjectileAt` en la posición exacta donde estaba enganchado el modelo, justo antes de desengancharlo).
  - Sondeo por tick con un único hilo persistente (duerme ~16ms y reencola en el hilo principal en bucle), a diferencia del hilo-nuevo-por-comprobación de `Throw::TrackProjectile` — a este ritmo (~60/s) generar un hilo por paso sería mucho más gasto que en el sondeo de estado de `THROW` (~20/s).
  - Al llegar a la mano (distancia `Constants::kReturnArrivalDistance`), `Return` destruye el proyectil y notifica a `WeaponManager::OnReturnComplete`, que reequipa el arma real — comparte el reequipado con la recuperación instantánea (`WeaponManager::ReequipAndReset`, extraído de la antigua `RecallWeapon`).
  - `RecallWeapon` (recuperación instantánea, sin trayectoria) se mantiene como red de seguridad: para cuando no hay forma de arrancar un regreso de verdad, y para abortar uno ya en marcha (p. ej. pantalla de carga a mitad del trayecto).

### v0.3.2

- Varias rondas de pruebas en el juego contra la v0.3.1 fueron encontrando y arreglando fallos encadenados (sin movimiento visible, clavado en actor sin desengancharse a tiempo, proyectil recién creado sin 3D listo, mitad de vuelo sin efecto) hasta acabar todas apuntando a la misma causa raíz: reutilizar el `Projectile` nativo para el regreso no funciona de forma fiable en cuanto ese `Projectile` estaba **activo en Havok** en el momento de tomar control (en vuelo, o recién relanzado vía `Projectile::LaunchArrow`). Confirmado con logging tick a tick: la posición volvía una y otra vez al punto exacto donde había arrancado el control, hasta que en algún momento se desbloqueaba solo (probablemente al desactivarse el cuerpo de Havok) y entonces el vuelo se completaba de golpe en una ráfaga — nunca de forma fluida. El caso de superficie (`Projectile` ya en reposo antes de tocarlo) no tenía este problema.
  - Se probaron sin éxito: forzar el refresco visual (`Update3DPosition`), empujar la posición directamente al `bhkRigidBody`, forzar `a_force=true` en `SetMotionType`, esperar un margen antes de escribir posición.
  - **Cambio de arquitectura**: el regreso deja de intentar controlar el `Projectile` nativo por completo. Al pulsar recuperar (en cualquiera de los tres casos: en vuelo, clavada en superficie, clavada en un actor), `WeaponManager::BeginReturn` captura la posición actual, destruye/desengancha lo que hubiera, y lanza una réplica visual nueva con `Throw::SpawnWeaponReplicaAt` (`TESObjectREFR::PlaceObjectAtMe`, una referencia normal del arma, **no** un `Projectile` — sin la lógica interna de vuelo balístico que competía con el control manual). `Return::BeginReturn` ahora trabaja sobre `RE::ObjectRefHandle`/`RE::TESObjectREFR` en vez de `RE::ProjectileHandle`/`RE::Projectile`; al llegar a destino, la réplica se borra con `Disable()`+`SetDelete(true)` en vez de `Projectile::Kill()`. `WeaponState` guarda el handle de la réplica de retorno (`returnReplicaHandle`) por separado del `ProjectileHandle` de la ida.
  - Confirmado en el juego: funciona correctamente en los tres casos (en vuelo, clavada en superficie, clavada en un actor), con vuelo físico visible y fluido en todos.

### v0.3.3

- Revisado el punto 8 de `Mecanica del arma.txt`: el regreso ya no es a velocidad constante, sino con aceleración constante partiendo de velocidad cero (`Return::ComputeReturnAcceleration`), como cualquier objeto con momentum. Sigue siendo híbrido: se usa `Constants::kReturnAcceleration` por defecto salvo que a esa aceleración tardara más de `Constants::kReturnMaxDuration` (2s) en cubrir la distancia hasta la mano capturada al empezar — en ese caso se recalcula la aceleración mínima para cumplir ese límite. `kReturnAcceleration` (3000 u/s²) se eligió para que, a la distancia máxima de lanzamiento (`kMaxThrowDistance`, 6000 u), el regreso tarde exactamente 2s partiendo del reposo; para cualquier distancia menor (siempre el caso) tarda menos sin necesitar forzar el aumento. La ida (`THROW`) no se toca: ya usa física real de Havok (punto 3), con aceleración de verdad (gravedad) desde el principio.
- Quitado el desplazamiento de 40 unidades al lanzar la réplica sobre un actor (pensado para evitar solape de colisión con el `Projectile` nativo, ya sin sentido desde que la réplica es una referencia normal — v0.3.2).
- **El jugador nunca debe ver el arma reaparecer sin animación de vuelta.** El log mostró que, cuando se agotan los reintentos de encontrar el nodo "Scene Root" tras clavarse en un actor, `WeaponManager::TryDetachAndBeginReturn` caía a `RecallWeapon()` (recuperación instantánea, sin trayectoria) — visible para el jugador como un "salto" sin animación, no aceptable. Ahora, si se agotan los reintentos sin encontrar el nodo, arranca igualmente el regreso animado desde la posición actual del actor (menos precisa que el punto exacto del clavado, pero siempre con vuelo real) en vez de rendirse al recall instantáneo. También se amplió `Constants::kEmbeddedWeaponDetachMaxAttempts` (10 → 20 intentos, hasta ~3s) para que este caso de reserva sea más raro. `RecallWeapon()` se mantiene como red de seguridad solo para el caso extremo sin jugador ni actor de los que partir.

### v0.3.4

- Punto 7 de `Mecanica del arma.txt` (curvatura obligatoria): el regreso deja de ser una línea recta y pasa a seguir una curva de Bezier cuadrática (`Math::EvaluateQuadraticBezier`, nuevo módulo `9.- MATH/CurveMath`). El punto de control (`Return::ComputeReturnControlPoint`, en `ReturnTrajectory`) se calcula una única vez al empezar el regreso, desviado a un lado de la línea recta entre el punto de partida y la mano del jugador en ese instante (`Constants::kReturnCurveLateralFraction`, acotado entre `kReturnCurveMinOffset`/`kReturnCurveMaxOffset`); el extremo final de la curva sigue actualizándose cada tick a la posición actual de la mano, para no perder la mano si el jugador se mueve durante el regreso.
  - `Return::ComputeNextPosition` (avance en línea recta a paso fijo, ya sin ningún llamante) se ha eliminado junto con el resto de la fase 1; `Return::ComputeTraveledDistance` calcula ahora la distancia recorrida (½·aceleración·tiempo²) para saber en qué punto (0-1) de la curva está el arma cada tick, reutilizando la misma aceleración híbrida del punto 8.
  - Sin objetivo al que priorizar todavía (homing, punto 10, pendiente), la curva se desvía siempre hacia el mismo lado — no es un criterio de diseño, solo una elección determinista para cumplir la curvatura obligatoria.
  - Probado en el juego: la primera versión desviaba la curva hacia un lado arbitrario (perpendicular a la línea recta vía producto vectorial con el eje Z del mundo), que en la práctica salía por la izquierda del jugador la mayoría de las veces — pero el arma se recoge con la mano derecha. Corregido pasando el vector "derecha" del jugador (`Return::GetPlayerRightVector`, eje X del nodo 3D raíz) a `ComputeReturnControlPoint`, que ahora proyecta ese vector perpendicular a la línea recta (Gram-Schmidt) para decidir el lado — la curva entra siempre por la derecha, cayendo al criterio anterior solo en el caso degenerado de que ese vector sea paralelo a la línea recta.
- Corregido: la recuperación automática por distancia máxima (punto 5) y por caer al agua todavía llamaban a `WeaponManager::RecallWeapon()` (recall instantáneo, sin trayectoria) en vez de al regreso animado con curva de `5.- RETURN` — el arma se teletransportaba a la mano en vez de volar de vuelta, saltándose la curvatura del punto 7 recién implementada. `WeaponManager::OnProjectileMaxRangeReached`/`OnProjectileEnteredWater` llaman ahora a `BeginReturn()`, igual que al pulsar el botón de recuperar. `RecallWeapon()` se mantiene para su otro uso (cierre de pantalla de carga a mitad del ciclo) y como red de seguridad sin jugador/proyectil/actor de los que partir.
- Probado en el juego el fix anterior: el caso de agua funciona bien, pero en el de distancia máxima el arma desaparece un instante y reaparece haciendo la curva desde una posición perceptiblemente distinta. Causa: la réplica que controla `5.- RETURN` tarda en cargar su 3D en segundo plano (la misma espera de siempre, `ReturnManager::WaitFor3DThenStart`), y ese hueco se nota mucho más aquí porque, a distancia máxima, el arma está muy lejos del jugador, donde el streaming de objetos es más lento. Una solución de raíz (mantener visible el proyectil nativo hasta que la réplica esté lista) reintroduciría el problema de dos físicas compitiendo ya descartado en `CLAUDE.md`, así que en su lugar se duplica `Constants::kMaxThrowDistance` (6000 → 12000): a esa distancia el auto-regreso por rango máximo ya casi no se da en juego normal (algo suele impactar antes), así que el jugador prácticamente no llega a ver el hueco.
- Probado y descartado el punto 10 de `Mecanica del arma.txt` (homing limitado): se implementó `Return::ComputeReturnControlPoint` con un enemigo objetivo opcional (el hostil vivo más cercano al jugador) y un ángulo máximo de desviación, incluyendo log para verificar en el juego que la curva se sesgaba de verdad hacia el enemigo — funcionaba tal cual estaba diseñado (confirmado con los propios números del log), pero tras probarlo en el juego no convenció como mecánica y se decidió no seguir con este enfoque. Revertido por completo: `ComputeReturnControlPoint` vuelve a la firma de v0.3.4 (sin homing), y `9.- MATH/RotationMath` vuelve a quedar como stub sin implementación.
- Diagnóstico temporal (**no es una mecánica del arma**, ver `1.- CORE/PerfTimer.h`): el usuario reportó tirones en el juego durante las pruebas de `RETURN` sin saber si los causa el plugin o es comportamiento normal de Skyrim. Se añade `Perf::ScopedTimer`, un cronómetro de ámbito que solo escribe en el log si una operación supera un umbral, para no llenar el log en el caso normal. Instrumentados los puntos más sospechosos: la creación de `std::thread` en los tres bucles de sondeo del hilo principal (`Throw::PollProjectile` cada ~50ms, `Return::WaitFor3DThenStart` cada ~16ms hasta 50 veces, `WeaponManager::TryDetachAndBeginReturn` cada 150ms — crear un hilo del sistema operativo no es gratis, y las tres ocurren en el hilo principal del juego) y el cuerpo completo de `Return::Tick` (corre ~60 veces/segundo durante todo el regreso). Pendiente de recopilar datos en el juego; esta instrumentación se quitará o desactivará una vez identificado (o descartado) el origen de los tirones.
- Primera ronda de datos: no saltó ningún `[PERF]` en una sesión completa de pruebas, así que la creación de hilos y `Return::Tick` quedan descartados como causa de tirones — coste de fotograma indetectable (<1-2ms) en todos los casos registrados. Sí apareció, sin relación con el rendimiento, un caso real de 20 reintentos agotados de `WeaponManager::TryDetachAndBeginReturn` buscando el nodo "Scene Root" clavado en un actor: el arma queda ~3,3s sin reaccionar tras pulsar recuperar antes de caer al regreso aproximado (comportamiento ya documentado y aceptado, ver `Constants::kEmbeddedWeaponDetachMaxAttempts`) — no es un tirón de framerate, pero se puede sentir como uno.
- El usuario aclaró que el tirón que le preocupa ocurre al cargar/empezar partida, antes incluso de equipar el arma, y no siempre — descarta por tanto toda la instrumentación de `RETURN`/`THROW` de arriba, que solo se activa una vez se lanza el arma. Revisado el único código propio que corre en ese momento (`Weapon::WeaponManager::ResetToInHand`, enganchado a `kNewGame`/`kPostLoadGame` en `Events::OnSKSEMessage`): solo reasigna variables en memoria, sin llamadas al motor ni E/S — no hay candidato plausible en el plugin para explicar el síntoma. Se instrumenta igualmente con `Perf::ScopedTimer` y un log con timestamp del mensaje SKSE recibido (`kNewGame` vs. `kPostLoadGame`), para confirmarlo con datos en vez de solo con lectura de código. Pendiente: cruzar el próximo tirón notado al cargar partida con estos logs.
- Segunda ronda de datos (misma sesión de juego, sin cargar/empezar partida de nuevo): sigue sin saltar ningún `[PERF]`, y el caso de los 20 reintentos agotados con "Sven" se repitió (2 de 9 lanzamientos contra él en esa sesión) — bastante frecuente, no un caso raro. Se aborda directamente en vez de seguir solo diagnosticando:
  - **Arreglado un riesgo real, no solo diagnóstico**: hasta ahora, si el nodo "Scene Root" aparecía después de agotar los reintentos, se quedaba enganchado al actor para siempre como un arma fantasma (visible pero sin dueño) — riesgo documentado pero sin ningún código que lo evitara. `WeaponManager::WatchForLateEmbeddedWeapon` (nueva) sigue vigilando en segundo plano hasta `Constants::kLateEmbeddedWeaponWatchAttempts` intentos extra (150ms cada uno, ~6s más) tras agotar los reintentos normales; si el nodo aparece, lo desengancha (limpieza — el regreso ya arrancó con la posición aproximada del actor, así que no cambia nada de la trayectoria) y registra en el log cuánto tardó de verdad en aparecer.
  - Ese dato (tiempo real hasta que aparece, cuando aparece más tarde de los 3s actuales) es justo lo que falta para decidir con criterio si conviene subir `kEmbeddedWeaponDetachMaxAttempts`/`kEmbeddedWeaponDetachRetryInterval` en vez de a ciegas — pendiente de una nueva sesión de pruebas contra "Sven" para recopilarlo.
- Tercera ronda de datos: `WeaponManager::WatchForLateEmbeddedWeapon` llegó a agotar sus intentos extra dos veces en la misma sesión, ambas contra NPCs distintos de "Sven" ("Alvor" y "Hod") — resultado en los dos casos: "el nodo nunca llegó a aparecer" tras ~9s totales de búsqueda (3s de reintentos normales + ~6s de vigilancia extra). Esto descarta que sea un problema de presupuesto de reintentos (subir las constantes no lo arreglaría, ya se le dio casi el triple de margen y siguió sin aparecer); el patrón en todo el log es consistente: contra "Sven" el nodo se encuentra casi siempre dentro del margen normal, contra "Alvor"/"Hod" a veces no aparece nunca. Apunta a algo específico de esos NPCs (posiblemente su condición de esenciales/protegidos, a diferencia de Sven) en vez de una condición de carrera genérica, pero confirmarlo requeriría volcar el árbol de nodos 3D completo del actor — investigación más profunda para un caso de borde ya mitigado sin riesgo real (el fallback por posición aproximada sigue funcionando, y `WatchForLateEmbeddedWeapon` confirma que no hay fuga de arma fantasma). Se deja así por ahora; no se toca ninguna de las constantes de reintentos.
- Punto 11 de `Mecanica del arma.txt` (giro y enderezado antes de llegar): la réplica gira ahora sobre sí misma durante todo el vuelo, a velocidad angular constante (`Constants::kReturnSpinDegreesPerSecond`), y se endereza en el último tramo antes de llegar a la mano (dentro de `Constants::kReturnStraightenDistance`) para apuntar en la dirección de vuelo en vez de seguir girando.
  - `Math::ComputeLookAtAngles` (nuevo en `9.- MATH/RotationMath`, misma convención de ejes que `Throw::GetCameraAimAngles`) calcula cabeceo/guiñada para que el eje de avance del modelo apunte hacia una dirección dada; `Math::EvaluateQuadraticBezierTangent` (nuevo en `9.- MATH/CurveMath`) da esa dirección como la tangente real de la curva en cada instante (no la línea recta hacia la mano), para que el enderezado tenga en cuenta hacia dónde está curvándose el arma en ese momento.
  - `ReturnManager::Tick` mezcla (interpolación lineal) el ángulo de giro acumulado con el ángulo de enderezado según la distancia restante a la mano — a distancia completa, giro puro; al llegar, apunta del todo en la dirección de vuelo. La rotación se sincroniza también con Havok (`SetHavokRotation`, mismo motivo que `SetHavokPosition`: `TESObjectREFR::SetAngle` no actualiza el `bhkRigidBody` por sí solo).
  - Verificado con datos reales del NIF del arma soportada (cajas de colisión `bhkBoxShape` de la cabeza y el mango, vistas en NifSkope): ambas están desplazadas solo en el eje Y respecto al origen, así que el eje Y local sí es la dirección mango→cabeza — confirma que `ComputeLookAtAngles` ya estaba usando el eje correcto, sin necesidad de cambiarlo.
  - Probado en el juego, primera ronda: el giro se veía como un tornillo (sacacorchos) en vez de un giro limpio de hacha arrojadiza, y con pocas vueltas a lo largo del regreso. Corregido (parcialmente, ver ronda siguiente) manteniendo la guiñada fija en la dirección inicial mientras dura el giro (en vez de seguir la curva tick a tick) y subiendo `Constants::kReturnSpinDegreesPerSecond` de 720 a 1440 (2 → 4 vueltas/segundo).
  - Probado en el juego, segunda ronda: seguía viéndose mal — el modelo giraba "estirado" hacia la dirección de vuelo (alineando el eje mango→cabeza con ella) en vez de perpendicular, que es como gira de verdad un hacha arrojadiza. Corregido moviendo el giro en vuelo del cabeceo (X) a la guiñada (Z), y añadiendo un giro fijo de 90° sobre el alabeo (`Constants::kReturnModelRollOffset`, el eje mango→cabeza) para que el modelo quede "de lado" en vez de "estirado" — ajuste empírico específico de este modelo 3D, no una fórmula general. El cabeceo pasa a mantenerse fijo (`g_initialPitch`, mismo motivo que antes con la guiñada) mientras dura el giro, y `Math::LerpAngle` (interpolación angular por el camino más corto) sigue usándose para las transiciones de ambos ángulos al enderezar.
- Giro también durante la ida (experimental), punto 11 aplicado a `THROW`: se planteó primero sustituir el `Projectile` nativo de la ida por una simulación manual igual que `RETURN`, para tener control total. Investigación en `commonlibsse-ng` antes de tocar nada: no hay ninguna función expuesta para aplicar daño de verdad a un actor (`ApplyDamage`/`SendHitEvent` no existen en la API pública; solo `RE::HitData::Populate()`, que calcula el daño sin ningún `Apply` correspondiente, y `Actor::DamageActorValue(kHealth, ...)` como único mecanismo verificado para restar vida, sin reacciones de golpe/tambaleo/feedback de combate nativo). Descartado por ese motivo — se mantiene el `Projectile` nativo para impacto/daño, que funciona bien (costó varias rondas de ajuste, ver v0.2.2-v0.2.5).
  - En su lugar, `Throw::ApplyLaunchSpin` (nueva, `4.- THROW/ThrowManager.cpp`) imprime una velocidad angular al `bhkRigidBody` del proyectil una única vez justo al lanzarlo (`bhkRigidBody::SetAngularVelocity`, confirmado existente en `commonlibsse-ng`), dejando que sea la propia simulación de Havok la que lo haga tumbarse — sin control manual tick a tick, así que no compite con la física de posición (a diferencia de forzar `SetAngle` cada tick, que sí arriesgaría el mismo conflicto que obligó a rediseñar `RETURN`, ver `CLAUDE.md`). Eje horizontal perpendicular a la dirección de lanzamiento (mismo patrón que el lado de la curva en `Return::ComputeReturnControlPoint`), velocidad `Constants::kThrowSpinDegreesPerSecond` (1440°/s, mismo valor de partida que el regreso). Experimental: no hay garantía de que el motor no reinicie/ignore la velocidad angular de un proyectil tipo flecha (comportamiento interno no expuesto); pendiente de confirmar en el juego.
- **Probado en el juego: `ApplyLaunchSpin` no funcionó** — el arma no gira visiblemente durante la ida, confirma la sospecha de que el motor reorienta el `Projectile` nativo según su velocidad cada fotograma, ignorando la velocidad angular impresa una vez.

## 2026-07-13 (reinicio: simulación propia para ambas direcciones)

### v1.0.1 (reinicio)

- **Reinicio completo del código, decisión de arquitectura.** Tras el fallo de `ApplyLaunchSpin` y una revisión de `Mecanica del arma.txt` (el usuario había reescrito varios puntos mientras tanto — ver el propio documento: la réplica ahora se describe como "otra entidad... con propiedades similares a las de un proyectil" en vez de "un proyectil"; el impacto contra un enemigo pasa a paralizarlo y hacerle daño eléctrico continuo mientras el arma siga clavada, liberándolo al recuperarla, en vez de solo clavarse; el regreso al golpear genera stagger; la ida también debería tener aceleración híbrida, no solo física nativa; y el punto 11 ya documenta las dos cajas de colisión del NIF), se decidió que ni la ida ni la vuelta usarán `RE::Projectile` nativo — las dos con control manual propio desde el principio, en vez de una arquitectura mixta con una costura entre ambas.
  - Investigación en `commonlibsse-ng` (antes de reescribir nada) confirmó que las piezas que hacían falta son viables: raycast por tick (`RE::TES::Pick`), daño calculado correctamente aunque sin feedback de combate nativo (`RE::HitData::Populate` + `Actor::DamageActorValue`), velocidad/gravedad del arco leíbles del propio formulario Projectile ya configurado (`BGSProjectile::data.speed`/`gravity`), y las trampas de composición de ángulos de Euler ya resueltas en la iteración anterior. Todo esto queda documentado en `CLAUDE.md` ("Arquitectura de física de proyectiles") para no tener que volver a investigarlo.
  - **Todo el código bajo `src/` se ha reseteado a su estado de esqueleto original** (commit `e578416`, "Generado setup basico de la mecanica") — cada `.cpp`/`.h` vuelve a ser solo el comentario de cabecera, sin implementación. Nada se ha perdido: la iteración anterior completa (ida con `Projectile` nativo funcionando de punta a punta, vuelta con control manual, curvatura, homing probado y descartado, giro y enderezado) sigue accesible en el historial de git (commits hasta `dbf728a`) y documentada en las entradas anteriores de este CHANGELOG.
  - Este CHANGELOG se mantiene como historial continuo (no se vacía) — las mecánicas se volverán a implementar desde cero en el código, empezando otra vez en v0.0.1 para las versiones, pero el registro de qué se probó y por qué se llegó a esta decisión no se pierde.

### v1.1.1 (reinicio)

- Ciclo de vida del arma sin física todavía (`InputManager`, `WeaponState`/`WeaponManager`), probado en el juego: apuntar → lanzar → recuperar con reequipado instantáneo (sin réplica visual ni trayectoria, eso llega a partir de la Fase 3-4 del plan de integración). Puerto directo de la iteración anterior en las partes que nunca dependieron de `RE::Projectile`:
  - `InputManager`: tecla configurable por INI (`Data/SKSE/Plugins/ThrowingWeapons.ini`, por defecto R), restringida al arma única marcada con la Keyword `WAF_ThrowableWeapon`.
  - Máquina de estados con el mismo botón apuntando/lanzando/recuperando; arma comprometida con el ciclo fijada al empezar a apuntar (no al soltar), para no lanzar por error un arma distinta si el jugador la cambia a mitad de apuntado.
  - Resincronización a "en mano" en `kNewGame`/`kPostLoadGame`; recall automático al cerrar cualquier pantalla de carga (`MenuOpenCloseEvent` sobre `"Loading Menu"`, no `TESCellAttachDetachEvent`/`TESCellFullyLoadedEvent`, que no se disparan de forma fiable para el jugador); bloqueo de equipar otra arma mientras la arrojadiza está fuera de la mano vía `TESEquipEvent` (`EquipGuard`); reequipar diferido un tick con `AddTask` (invocado síncronamente en el cierre de pantalla de carga, el juego sonaba el equipar pero no llegaba a hacerlo).
  - `WeaponState::State` declara ya los 5 valores del modelo completo (`kInHand`/`kAiming`/`kThrown`/`kStuck`/`kReturning`), pero solo los tres primeros son alcanzables hasta que `THROW`/`RETURN` existan.

### v1.2.1 (reinicio)

- Primitiva de movimiento manual (`6.- PHYSICS/PhysicsManager`), compartida desde ahora por ida y vuelta — pieza central de la nueva arquitectura sin `RE::Projectile` (ver `CLAUDE.md`). Probada en el juego con un lanzamiento de validación en línea recta a velocidad constante (`Throw::LaunchWeapon`, provisional, se sustituye por la parábola real en la próxima fase): movimiento fluido, sin tirones.
  - `Physics::SpawnReplica`: crea la réplica con `PlaceObjectAtMe` + reposicionado exacto, espera a que su 3D cargue en segundo plano (mismo patrón hilo-que-duerme-y-reencola que el resto) y la pone en modo Havok `kKeyframed`.
  - `Physics::SyncHavok`: escribe posición y rotación en el `bhkRigidBody` (Havok no se entera de `SetPosition`/`SetAngle` por sí solo) y refresca el nodo visual.
  - `Physics::StartTickLoop`: bucle de tick genérico por callback (`std::function`), guardado por `shared_ptr` para que el estado mutable capturado por el callback (p. ej. distancia acumulada) persista correctamente de un tick al siguiente en vez de copiarse cada vez.
  - `WeaponManager::ThrowWeapon`/`RecallWeapon` ahora lanzan y destruyen la réplica de verdad (`WeaponState::activeReplicaHandle`), en vez de la transición instantánea sin física de la fase anterior.

### v1.3.1 (reinicio)

- Ida (`THROW`) completa y probada en el juego: apuntado corregido, parábola real, colisión y distancia máxima (puntos 2, 3 y 5 de `Mecanica del arma.txt`). El impacto contra un actor se trata igual que contra una superficie por ahora (clavado genérico, sin daño ni parálisis) — eso es la siguiente fase.
  - **Apuntado corregido**: la dirección de lanzamiento ya no es la de la cámara aplicada tal cual desde la mano (paralaje — no coincidía con la mirilla, sobre todo cerca). `Throw::ComputeAimedDirection` lanza primero un rayo desde la cámara para hallar el punto real al que apunta la mirilla, y calcula la dirección desde el origen en la mano hacia ese punto.
  - **Parábola propia**: `6.- PHYSICS/CollisionManager` (`RE::TES::Pick`, sin ningún `RE::Projectile`) y fórmula cerrada `origen + v0·t + ½·g·t²` con constantes propias (`kThrowInitialSpeed`/`kThrowGravity`), ya no depende de ningún formulario Projectile de la Creation Kit.
  - **Colisión, varias rondas de ajuste en el juego** hasta dar con algo fiable:
    - Un único rayo por tick aceptaba cualquier capa golpeada como "pared" — un lanzamiento al cielo se quedaba clavado en el aire contra `ActorZone` (un volumen de detección de IA invisible, sin geometría real). Arreglado con una lista blanca de capas realmente sólidas (`Collision::IsSolidLayer`).
    - Con la lista blanca, la detección seguía siendo poco fiable: paredes gruesas atravesadas casi enteras, NPCs detectados de forma intermitente. Se probó primero un barrido de varios rayos en cruz alrededor de la trayectoria (aproximando el volumen del arma) — mejoró la detección contra NPCs, pero resultó ser casualidad: la causa real era que faltaba `kCharController` (la cápsula de movimiento normal de un actor de pie) en la lista blanca. El entorno seguía sin detectarse igual de mal con o sin el barrido.
    - El entorno se arregló añadiendo una segunda comprobación en la capa `kLineOfSight` (la que usa el propio juego para "¿hay algo sólido en medio?") cuando `kProjectile` no detecta nada — cierta geometría de pared, confirmado con logging de diagnóstico (capa + referencia resuelta por cada impacto), no interactúa con `kProjectile` a nivel del motor.
    - Con capas y `kLineOfSight` arreglados, se retiró el barrido (`SweepRaycast` → `RaycastSolid`, un solo rayo con las dos capas) por innecesario — pero el arma pasó a clavarse notablemente hundida en la malla, más que antes. El barrido en cruz sí aportaba algo real: varias muestras cercanas tienden a encontrar la cara visible de la superficie en vez de una capa de colisión más profunda. Se restauró el barrido combinado con las dos capas por cada rayo (`Collision::SweepRaycast` llama a `Collision::RaycastSolid` por cada uno de los 5 rayos).
    - El punto de impacto (donde el rayo cruza la superficie) hundía visualmente la malla del arma (que tiene volumen hacia delante) — se retrocede `Constants::kStickEmbedBackoff` unidades a lo largo de la dirección de vuelo al fijar el punto de clavado.
    - El barrido introdujo un efecto secundario: cuando el rayo que detectaba el impacto era uno de los desviados (no el central), el arma se clavaba hasta `kThrowCollisionRadius` unidades a un lado de la trayectoria real. Arreglado recalculando siempre el punto final sobre la línea central del recorrido (usando la fracción del impacto, `HitResult::fraction`), no sobre el rayo desviado que lo detectó.
  - **Recogida bloqueada**: la réplica es un `TESObjectWEAP` real y activable — sin nada que lo impida, el jugador podía recogerla del suelo con la tecla de activar y duplicar el arma. `Physics::SpawnReplica` llama a `SetActivationBlocked(true)` nada más crearla.
  - Sin tirones perceptibles con ninguna de las rondas de raycasts por tick (hasta 10 en el peor caso, solo mientras el arma está en vuelo) — confirmado por el usuario en el juego, no se ha instrumentado con cronómetro al no haber indicio de problema real.

### v1.3.2 (reinicio)

- Impacto en actor completo y probado en el juego (punto 6 de `Mecanica del arma.txt`): daño real, parálisis, seguimiento de posición mientras sigue clavada, liberación al recuperar, límites y configuración por INI. `7.- COMBAT/DamageManager` nuevo.
  - **Investigación previa de parálisis** (antes de escribir nada): no existe ninguna API propia de "paralizar" en `commonlibsse-ng`. Primer intento con el hechizo vanilla real vía `MagicCaster::CastSpellImmediate` — **crasheaba el juego al golpear a un actor**, descartado sin llegar a confirmar la causa exacta (llamada virtual, candidata más probable) a favor de un hechizo propio (copia del efecto vanilla de Parálisis, tipo Ability/Constant Effect/Self) concedido con `Actor::AddSpell`/`RemoveSpell` — no virtuales, aplican y quitan el efecto al instante sin duración que refrescar.
  - **Crash real encontrado y corregido** (no era `CastSpellImmediate`): `Actor::DamageActorValue` también crasheaba. Causa: `ActorValueOwner` (de donde viene) no es la primera clase base de `Actor` en su herencia múltiple — su offset cambia entre versiones del juego (`0xB0` antes de SE 1.6.629, `0xB8` en AE/posteriores) y este proyecto compila multi-runtime (`SKYRIM_CROSS_VR`), así que un upcast implícito usa un offset fijo que no tiene por qué coincidir con la versión real. Arreglado con el accessor versionado `Actor::AsActorValueOwner()` que ya expone `commonlibsse-ng` para esto exacto. Documentado en `CLAUDE.md` como clase de error nueva a vigilar — afecta a cualquier acceso directo a una base no-primera de `Actor` (`MagicTarget`, `ActorState`...).
  - Segundo bug independiente encontrado con `HitData`: `Populate()` no debe llamarse sobre una instancia declarada en la pila sin invocar antes su constructor real (privado) — `HitData::Create` encadena ambos pasos correctamente, usarlo directamente en vez de construir a mano. (Superado más tarde: ver más abajo, el daño dejó de calcularse con `HitData` en absoluto.)
  - **Nerfeos pedidos tras las primeras pruebas en el juego** (el daño automático de `HitData::Populate` + repetir el golpe completo cada tick de goteo mataba NPCs débiles en pocos segundos de forma imperceptible hasta soltarlos):
    - Duración máxima clavada en un actor, 5s (`Constants::kEmbeddedMaxDuration`) — pasado ese tiempo, recall automático aunque no se pulse el botón.
    - Daño configurable por INI (`[Damage]` en `ThrowingWeapons.ini`, `Combat::Init`), sustituyendo por completo el cálculo automático de `HitData`: modo `Direct` (valores fijos de golpe/goteo) o `Level` (`base + porNivel × nivel del atacante`).
  - **Detección de inmunidad/dragón, dos iteraciones**: `AddSpell` siempre tiene éxito aunque la condición del propio efecto (inmune a parálisis, dragón...) le impida aplicarse de verdad, así que hace falta comprobarlo aparte para poder recuperar el arma sola en ese caso.
    - Primera versión: `Actor::IsDragon()` aparte (atajo inmediato) + inferencia genérica por `Actor::AsActorState()->GetKnockState()` (si sigue en `kNormal` pasado un margen, se asume inmune).
    - Corregido a petición del usuario: la condición de inmunidad ya vive en el propio efecto de la Creation Kit — duplicarla en código (el caso dragón) le resta flexibilidad (cambiarla exigiría tocar el CK y el código). Sustituido por una comprobación exacta y genérica: `MagicTarget::HasMagicEffect` (vía `Actor::AsMagicTarget()`, mismo motivo de accessor versionado que `ActorValueOwner`) sobre el `EffectSetting` concreto de la habilidad, comprobado cada tick hasta confirmarse o agotar `Constants::kImmunityCheckDelay`. Cubre cualquier caso de inmunidad (incluidos dragones) sin duplicar nada — cambiar quién es inmune ya solo requiere tocar la condición en el CK.
  - **Seguimiento de posición mientras sigue clavada**: sin API para engancharse a un hueso concreto (ver `CLAUDE.md`), se aproxima con un desplazamiento respecto al nodo raíz del actor, guardado en su espacio local (girado con su rotación del instante del impacto) y reconvertido a espacio del mundo cada tick con la rotación actual — así el arma gira con el actor si cae por la parálisis, en vez de quedarse fija en un punto del mundo.
  - **Punto de clavado contra un actor, distinto de contra una superficie**: la capa golpeada (`CharController`) es una cápsula de colisión más grande que la malla visual real, notable sobre todo en objetivos pequeños — retroceder como con una pared (`kStickEmbedBackoff`) dejaba el arma flotando lejos del cuerpo. Se avanza en su lugar (`Constants::kActorStickForwardOffset`).
    - **Pendiente**: el resultado es inconsistente ("a veces se ve bien, a veces no", según el usuario) — placeholder aceptado por ahora, a revisar más adelante con más pruebas en el juego contra distintos tamaños de actor.
  - Limitación aceptada: no existe ninguna API de "stagger" en `commonlibsse-ng` (ni siquiera vía magic effect, a diferencia de la parálisis) — queda pendiente para el punto 9 (golpear sin clavarse durante el regreso).

## 2026-07-14 (RETURN, puntos 7-8)

### v1.4.1

- Regreso completo con curva y aceleración híbrida (puntos 7-8 de `Mecanica del arma.txt`), sobre la réplica que ya existe desde el lanzamiento (nunca se recrea, a diferencia de la iteración anterior a `RE::Projectile` — ver `CLAUDE.md`):
  - `9.- MATH/CurveMath::EvaluateQuadraticBezier` (genérica, reutilizable) y `5.- RETURN/ReturnTrajectory` (`ComputeReturnControlPoint`, `ComputeReturnAcceleration`, `ComputeTraveledDistance`, `GetPlayerRightVector`) — mismos nombres y comportamiento que la iteración anterior, ya calibrados (ver `Constants::kReturnAcceleration`/`kReturnMaxDuration`/`kReturnCurveLateralFraction`/`kReturnCurveMinOffset`/`kReturnCurveMaxOffset`, reutilizados tal cual).
  - Punto de control de la curva (punto 7) y distancia inicial (para la aceleración híbrida, punto 8) se calculan una única vez al empezar el regreso, capturando la posición de la mano en ese instante; el extremo final de la curva se sigue actualizando cada tick a la posición actual de la mano, para no perderla si el jugador se mueve durante el regreso. El punto de control se desvía hacia el lado del vector "derecha" del jugador (proyectado perpendicular a la línea recta vía Gram-Schmidt), para que la curva entre siempre por la mano derecha.
  - `5.- RETURN/ReturnManager::BeginReturn` arranca el bucle de tick del regreso directamente sobre la réplica activa (`WeaponManager` ya no la recrea al recuperar el arma, a diferencia de la primera versión de esta mecánica en la iteración anterior): el punto de partida es sencillamente la posición actual de la réplica, sea cual sea el estado en el que estuviera (en vuelo, clavada en superficie, o siguiendo a un actor clavado).
  - **Cambio transversal necesario, no anticipado en el plan**: el botón de recuperar llega desde fuera de cualquier bucle de tick en marcha (a diferencia de la transición ida→clavada, que ocurre dentro del propio tick y se autodetiene devolviendo `false`) — sin detener el bucle anterior (vuelo de `Throw::LaunchWeapon`, o seguimiento de actor de `Combat::BeginEmbeddedEffect`) antes de arrancar el del regreso, los dos escribirían la posición de la misma réplica cada tick. Se añade `Physics::TickToken` (alias de `shared_ptr<atomic<bool>>`) devuelto por `Physics::StartTickLoop` y `Physics::CancelTickLoop` para pararlo desde fuera; `WeaponState` guarda el token del bucle activo en cada momento (`activeTickToken`), y `WeaponManager` lo cancela antes de arrancar el siguiente. `Throw::LaunchCallbacks` y `Combat::BeginEmbeddedEffect` ganan un nuevo callback (`onTickStarted`) para reportar hacia arriba el token de cada bucle que arrancan.
  - `WeaponManager::RecallWeapon` se divide: el reequipado/limpieza final (`ReequipAndReset`, cancela el token activo, destruye la réplica, reequipa) pasa a compartirse entre la recuperación instantánea y la llegada del regreso animado (`Return::BeginReturn`'s `onArrived`). `RecallWeapon` en sí queda como red de seguridad (cierre de pantalla de carga a mitad de cualquier fase del ciclo — incluido ahora `kReturning` — y el caso sin jugador/réplica válida dentro de `BeginReturn`).
  - Pendiente para fases siguientes del plan: golpear sin clavarse durante el regreso (punto 9), giro y enderezado en ida y vuelta (punto 10, esta vez con simulación propia en ambas direcciones en vez del intento fallido sobre `RE::Projectile` en la ida), y el temblor al desprenderse antes de regresar (punto 11).
- **Bug encontrado en el juego tras probar la Fase 6**: la recuperación automática (distancia máxima, agua) seguía llamando a `RecallWeapon()` (instantáneo) en vez de a `BeginReturn()` — el arma se teletransportaba a la mano de golpe, saltándose por completo la curva del punto 7 recién implementada. `WeaponManager::ThrowWeapon`'s `callbacks.onAutoRecall` corregido para llamar a `BeginReturn()`; cubre tanto la ida sin impactar (`kThrown`) como el objetivo clavado inmune o que supera la duración máxima (`kStuck`), ambos casos ya compartían este mismo callback.

### v1.5.1

- Golpear a un enemigo durante el regreso sin quedarse clavado (punto 9 de `Mecanica del arma.txt`): `5.- RETURN/ReturnManager::BeginReturn` hace ahora un `Collision::SweepRaycast` cada tick (mismo radio que la ida, `Constants::kThrowCollisionRadius`) entre la posición anterior y la siguiente de la curva. Un impacto contra un actor aplica el golpe (`Combat::ApplyReturnHit`) y el vuelo continúa igual por la curva, sin detenerse; un impacto contra cualquier otra cosa (pared, suelo...) se ignora sin más — a diferencia de la ida, el regreso no choca contra el escenario, solo le interesan los actores. Cada actor solo recibe el golpe una vez por trayecto de regreso (lista de `RE::ActorHandle` capturada por el propio bucle de tick), para no repetirlo tick a tick mientras la réplica pasa cerca de él.
- `Combat::ApplyReturnHit` (`7.- COMBAT/DamageManager`):
  - **Stagger**: decisión de arquitectura tomada con el usuario tras encontrar en la Creation Kit varios efectos mágicos de Archetype `Stagger` — en vez de reutilizar uno vanilla (que podría arrastrar condiciones o efectos secundarios fuera de nuestro control), se crea una copia propia dedicada (`CAP_ThorMjolnir_MagicEffect_Stagger`, Archetype Stagger, envuelto en el Ability `Constants::kStaggerSpell` = `CAP_ThorMjolnir_Ability_ThrowingStagger`, Constant Effect/Self) — mismo patrón ya probado sin crashear que la parálisis del punto 6 (`Actor::AddSpell`/`RemoveSpell`, no virtuales, en vez de `MagicCaster::CastSpellImmediate`). A diferencia de la parálisis (dura mientras el arma siga clavada), el empujón de Stagger ya ha ocurrido en el instante de `AddSpell` (confirmado que existe `RE::StaggerEffect::Start()` en `commonlibsse-ng`, el propio archetype lo dispara ahí) — no hace falta ninguna lógica continua ni comprobación de que el efecto cuajó (a diferencia de `HasMagicEffect` en la parálisis), solo retirar la habilidad poco después (`Constants::kStaggerSpellDuration`, 200ms placeholder) con el mismo patrón de hilo-que-duerme-y-reencola del resto del proyecto, para no dejarla colgada en la lista de hechizos del actor.
  - **Daño reducido, configurable por INI**: nueva entrada `[Damage] ReturnHitMultiplier` (0.5 por defecto, decidido con el usuario) — fracción del daño de golpe normal ya existente (`Combat::ComputeHitDamage`, respeta el modo Direct/Level configurado); a 0 desactiva el daño y deja solo el stagger.
  - Pendiente de probar en el juego (efecto Stagger nuevo en el CK, sin verificar todavía que produzca la reacción visual esperada al concederse vía `AddSpell`).
- **Probado en el juego y confirmado**: el efecto Stagger funciona con `Type=Ability`, `Casting=Constant Effect`, `Delivery=Self` (no `Fire and Forget`/`Contact`, que es como aparecen los `Enchantment` vanilla de stagger — esos se invocan desde el pipeline nativo de "aplicar encantamiento al golpear", no expuesto, distinto del `Actor::AddSpell` que usamos aquí; `AddSpell` concede el hechizo directamente al que lo lleva, así que solo tiene sentido con `Delivery=Self`, igual que cualquier Ability vanilla). Magnitud calibrada a 100.00 (de 0.00), copiando el valor del `EnchStagger` vanilla como referencia.
- **Bug de diseño detectado por el usuario tras probar la Fase 7**: ni los enemigos golpeados reaccionaban (no perseguían ni atacaban de vuelta al jugador) ni los aliados se lo tomaban como una agresión — al no pasar nunca por el pipeline nativo de golpes (sin `RE::Projectile`, ver `CLAUDE.md`), el motor nunca se enteraba de que hubo un ataque, solo de que bajó la vida. Exploit potencial (golpe gratis sin consecuencias). Corregido con `Combat::NotifyHit` (privada, `7.- COMBAT/DamageManager.cpp`): llama a `Actor::HandleHealthDamage(atacante, cantidad)`, confirmado que existe en `commonlibsse-ng` declarada directamente en `Actor` (sin el problema de offset de herencia múltiple de `ActorValueOwner`/`MagicTarget`) — separada de `DamageActorValue`, son dos pasos independientes del pipeline nativo (restar vida y avisar al sistema de combate/crimen de que hubo un ataque). Se llama en los tres puntos donde ya se aplicaba daño (golpe inicial y goteo del punto 6, golpe durante el regreso del punto 9), incluso con cantidad 0 si el daño del regreso está desactivado por INI — la reacción no debe depender de esa opción, solo de que hubo un golpe de verdad. Pendiente de confirmar en el juego que no vuelve a restar vida por su cuenta (sin fuente que documente con certeza qué hace exactamente, solo verificado que existe con esa firma).

### v1.5.2

- Probado en el juego: HandleHealthDamage/SetBeenAttacked no bastan -- IsHostileToActor se quedaba en false->false incluso en un golpe durante el regreso sin paralisis de por medio (descarta que la paralisis estuviera enmascarando el resultado). Ninguna funcion de commonlibsse-ng resuelve esto (comprobado exhaustivamente: StartCombat, SetCombatTarget, CombatController/CombatGroup, sistema de crimen -- nada expuesto para forzar aggro real).
- Una sugerencia externa de usar actor->StartCombat(attacker) se descarto tras verificar contra el codigo fuente real de commonlibsse-ng (Actor.h, Character.h, y todo el arbol de includes): no existe ninguna funcion asi, solo un ID de funcion de condicion de Papyrus (TESCondition::kStartCombat, no invocable desde C++). API inventada, no usada.
- Localizada la funcion interna real via el codigo fuente publico de Precision (Ershin): DealDamage(RE::Actor* attacker, RE::Actor* target, RE::Projectile* sourceProjectile, bool bIsLeftHand), REL::RelocationID(37673, 38627) -- la misma funcion que usa el pipeline nativo de combate cuerpo a cuerpo, confirmada por su uso en un plugin publico, estable y muy usado (no es una direccion inventada). Solo tiene ID verificado SE/AE (Precision no soporta VR) -- en VR se mantiene el intento anterior (HandleHealthDamage/SetBeenAttacked, sin aggro confirmado) para no arriesgar una direccion incorrecta.
- DealDamage calcula su propio dano a partir del arma equipada del atacante en ese instante -- durante todo el ciclo la mano del jugador va vacia (punto 2, arma real oculta), asi que ese calculo no es el que queremos. Se usa DealDamage solo por su efecto colateral real (aviso a IA de combate/crimen); su efecto en la vida del objetivo se revierte (ActorValueOwner::RestoreActorValue) y se aplica en su lugar el importe ya calculado por Combat::ComputeHitDamage/ComputeDotDamage (INI), sin tocar el sistema de configuracion de dano existente.
- Log de diagnostico anadido para confirmar en el juego que la compensacion cuadra exactamente (vida final == vida antes de DealDamage) y que el aggro por fin se produce de verdad -- pendiente de esa prueba.
- Nuevo modulo `1.- CORE/GameOffsets.h`: sitio unico donde centralizar funciones nativas del motor no expuestas por commonlibsse-ng (declaradas via REL::Relocation con IDs de Address Library verificados), documentando siempre la fuente de cada ID -- en vez de dejarlas sueltas en el archivo que las necesita. `DealDamage` es la primera entrada.

## 2026-07-15 (giro, punto 10)

### v1.6.1

- Primera versión, deliberadamente mínima, del giro sobre sí misma durante el vuelo (punto 10 de `Mecanica del arma.txt`), compartida por ida y vuelta: `Math::ComputeSpinAngle` (nuevo, `9.- MATH/RotationMath`) gira siempre de la misma forma, sobre un único eje fijo del mundo (cabeceo/X) a `Constants::kSpinDegreesPerSecond`, calculado sobre el tiempo transcurrido (forma cerrada, sin acumular tick a tick) — sin depender de la dirección de lanzamiento/vuelo ni de la orientación del modelo 3D original. `Throw::LaunchWeapon` y `Return::BeginReturn` llaman a `SetAngle` con este ángulo antes de `Physics::SyncHavok` en cada tick de vuelo.
  - Decisión explícita tomada con el usuario: se limpiaron de `CLAUDE.md` y `Constants.h` los datos de la iteración anterior descartada sobre este mismo punto (eje concreto, offset de montaje del modelo, valores "ya calibrados") para no sesgar el diseño desde cero — ver `CLAUDE.md`. `kSpinDegreesPerSecond` es un placeholder nuevo (720°/s), sin relación con el valor usado antes.
  - Pendiente a propósito para escalones siguientes: enderezado antes de impactar/llegar a la mano, y cualquier ajuste de eje/orientación específico del modelo soportado.

### v1.6.2

- Probado en el juego: doblada la velocidad de `kSpinDegreesPerSecond` (720 → 1440°/s, 4 vueltas/segundo) — el giro se veía fluido pero con pocas vueltas a lo largo del trayecto.
- **Sustituido por completo el giro por código de v1.6.1** (no convencía visualmente por mucho que se ajustara eje/velocidad) **por una animación horneada en el propio NIF del arma**: `Animation::StartSpin`/`StopSpin` (nuevo, `8.- ANIMATION/WeaponAnimation`, el módulo cuyo comentario de cabecera ya anticipaba este uso desde el principio) arrancan/paran directamente un `NiTimeController` (`Start()`/`Stop()`, ya virtuales en la propia clase base, sin necesidad de `NiControllerManager` ni nombre) colgado del nodo raíz del NIF del arma. Es puramente visual: no toca el ángulo lógico del `TESObjectREFR` ni la rotación de Havok, así que no afecta a la colisión (`Collision::SweepRaycast`, basada en posición/radio).
  - Investigado a raíz de revisar mods de referencia del mismo mecanismo (`KratosCombat`, ver `CLAUDE.md`/plan aparcado de animaciones) — ellos consiguen el giro adjuntando (`AttachChild`) un clon del arma real a un hueso ya giratorio dentro del NIF de un proyectil "portador" aparte, porque su objeto en vuelo nunca es el arma real. Nuestra arquitectura ya lanza el propio `TESObjectWEAP` directamente (`Physics::SpawnReplica`), así que no hace falta ningún portador.
  - Patrón real usado, encontrado por el usuario en un NIF vanilla (una gema giratoria de coleccionista): un `NiTransformController`+`NiTransformInterpolator` sueltos, sin `NiControllerManager`/`NiControllerSequence` (más simple de lo previsto en el plan original, que asumía necesitar el mecanismo con nombre de `TESObjectREFR::PlayAnimation`/`GetSequenceByName`) — copiados al NIF del arma y con el flag `kActive` quitado (para que no gire solo al equiparse/tirarse al suelo, ver `CLAUDE.md`), así que arrancarlo/pararlo es cosa nuestra por código.
  - `9.- MATH/RotationMath` vuelve a quedar como stub sin implementación; `Constants::kSpinDegreesPerSecond` se elimina (la velocidad angular pasa a vivir en el propio NIF, no en una constante de C++).
  - Enganchado en el ciclo existente sin tocar la máquina de estados: `Animation::StartSpin` al arrancar el vuelo de ida (`Throw::LaunchWeapon`) y al iniciar el regreso (`Return::BeginReturn`); `Animation::StopSpin` justo al clavarse (superficie o actor).
  - **Pendiente de asset**: hasta que el usuario termine de montar el NIF (NifSkope), `FindTransformController` no encuentra ningún controlador y no hace nada (aviso en el log, no falla) — el arma no girará visualmente hasta entonces.
- NIF terminado y probado en el juego: giro horneado funcionando de punta a punta. Ronda de ajuste del propio asset en NifSkope (sin cambios de código, salvo lo del último punto):
  - El giro copiado tal cual de la gema de referencia (tipo `XYZ_ROTATION_KEY`, tres pistas de ángulos de Euler independientes) se veía a trompicones — sustituido por un giro propio con `Rotation Type = LINEAR_KEY` (cuaterniones, interpolación esférica de verdad) y una pista de traslación heredada de la gema (`Num Keys` a `0`) que habría desplazado el nodo mientras giraba.
  - El eje de giro y qué letra de NifSkope (`Y`/`P`/`R`, realmente ángulos de Euler mostrados como atajo de edición, no correspondencia fija con ejes del mundo) lo controla no se pueden asumir de memoria — hubo que probar en la vista 3D hasta dar con la columna correcta para el eje deseado del modelo.
  - Con 4 claves (pasos de 90°) el giro seguía notándose a trompicones pese a la interpolación correcta; con 8 claves (pasos de 45°) quedó fluido — probado en el juego.
  - **Bug encontrado y corregido, muy pocos lanzamientos de cada varios**: el arma salía sin girar, en la pose estática del NIF. Causa: `Physics::SpawnReplica` espera a que `Get3D()` deje de ser nulo antes de dar la réplica por lista, pero eso no garantiza que todo el subárbol (incluido el nodo de giro) haya terminado de cargar en ese mismo instante — `Animation::StartSpin` fallaba en encontrar el nodo esas pocas veces y se rendía tal como estaba diseñado (aviso en el log, sin romper el lanzamiento). `Animation::StartSpinWhenReady` (nueva, sustituye a la llamada directa en `Throw::LaunchWeapon`) reintenta hasta `Constants::kWeaponSpinNodeWaitAttempts` veces con el mismo patrón hilo-que-duerme-y-reencola que ya usa `Physics::SpawnReplica` para el 3D en sí. `Return::BeginReturn` sigue llamando a `StartSpin` directamente (la réplica lleva rato volando en ese punto, sin la carrera de datos del primer spawn).

### v1.6.3

- Corregido el flote del arma clavada en un actor (punto 6), reportado por el usuario jugando: `Combat::BeginEmbeddedEffect` (`7.- COMBAT/DamageManager.cpp`) solo seguía el **nodo raíz** del actor con un desplazamiento fijo — cualquier movimiento de hueso independiente del root (respiración, el tembleque de la propia parálisis, reacciones de golpe) no se compensaba, así que el arma se veía flotando según el cuerpo se animaba (limitación ya señalada en `CLAUDE.md` y en el "Pendiente" de v1.3.2).
  - `ActorUtils::FindNearestBoneName` (nuevo, `11.- SKYRIM/ActorUtils`): recorrido recursivo del árbol 3D del actor (`NiNode::GetChildren()`, verificado seguro en SE/AE/VR vía `RUNTIME_DATA_ACCESSOR_EX` a diferencia del miembro `children` crudo) que devuelve el nombre del nodo más cercano a un punto del mundo — no existe ninguna API nativa para esto (ver `CLAUDE.md`). Se llama una única vez por clavado, no por tick, así que recorrer un esqueleto completo es irrelevante en coste.
  - `BeginEmbeddedEffect` calcula ahora el desplazamiento local respecto a ese hueso en vez del nodo raíz, y lo sigue guardando por *nombre* (no puntero crudo) para volver a resolverlo cada tick — mismo motivo de cautela que ya había para el nodo raíz (el 3D podría recargarse). Si no se encuentra ningún hueso, cae al nodo raíz (comportamiento anterior, nunca rompe el clavado).
  - Deliberadamente sin filtrar por tipo de nodo ni verificado contra actores de tamaños muy distintos (lobo, gigante) — placeholder aceptado como primera versión, mismo criterio que `kActorStickForwardOffset`/`kStickEmbedBackoff`, pendiente de más pruebas en el juego.

### v1.6.4

- Auditoría con la skill de proyecto `skse-plugin-pitfalls` (`.claude/skills/skse-plugin-pitfalls/SKILL.md`) — corregido el hallazgo de severidad media:
  - **Registro de event sinks antes de cualquier hito de SKSE**: `SKSEPluginLoad` → `Plugin::Init()` → `Events::Init()` registraba `EquipGuard` (`RE::ScriptEventSourceHolder`) y `LoadingScreenWatcher` (`RE::UI`), y llamaba a `Combat::Init()`, de forma síncrona al cargar la DLL — antes de cualquier mensaje de `MessagingInterface`, inconsistente con `InputManager` (que ya esperaba correctamente a `kInputLoaded`). `Events::Init()` ahora solo registra el listener de mensajería; el registro de esos sinks y `Combat::Init()` se difieren a `kDataLoaded` dentro de `OnSKSEMessage`, mismo patrón que ya usaba `InputManager` para `kInputLoaded`.
  - El otro hallazgo de la auditoría (punteros `RE::Actor*` crudos capturados en los bucles de tick de ida/vuelta/clavado, en vez de `RE::ActorHandle`) se deja señalado pero sin tocar — severidad baja mientras el lanzador sea siempre el jugador (singleton estable durante todo el proceso); solo sería un riesgo real si el proyecto llegara a permitir que un NPC lance el arma.

### v1.6.5

- Corregido bug de guardado reportado por el usuario: lanzar el arma, no recuperarla, guardar y cargar esa partida dejaba **dos copias** — el arma original seguía en el inventario (nunca se llega a quitar, solo se desequipa) y la réplica quedaba suelta en el mundo, activable. Causa: `Weapon::WeaponState` vive solo en memoria, nunca en el save; `ResetToInHand()` (en `kNewGame`/`kPostLoadGame`) se limitaba a "olvidar" los handles en memoria, con el comentario explícito (ahora falso en este caso) de que "no hay ninguna réplica real que limpiar" — pero si se guarda a mitad de un ciclo, el motor sí restaura la réplica como referencia de mundo normal (y, si estaba clavada, el actor objetivo conserva la habilidad de parálisis en su lista de hechizos, que sí es parte del save vanilla), y el plugin perdía el rastro de todo eso al cargar.
  - **Primer uso de cosave en este proyecto** (`SKSE::GetSerializationInterface()`, ver `CLAUDE.md` "Serialización del cosave", nunca implementado hasta ahora): se persiste el FormID (no el handle, que no sobrevive a un guardado) del arma activa, la réplica y el actor clavado (`Weapon::WeaponManager::SaveCycleData`/`CaptureSaveData()`), remapeados con `SerializationInterface::ResolveFormID` al cargar (necesario incluso para la réplica, una referencia dinámica creada con `PlaceObjectAtMe`, que también vive en el save con su propio FormID).
  - `WeaponManager::RecoverOrReset()` sustituye a `ResetToInHand()` en `kPostLoadGame`: si había un ciclo activo al guardar, libera al actor clavado (`Combat::EndEmbeddedEffect`), destruye la réplica huérfana (`Physics::DestroyReplica`) y reequipa el arma real — mismos caminos que ya usaban `RecallWeapon`/`ReequipAndReset`, reutilizados en vez de duplicados. Sin datos pendientes (partida antigua sin este cosave, o guardada con el arma ya en mano), se comporta exactamente igual que `ResetToInHand()` de siempre.
  - **Revisado contra la skill `skse-plugin-pitfalls` antes de darlo por bueno** (a petición del usuario, justo después de corregir el hallazgo de v1.6.4): el registro de los callbacks del cosave (`SetUniqueID`/`SetSaveCallback`/`SetLoadCallback`/`SetRevertCallback`) se difiere también a un mensaje de SKSE (`kPostLoad`, el hito más temprano, el que usa la comunidad específicamente para esto) en vez de hacerse síncronamente — por consistencia con el fix de v1.6.4, aunque `SKSE::GetSerializationInterface()` es un objeto propio de SKSE (disponible tras `SKSE::Init`) y no un singleton del motor como `RE::UI`, así que en teoría no tenía el mismo riesgo; no verificado con el mismo rigor que el resto, así que se prefiere no repetir el mismo tipo de suposición sin comprobar. Revisados también los otros dos puntos de la categoría "cosave mal implementado" de la skill: revert callback implementado (limpia el buffer pendiente), y ausencia de datos manejada explícitamente (partidas antiguas se comportan igual que antes).
  - **Confirmado en el juego por el usuario**: lanzar el arma, dejarla sin recuperar, guardar y cargar ya no duplica el arma — el orden `SerializationLoadCallback` antes de `kPostLoadGame` (que se asumía sin verificar) queda confirmado en la práctica.
- **Repaso literal contra la skill `skse-plugin-pitfalls`, a petición del usuario**: de los cuatro puntos que pide su categoría "cosave mal implementado" (registro con `SerializationInterface`, los tres callbacks, ID propio *y verificado como único*, manejo explícito de ausencia de datos), el ID no pasaba el "verificado como único" — `'WAF1'` era propio pero no verificado, y siendo honestos **no existe ningún registro central de estos IDs contra el que verificar unicidad de verdad**. Sustituido por `Fnv1aHash32("Capitto91::Throwing Weapons::cosave")` (FNV-1a de 32 bits, en tiempo de compilación, sobre el nombre+autor ya declarados en `xmake.lua`) — una cadena larga y específica del proyecto reduce muchísimo más la probabilidad de colisión por azar que un código corto de 4 letras elegido a mano (menos combinaciones posibles = más fácil coincidir sin querer). Los otros tres puntos ya estaban cubiertos.
  - **Rotura de compatibilidad deliberada y aceptada**: cambiar `SetUniqueID` invalida el bloque de cosave de cualquier partida de prueba guardada con el `'WAF1'` anterior — el motor ya no lo encuentra bajo el ID nuevo. No es grave (cae en el mismo camino ya implementado de "sin datos guardados", exactamente como antes de tener cosave), pero una partida de prueba guardada a medio ciclo con el ID viejo no se recuperará sola al cargarla con este build.

## 2026-07-18 (estela visual, trail)

### v1.7.1

- Estela visual del arma durante ida y vuelta (no numerada en `Mecanica del arma.txt`, plan propio ya implementado y borrado — ver `PLAN-trail.md` en el historial de git): nuevo módulo `8.- ANIMATION/WeaponTrail`, reposiciona cada tick los segmentos ya modelados de un NIF de efecto (`Constants::kTrailEffectPath`) siguiendo el historial reciente de posiciones de la réplica, interpolado con Catmull-Rom. Basado en el sistema de estelas de Precision (Ershin, MIT License, `github.com/ersh1/Precision`, `src/AttackTrail.h/.cpp`) — reimplementado sin depender de Precision en tiempo de ejecución, adaptado a seguir la posición de la réplica en vuelo en vez de un arma equipada en la mano de un actor (no hace falta nada de la lógica de "qué NIF de estela usar según arma/encantamiento" del original, siempre es la misma réplica única). Aviso de atribución MIT incluido en la cabecera del archivo.
  - `Math::CatmullRom` (`9.- MATH/CurveMath`) y `Math::SetRotationMatrix` (`9.- MATH/RotationMath`, que llevaba stub desde que el giro del punto 10 pasó a resolverse por NIF) portados de Precision.
  - Enganchado con estado mutable capturado por valor en los bucles de tick existentes (mismo patrón que `elapsed`/`hitActors`), una instancia independiente por tramo (`Throw::LaunchWeapon` / `Return::BeginReturn`) — sin tocar `WeaponState` ni la máquina de estados.
  - Constantes nuevas en `Constants.h`: `kTrailEffectPath`, `kTrailRootNodeName`, `kTrailSegmentLifetime`, `kTrailSegmentsPerSecond`, `kTrailBaseColor`/`kTrailBaseColorScaleMult` (azul eléctrico, a petición del usuario), `kTrailSegmentScale`, `kTrailAnchorLocalOffset`.
- **La réplica se quedaba clavada visible varios segundos** tras el impacto o al cancelar el vuelo (recuperación a mitad de trayecto): destruir el objeto C++ `WeaponTrail` no retira el `BSTempEffectParticle` ya spawneado de la escena, que vive hasta agotar su propio tiempo de vida. Arreglado con un destructor RAII que fuerza `age >= lifetime` (mismo truco que usa Precision) para que el motor lo retire en su siguiente actualización interna — confirmado que existe una lista global (`RE::ProcessLists::globalTempEffects`) que el motor recorre cada fotograma para esto. `WeaponTrail` pasa a ser solo-movible (copia borrada) para no duplicar la propiedad del efecto real, y se captura por `std::shared_ptr` en la lambda del tick (`Physics::TickCallback` es un `std::function`, exige que el objetivo sea copiable).
- **Dirección de cada segmento, varias rondas de corrección tras pruebas en el juego**:
  - La técnica original de Precision (deducir la tangente comparando dos interpolaciones Catmull-Rom, una de ellas desplazada según la rotación de la réplica) degenera en un vector constante en nuestro caso: `Throw::LaunchWeapon` nunca actualiza el ángulo de la réplica en vuelo. Sustituida por la tangente real del propio historial de posiciones (`p3-p0` de las 4 muestras usadas para interpolar).
  - La matriz de corrección de ejes de Precision (`weaponRotation`, pensada para su propio `collisionNode`) no aplica a este NIF — quitada por completo tras confirmar en NifSkope (con el usuario) que el segmento se extiende a lo largo de su propio eje Y local, justo el que `Math::SetRotationMatrix` ya alinea con la dirección de vuelo sin corrección adicional.
  - Bug propio detectado tras las pruebas: al quitar esa matriz se borró por error también la propia llamada a `SetRotationMatrix` de los segmentos activos (solo los aparcados se orientaban) — corregido.
  - Degradado de intensidad invertido (confirmado por el usuario en el juego): un giro de 180° sobre el propio eje no podía arreglarlo (no mueve nada a lo largo de ese mismo eje) — se invierte en su lugar la dirección alineada con Y (`segmentAxis = -travelDir`).
- **Punto de anclaje y giro visual**: el nodo raíz de la réplica no coincide con el centro visual de la pieza (confirmado en NifSkope: cae en la base del mango) ni gira durante el vuelo (el giro del punto 10 vive solo en el nodo `Constants::kWeaponSpinNodeName`, un hijo). `Constants::kTrailAnchorLocalOffset` (ajustado a ojo en el juego por el usuario) desplaza el punto de anclaje en el espacio local del arma; `Animation::GetVisualTransform` (nuevo, `8.- ANIMATION/WeaponAnimation`) da la transformación del nodo de giro en vez de la del raíz, para que ese offset orbite con el giro visible en vez de ir en línea recta.
- **Tamaño**: sin escalado por longitud de arma (a diferencia de Precision, que lo deriva del alcance del arma equipada — descartado al portar). `Constants::kTrailSegmentScale` como placeholder fijo resultó demasiado grande (probado en el juego) y se ajustó a la baja; después, a petición del usuario, cada segmento activo se escala según su antigüedad (completo al añadirse, hacia 0 al expirar) en vez de todos por igual — confirmado con el usuario que la malla del efecto está *skinneada* a los 30 huesos de la cadena (`NiSkinInstance`), así que el escalonado se interpola suavemente en vez de dar un aspecto dentado. Los segmentos todavía sin activar se aparcan a escala 0 en vez de tamaño completo, para no amontonar copias superpuestas en el punto de anclaje.

### v1.7.2

- **Giro del punto 10 (`Mecanica del arma.txt`) rediseñado por completo tras confirmar, con evidencia directa de varias pruebas en el juego, que el enfoque de v1.6.2 (animación horneada en el NIF, `NiTimeController::Start()`/`Stop()`) tiene un fallo de fondo del motor, no de nuestro código o del asset**: se probaron tres arquitecturas distintas basadas en `NiTimeController` y las tres fallaron de forma intermitente por el mismo motivo — el motor a veces nunca llega a llamar `NiTimeController::Update()` por su cuenta sobre el controller de una réplica creada en tiempo de ejecución (`PlaceObjectAtMe`), aunque `Start()` reporte éxito y el flag `kActive`/nodo/controller estén perfectamente montados (confirmado con diagnóstico dedicado leyendo `lastTime`/`flags` directamente, congelado en el centinela `-FLT_MAX` del motor):
  1. `NiTransformController` suelto con `Start()`/`Stop()` por código (diseño original de v1.6.2): ~40-60% de fallos.
  2. `NiControllerManager`/`NiControllerSequence` (el mecanismo que usan las animaciones de escena vanilla), activado con `Activate()` por código: mismo fallo; hasta el propio flag `kActive` del manager se leía en `false` tras llamar a `Start()` sobre él, hubo que escribir `flags`/`startTime` directamente como miembros públicos para mejorar la tasa de éxito (0% → ~80%), sin eliminar la intermitencia.
  3. Formulario/NIF dedicados exclusivamente a la réplica en vuelo (`ThorMjolnirFlying`/`MjolnirFlying.nif`, nunca equipado), con el controller horneado activo desde la carga y **sin ninguna llamada a `Start()` por código en absoluto** — ni siquiera así se libra del fallo (confirmado con el mismo diagnóstico, controller encontrado y activo en el 100% de los lanzamientos, pero sin girar en ninguno), descartando que el problema fuera "cuándo" se activa.
- **Intento de arreglo llamando a mano a `NiTimeController::Update()`** desde el propio bucle de tick (en vez de depender de que el motor lo llame): `Update()` es un método virtual público, no privado del motor, así que en teoría se puede invocar directamente sin ningún hook. **Crasheó el juego** en el primer tick de todos los lanzamientos probados — llamar a ese método fuera del recorrido interno del motor no es seguro, aunque sea técnicamente invocable.
- Investigada la alternativa de un hook real (motivada por el mismo patrón que usa `KratosCombat` para su propio giro) como último recurso: verificado contra código publicado real (`community-shaders`, `TrueHUD`, `imgui-vr-helper`) que el punto de enganche fiable "una vez por fotograma, en el hilo principal" en CommonLibSSE-NG es `BSInputDeviceManager::PollInputDevices` (`RELOCATION_ID(67315, 68617)`, llamado desde dentro de `Main::Update`, ya documentado como tal en el propio código fuente vendorizado de `commonlibsse-ng`), con `REL::Relocation<T>::write_call`/`SKSE::Trampoline` como primitivas ya presentes en este proyecto. **No se llegó a instalar**: quedó descartado por innecesario en cuanto se abandonó `NiTimeController::Update()` (ver más abajo), que era la única razón para necesitarlo.
- **Solución final adoptada**: el giro se calcula y escribe directamente por código cada tick — `Animation::TickSpin` (`8.- ANIMATION/WeaponAnimation`, reemplaza a `StartSpin`/`StopSpin`/`StartSpinWhenReady`) construye una `NiMatrix3` con `MakeRotation(ángulo, eje)` a partir del tiempo transcurrido (`Constants::kSpinAngularSpeed`/`kSpinAxisLocal`, placeholders sin ajustar) y la escribe directamente en `local.rotate` del nodo `Constants::kWeaponSpinNodeName`, sin pasar por `NiTimeController`/`NiInterpolator`/`NiTransformData` en absoluto. Es el mismo tipo de operación que `SetPosition`/`SetAngle` (ya usadas sin un solo fallo en cientos de ticks a lo largo de todo el proyecto): no depende de ningún método interno del motor, así que el bucle de tick ya existente (`Physics::StartTickLoop`, el mismo que mueve la réplica) es suficiente por sí solo — sin animación horneada en el NIF, sin formulario dedicado, sin hook.
  - Revertida toda la complejidad que existía solo para lidiar con la fiabilidad de `NiTimeController`: el formulario/NIF dedicados (`ThorMjolnirFlying`) y el intercambio de réplica al reanudar el regreso desde `kStuck` (`Return::BeginReturn` vuelve a su firma simple, sin `a_wasStuck`/`onReplicaReplaced`) — ya no hacen falta con este diseño. "Detener" el giro al clavarse ya no es una operación explícita: simplemente se deja de llamar a `TickSpin` sobre esa réplica y el ángulo se queda congelado donde estuviera.
  - Pendiente de confirmar en el juego (compilado y desplegado, sin prueba visual todavía) y de ajustar a ojo `kSpinAngularSpeed`/`kSpinAxisLocal` — el eje asume mango a lo largo de Y local, sin verificar contra el modelo real.

## 2026-07-20 (mejoras Kratos, ver `PLAN-mejoras-kratos.md`)

### v1.7.3

- **Punto 1 del plan**: gravedad variable en el lanzamiento (`Throw::LaunchWeapon`, `4.- THROW/ThrowManager.cpp`). La parábola de la ida usaba `Constants::kThrowGravity` como constante fija desde el instante cero; ahora usa una rampa lineal (0 → `kThrowGravity` durante los primeros `Constants::kThrowGravityRampDuration` segundos, luego constante), para que la trayectoria salga más "plana"/directa justo al salir de la mano en vez de empezar a caer de inmediato.
  - Nueva función `ComputeGravityDrop` (namespace anónimo de `ThrowManager.cpp`), forma cerrada en las dos ramas (verificada por doble integración y por diferenciación inversa: posición, velocidad y aceleración continuas en el empalme), sin acumular tick a tick — mismo criterio que ya usaba el cálculo anterior.
  - Nueva constante `Constants::kThrowGravityRampDuration` (placeholder, sin valor de referencia previo, pendiente de ajustar en el juego); `<= 0.0f` desactiva la rampa (comportamiento anterior).
  - Log de verificación (`logs::info`) del valor de `ComputeGravityDrop` en el primer tick y en el instante de cruce de la rampa, no en cada tick, para confirmar la fórmula sin inundar el log.
  - Confirmado en el juego por el usuario (log revisado campo a campo): el cruce de rampa a t=0.160s da drop=-4.88, coincide con el cálculo manual de las dos ramas empalmadas (sin salto), y el resto de la sesión (clavados, regresos) funciona con normalidad.

### v1.7.4

- **Punto 2 del plan**: stagger durante el golpe en el regreso (`Combat::ApplyReturnHit`, `7.- COMBAT/DamageManager.cpp`, punto 9 de `Mecanica del arma.txt`) escrito directamente en el animation graph del actor golpeado, en vez de concederle un hechizo propio (`Constants::kStaggerSpell`, `AddSpell`) y retirarlo con un hilo que duerme (`kStaggerSpellDuration`).
  - `a_target->SetGraphVariableFloat("staggerMagnitude", Constants::kStaggerMagnitude)` + `SetGraphVariableFloat("staggerDirection", 0.0f)` (placeholder "de frente", sin verificar unidades/rango real) + `NotifyAnimationGraph("staggerStart")`. Verificado que `IAnimationGraphManagerHolder::SetGraphVariableFloat`/`NotifyAnimationGraph` existen (`IAnimationGraphManagerHolder.h`) y que `Actor` los hereda con offset seguro (`TESObjectREFR` es la primera base de `Actor`, offset 0; `IAnimationGraphManagerHolder` está a offset fijo `0x38` dentro de `TESObjectREFR`, sin variación SE/AE/VR a diferencia de `ActorValueOwner`/`MagicTarget`) — llamable directo sobre `Actor*` sin ningún accessor `AsX()`. `"staggerMagnitude"`/`"staggerDirection"` están pre-registradas como `BSFixedString` del motor (`FixedStrings.h`); `"staggerStart"` no está en ningún header de `commonlibsse-ng` (no hace falta, es un string de evento, no una API), pero es el mismo evento que usa `KratosCombat` (`FenixUtils::stagger`) para el mismo propósito sobre su propia arma.
  - Elimina el hilo (`std::thread`+`sleep_for`+`AddTask`) que antes retiraba el hechizo — las tres llamadas ahora son síncronas dentro del mismo callback de tick de `Return::BeginReturn`, que ya corre en el hilo principal (mismo patrón que `SetPosition`/`SyncHavok` en ese bucle).
  - Constante nueva `Constants::kStaggerMagnitude` (placeholder); eliminadas `kStaggerSpell`/`kStaggerSpellDuration` (sin uso). El hechizo/efecto de la Creation Kit de la iteración anterior queda sin usar, no se toca automáticamente.
  - Pendiente de confirmar en el juego: que el actor golpeado siga tambaleándose igual que antes, sin ningún hechizo concedido/retirado.

### v1.7.5

- **Punto 3 del plan**: transición visual breve al llegar de verdad a la mano durante el regreso animado (no en `RecallWeapon`, que sigue siendo instantáneo sin cambios) — antes `WeaponManager::ReequipAndReset` cortaba en seco (destruye réplica, reequipa un tick después); ahora `Return::BeginReturn` con llegada real (`onArrived`) pasa por `WeaponManager::ReequipAfterCapture`, que clona el modelo visible de la réplica, lo engancha al nodo `"WEAPON"` de la mano un instante, y solo entonces completa el reequipado.
  - `Animation::PlayCaptureTransition` (nuevo, `8.- ANIMATION/WeaponAnimation`): `NiObject::Clone()` (nunca `NiCloningProcess` a mano, sin uso documentado de sus campos en todo `commonlibsse-ng`) envuelto en `RE::NiPointer` de inmediato (mismo idioma que `WeaponTrail.cpp`), `NiNode::AttachChild` sobre el nodo de la mano, `local`/`world` reescritos a mano (mismo patrón que `WeaponTrail.cpp`) con `Constants::kCaptureTransitionLocalOffset` (rotación en identidad de partida). El desenganche, tras `Constants::kCaptureTransitionDuration`, usa el propio `clone->parent` en vez de depender de que el puntero a la mano capturado siga siendo válido varios cientos de ms después (mismo motivo de cautela que ya aplica en el proyecto a punteros de larga vida).
  - `WeaponManager::ReequipAndReset` dividido en dos partes reutilizables (`DestroyReplicaKeepWeapon` + `ReequipActiveWeapon`) para que `ReequipAfterCapture` pueda insertar la transición *entre* ambas sin duplicar la lógica de equipar. Réplica clonada **antes** de `Physics::DestroyReplica` (que llama a `Disable()`), no después.
  - Sin mano o réplica visible de la que partir, `ReequipAfterCapture` cae en `ReequipAndReset` sin transición (mismo comportamiento de siempre).
  - Constantes nuevas: `kCaptureTransitionLocalOffset` (`NiPoint3`, placeholder en cero), `kCaptureTransitionDuration` (300ms, placeholder) — pendientes de ajustar en el juego.
  - **Sin verificar todavía** (señalado en el plan, no comprobado en esta sesión): si el NIF del arma tiene algún nodo de colisión (`bhkCollisionObject`) colgado de su raíz, clonarlo junto con el resto queda huérfano en el hueso de la mano sin nada que sincronice su posición Havok durante la transición — pendiente de comprobar en NifSkope o confirmar en el juego que no se nota.
  - Efecto colateral aceptado: si la partida se guarda durante los ~300ms de la transición, `CaptureSaveData` ve el ciclo activo pero sin réplica (ya destruida) — al cargar, `RecoverOrReset` reequipa el arma sin repetir la animación. No es un bug, solo se pierde el efecto visual en ese caso límite.

### v1.7.6

- **Fix reportado por el usuario al probar el punto 3** (no forma parte de `PLAN-mejoras-kratos.md`, es un fix aparte de `WeaponManager`): al recuperar el arma, se veía en la mano un instante y se envainaba sola. Causa: el ciclo de desequipar (`ThrowWeapon`)/reequipar (`ReequipActiveWeapon`) no conserva por sí solo el estado interno "desenvainada" del actor — el arma se reequipa en la posición envainada por defecto, visible en mano solo durante el frame de la animación de equipar.
  - `Actor::DrawWeaponMagicHands(true)` (directo de `Actor`, offset 0, sin riesgo de versión) llamado en dos puntos: `WeaponManager::BeginAiming` (síncrono, fuerza el arma a desenvainada desde la primera pulsación de G, sea cual sea el estado previo) y `WeaponManager::ReequipActiveWeapon`.
  - **Primer intento (insuficiente, reportado por el usuario)**: llamar `DrawWeaponMagicHands` en el mismo tick que `EquipObject` hacía que el arma iniciara la animación de desenvainar y se envainara sola al instante -- probablemente porque `EquipObject` todavía no había asentado del todo su efecto. Corregido pidiendo el estado desenvainado aparte, tras un margen real (`Constants::kPostEquipDrawDelay`, placeholder), con el mismo patrón hilo-que-duerme-y-reencola de todo el proyecto -- nunca un `AddTask` anidado dentro de la propia tarea de equipar (congela el juego, ver `CLAUDE.md`).
  - No cubierto por `Mecanica del arma.txt` (no menciona envainado/desenvainado) -- decisión de diseño tomada directamente con el usuario.
  - Sin verificar: `DrawWeaponMagicHands` no tiene cuerpo documentado en `commonlibsse-ng` (solo firma reverse-engineered), así que no se puede confirmar por código si llamarla con el arma ya desenvainada es idempotente (sin blip visual), ni si `kPostEquipDrawDelay` es suficiente en todos los casos -- pendiente de confirmar en el juego.

### v1.7.7

- **Fix reportado por el usuario, no relacionado con el plan Kratos ni con las animaciones**: el arma se veía "aplastada", casi en 2D, durante el vuelo (ida y vuelta). Causa: `Constants::kSpinAxisLocal` (eje del giro del punto 10, `Animation::TickSpin`) valía `{0, 0, 0.7f}` -- un vector de longitud 0.7, no unitario. `NiMatrix3::MakeRotation` (`lib/commonlibsse-ng/src/RE/N/NiMatrix3.cpp:174-199`) implementa la fórmula de Rodrigues, que asume el eje ya normalizado; con longitud distinta de 1 la matriz resultante deja de ser una rotación pura y mete un escalado que varía con el ángulo -- el arma se achataba y estiraba en un eje según giraba, visible cada tick al ser recalculado en ida y vuelta por igual.
  - Corregido a `{0, 0, 1.0f}` -- misma dirección, magnitud unitaria. La elección del eje en sí (Z, no X pese a lo que sugiere el comentario adyacente) no se ha tocado, solo su magnitud -- pendiente de que el usuario confirme si además el eje necesita cambiar.

### v1.7.8

- **Punto 4 del plan, campo 1 y 2 (de varios, ver protocolo de testeo campo a campo de `PLAN-mejoras-kratos.md`)**: primeros dos campos de la curva de vuelta cúbica, el punto más delicado de los cinco del plan.
  - Campo 1 (cambio de comportamiento real): `Return::ComputeReturnControlPoint` (`ReturnTrajectory.h/.cpp`) gana un parámetro `a_anchorFraction` -- el punto de anclaje del punto de control a lo largo de la línea inicio→mano, antes fijo al punto medio (`0.5` implícito) dentro del cuerpo de la función. Nueva constante `Constants::kReturnCurveAnchorFraction = 1/3`, usada en la única llamada real (`Return::BeginReturn`) para que el punto de control quede cerca del origen (como en Kratos) en vez de en el medio. Concepto distinto de `kReturnCurveLateralFraction*` (que controla la magnitud del desplazamiento lateral, ya existente) -- documentado explícitamente en el header para no confundirlos.
  - Campo 2 (solo diagnóstico, sin cambiar la curva todavía): log de una vez por regreso (`logs::info`) de los tres ejes (`GetVectorX/Y/Z`) de la rotación mundial del hueso `"WEAPON"` de la mano -- sin precedente en este proyecto de leer la *rotación* de ese hueso (todo el código existente solo leía su posición). Necesario antes de confiar en él para el punto de control `p2` de la curva cúbica (campo siguiente): un hueso de esqueleto puede tener ejes locales sin relación clara con "hacia fuera de la palma", a diferencia de la cámara.
- **Confirmado en el juego por el usuario**: los campos 1 y 2 funcionan y el posicionamiento se ve correcto. El usuario da por **finalizado el plan de mejoras Kratos** (los 5 puntos, ver `PLAN-mejoras-kratos.md`) en este punto -- los campos 3-4 del punto 4 (curva cúbica completa con `p2`/`p3` y suavizado de dirección) no se llegaron a implementar, quedan pendientes si se retoma el plan más adelante.

### v1.7.9

- **Sustituido el intento de v1.7.6 de forzar el estado "desenvainada" a mano** (`Actor::DrawWeaponMagicHands` + `Constants::kPostEquipDrawDelay` en `WeaponManager::ReequipActiveWeapon`) **por una dependencia externa nueva**: el mod [SkipEquipAnimation](https://www.nexusmods.com/skyrimspecialedition/mods/120926) (y su propia dependencia, BehaviorDataInjector, confirmada con el usuario) elimina la animación completa de equipar/desenvainar, resolviendo de raíz el salto de pose al recuperar el arma en vez de solo mitigarlo después. Ver `CLAUDE.md`, nueva sección "Dependencias externas de mod".
  - `WeaponManager::ReequipActiveWeapon` activa la variable de animation graph `"SkipEquipAnimation"` (`Actor::SetGraphVariableBool`, misma API ya verificada para el stagger del punto 9) justo antes de `EquipObject` y la desactiva justo después, dentro de la misma tarea ya diferida -- para no dejarla activada fuera de este ciclo concreto y afectar a otros equipados del jugador.
  - Eliminados `Constants::kPostEquipDrawDelay` y el hilo-que-duerme-y-reencola asociado (ya no hacen falta). La llamada a `DrawWeaponMagicHands` en `WeaponManager::BeginAiming` (fuerza el arma a desenvainada desde la primera pulsación de G) se mantiene sin cambios -- resuelve un problema distinto (estado inicial, no animación de equipar), no relacionado con `SkipEquipAnimation`.
  - **Confirmado insuficiente en el juego**: activar y desactivar la variable en el mismo tick que `EquipObject` no bastaba -- diagnóstico añadido (`SetGraphVariableBool` devuelve si la variable existe de verdad en el graph) confirmó que la escritura tenía éxito (`-> true`, BehaviorDataInjector inyectándola correctamente) pero la animación seguía reproduciéndose de todas formas. Corregido diferiendo la desactivación aparte, tras `Constants::kSkipEquipAnimationWindow` (500ms, placeholder), mismo patrón hilo-que-duerme-y-reencola de todo el proyecto -- `EquipObject` no procesa el equipado de verdad de forma síncrona (mismo motivo, ya documentado, por el que su propia llamada se difiere un tick), así que la variable se apagaba antes de que el hook de `SkipEquipAnimation` llegara a leerla.
  - **Confirmado en el juego por el usuario**: con este margen, la animación de equipar/desenvainar deja de reproducirse -- el arma se queda en la mano sin enfundar.

### v1.7.10

- **Eliminada la transición de captura del punto 3 del plan Kratos** (`Animation::PlayCaptureTransition`, `WeaponManager::ReequipAfterCapture`): con `SkipEquipAnimation` funcionando (v1.7.9), dejó de tener sentido. Reportado por el usuario: al llegar el arma a la mano se veía en una pose durante ~300ms (el clon de la transición, con `Constants::kCaptureTransitionLocalOffset` sin calibrar nunca, rotación identidad de partida) y luego saltaba a otra pose (el arma real, ya reequipada). Antes de `SkipEquipAnimation` la propia animación de equipar disimulaba ese salto; con el reequipado ahora instantáneo, el salto entre el clon sin calibrar y el arma real se veía crudo.
  - La transición se añadió originalmente (inspirada en Kratos/God of War) para suavizar el corte seco de "destruir réplica → reequipar un tick después" -- ese corte seco ya no existe con `SkipEquipAnimation`, así que la transición había quedado sin motivo de ser, y encima introducía el defecto descrito.
  - `WeaponManager::ReequipAndReset` vuelve a ser una única función (revertido el split en `DestroyReplicaKeepWeapon`/`ReequipActiveWeapon` que solo existía para insertar la transición entre ambas); `Return::BeginReturn`'s `onArrived` vuelve a llamarla directamente, igual que `RecallWeapon`.
  - Eliminados `Animation::PlayCaptureTransition` (`WeaponAnimation.h/.cpp`) y las constantes `Constants::kCaptureTransitionLocalOffset`/`kCaptureTransitionDuration`, sin más uso.

### v1.7.11

- **Fix de la estela (trail) reportado por el usuario**: al lanzar, un instante justo al salir de la mano se veía "descuadrada". Repasado el diff completo desde antes del plan Kratos (`git diff` contra el commit previo) para confirmar que `WeaponTrail.cpp` no había cambiado ni una línea -- no era una regresión directa de ese archivo, sino un fallo estructural preexistente que se hizo más perceptible con la trayectoria inicial más plana del punto 1 del plan Kratos (rampa de gravedad).
  - `WeaponTrail::Update` necesita 4 muestras de historial para interpolar una dirección real con Catmull-Rom; hasta entonces, la función no tocaba los segmentos en absoluto -- se quedaban en la pose que trae el NIF de fábrica (visible, sin relación con el vuelo) durante los primeros ~3 ticks (~48ms) tras aparecer la réplica.
  - Corregido: mientras `history.size() < 4`, los segmentos se aparcan a escala 0 en la posición actual (mismo mecanismo que el aparcado normal de segmentos sin usar, más abajo en la función) en vez de dejarse sin tocar -- la orientación no importa a escala 0, así que no hace falta esperar a tener suficiente historial para calcularla.
- **Confirmado por el usuario: mucho menos perceptible, pero seguía viéndose una fracción mínima**. Quedaba un hueco adicional entre `WeaponTrail::Start` (crea el efecto, pero nunca tocaba los segmentos) y el primer `Update()` que ya los aparca -- si el 3D del efecto (`BSTempEffectParticle`, carga en segundo plano igual que cualquier 3D nuevo) llegaba a resolverse en ese primer instante, se veía un fotograma o dos en la pose de fábrica antes del primer aparcado. Nueva función auxiliar `ParkAllSegments` (reutilizada por `Start` y `Update`, mismo mecanismo) intenta aparcar también dentro de `Start`, en el mismo instante de crear el efecto, por si el 3D ya está listo síncronamente -- best-effort, no elimina el hueco del todo (sigue dependiendo de si el 3D carga síncrono o no esa vez concreta).
- **Confirmado por el usuario: sin ningún efecto** -- la teoría del aparcado no era la causa real (o no toda la causa). Diagnóstico revisado desde cero: la pista clave es que el defecto **solo pasaba al lanzar, nunca al volver**.
  - **Causa real encontrada**: `Animation::TickSpin` (punto 10) escribe una rotación *absoluta* en el nodo de giro (`MakeRotation(velocidad·elapsed, eje)`), no incremental. `trail->Start()` se llama **antes** de que arranque el bucle de tick de la ida, así que lee la rotación de reposo que trae el NIF de fábrica para calcular el punto de anclaje de la estela (`Constants::kTrailAnchorLocalOffset`, brazo de 20 unidades) -- rotación que no tiene por qué ser la identidad. En el primer tick, `TickSpin` salta de golpe a la rotación absoluta calculada desde `elapsed=0`, y ese salto (de la rotación de reposo del NIF a la rotación controlada por código) se colaba directamente en la primera muestra del historial de la estela, torciendo su forma justo al principio. En el regreso no pasa: el nodo ya lleva un rato bajo control continuo de `TickSpin` desde la ida, sin ningún "reposo de fábrica" que saltar.
  - Corregido en `Throw::LaunchWeapon`: `Animation::TickSpin(*replica, 0.0f)` justo antes de `trail->Start()` -- siembra el nodo de giro a la rotación identidad (`MakeRotation(0, eje)` = identidad, verificado en `NiMatrix3.cpp`) que el propio código controla, para que la primera muestra de la estela ya parta de esa misma referencia y no de la pose de fábrica del NIF.
- **Confirmado por el usuario: el defecto pasó a ser intermitente** (a veces claro, a veces apenas, a veces nada) en vez de desaparecer -- señal de una carrera con la carga asíncrona del propio 3D del efecto (`BSTempEffectParticle`, que carga en segundo plano igual que cualquier 3D nuevo, independiente de nuestro código), no ya del salto de rotación (ese sí quedó resuelto, pero no era la única causa).
  - **Fix definitivo**: en vez de seguir intentando cazar el instante exacto en que el 3D del efecto está listo para aparcarlo (intentado en el intento anterior, ganaba la carrera unas veces sí y otras no -- de ahí la intermitencia), `WeaponTrail::Start` ahora crea el efecto directamente a **escala 0** (`BSTempEffectParticle::Spawn` con `a_scale=0.0f` en vez de `1.0f`) -- invisible desde el primer fotograma en que llega a renderizarse, sin ninguna carrera que ganar o perder. `WeaponTrail::Update` restaura la escala del nodo raíz a 1 en su primera llamada exitosa (una vez resuelto `particleObject`/`fadeNode`); a partir de ahí, cada segmento individual sigue controlando su propia visibilidad con su propia escala, como ya hacía.
  - Eliminado el intento anterior de aparcar los segmentos dentro de `Start` (best-effort, solo funcionaba si el 3D cargaba síncrono) -- ya no hace falta, la escala 0 del nodo raíz cubre ese mismo hueco de forma incondicional.

## 2026-07-20 (temblor al desclavarse, punto 11)

### v1.8.1

- Implementado el punto 11 de `Mecanica del arma.txt` (temblor de desprendimiento): al pulsar recuperar con el arma clavada (superficie o actor), antes de arrancar el movimiento de vuelta se reproducen `Constants::kStickShudderDuration` (0,1s, valor dado explícitamente por el documento) de pequeñas rotaciones oscilantes de frecuencia creciente en un único eje -- simulando el tirón que la va aflojando, a petición del usuario, mismo espíritu que el temblor de las flechas clavadas en madera del propio juego.
  - `Animation::TickShudder` (nuevo, `8.- ANIMATION/WeaponAnimation`), mismo mecanismo que `TickSpin` (`NiMatrix3::MakeRotation` sobre `local.rotate` del nodo `Constants::kWeaponSpinNodeName`, puramente visual, sin tocar el ángulo lógico del `TESObjectREFR` ni Havok): un chirp de fase continua (`Constants::kStickShudderFrequencyStart`→`End`, forma cerrada, misma filosofía que `Throw::ComputeGravityDrop`, no acumulada tick a tick) con amplitud fija (`Constants::kStickShudderMaxAngle`, ~2°) sobre un eje propio (`Constants::kStickShudderAxisLocal`, distinto del eje de giro en vuelo a propósito). Todos placeholders nuevos, pendientes de ajustar en el juego.
  - `Return::BeginReturn` (`5.- RETURN/ReturnManager`) gana un parámetro `a_wasStuck`: si es `true`, arranca primero un bucle de tick breve que llama a `TickShudder` cada paso y, al agotar la duración, arranca el movimiento de vuelta de siempre (extraído a una función interna, `BeginReturnMovement`, para no duplicar esa lógica); si es `false` (arma todavía en vuelo, nunca llegó a clavarse), el movimiento arranca de inmediato como antes. `WeaponManager::BeginReturn` captura `wasStuck` (`weaponState.GetState() == State::kStuck`) antes de tocar el estado y se lo pasa tal cual.
  - Sin cambios en `WeaponState` ni en la máquina de estados: el temblor reutiliza el mismo `Physics::TickToken`/`onTickStarted` ya existente (primero el del propio temblor, luego el del movimiento), así que sigue siendo cancelable desde fuera (p. ej. cierre de pantalla de carga a mitad del temblor) sin ningún código nuevo en `WeaponManager`.
  - Pendiente de confirmar en el juego: sin prueba visual todavía, ángulo/eje/frecuencias son placeholders a ajustar.

### v1.8.2

- **Probado en el juego y reportado por el usuario: el temblor no se apreciaba visualmente.** Auditado con las skills `skse-plugin-pitfalls`/`verify-commonlibsse-api` antes de tocar nada (sin hallazgos: `NiMatrix3::MakeRotation`/`NiTransform::rotate`/`GetObjectByName` verificados contra los headers reales, mismo uso ya validado que `Animation::TickSpin`). Causa real encontrada al repasar la diferencia con el vuelo: `Animation::TickShudder` solo escribe la rotación *local* del nodo de giro; durante el vuelo eso se propaga a `world.rotate` porque `Physics::SyncHavok` llama a `TESObjectREFR::Update3DPosition` en cada tick (la posición también cambia), pero durante el temblor la réplica no se mueve, así que esa llamada nunca ocurría -- el cálculo del chirp era correcto pero nunca llegaba a verse. Corregido: `Return::BeginReturn` llama a `a_refr.Update3DPosition(true)` tras `TickShudder` en cada tick del temblor, aunque la posición no cambie.
- `Constants::kStickShudderDuration` subida de 0,1s (valor exacto de `Mecanica del arma.txt`) a 0,5s, a petición expresa del usuario para confirmar visualmente el efecto antes de recortarla de vuelta -- pendiente de una prueba en el juego para decidir el valor final (0,1s del documento, o quedarse con algo intermedio si a 0,1s con el fix de arriba ya se aprecia bien).

### v1.8.3

- **Dos ajustes pedidos por el usuario tras probar v1.8.2 en el juego** (el temblor ya se apreciaba, pero con dos problemas):
  - **"Cambia de posición respecto a cómo estaba clavada"**: causa real, `Animation::TickShudder` escribía una rotación *absoluta* (`MakeRotation` desde cero, mismo mecanismo que `TickSpin`) en vez de partir de la orientación con la que el arma quedó clavada -- que viene de un ángulo de vuelo arbitrario (el giro del punto 10 se congela en el momento del impacto, no vuelve al reposo). En `a_elapsedSeconds=0` la oscilación es cero, así que la rotación escrita saltaba de golpe de "como estaba clavada" a "identidad", visible como un salto de posición del modelo (más notorio cuanto más lejos del origen está la malla visible, ver `kTrailAnchorLocalOffset`). Corregido: `TickShudder` gana un parámetro `a_baseRotation`, y compone la oscilación *sobre* ella (`a_baseRotation * wobble`) en vez de sustituirla; `Return::BeginReturn` captura esa rotación base una única vez (`spinNode->local.rotate` en el instante de empezar el temblor) antes de arrancar el bucle de tick.
  - **"La vibración es muy poco amplia" y el ángulo debe crecer exponencialmente hasta un máximo**: `Constants::kStickShudderMaxAngle` sube de ~2° (placeholder inicial) a 35° (valor dado por el usuario), y deja de ser una amplitud constante -- ahora es el techo de una envolvente que crece exponencialmente desde 0 a lo largo del temblor (`1 - e^(-decayRate·t)`), alcanzando `Constants::kStickShudderAmplitudeRampFraction` (95%, nueva constante) de ese máximo justo al final de `kStickShudderDuration`. `decayRate` se despeja en forma cerrada de esa condición, mismo criterio que el resto del proyecto (no ajustado a mano ni acumulado tick a tick). La frecuencia (chirp) no cambia, solo la amplitud pasa de fija a creciente.
  - Auditado de nuevo con las skills `skse-plugin-pitfalls`/`verify-commonlibsse-api` antes de dar el fix por bueno: sin hallazgos nuevos (`NiMatrix3::operator*` confirmado contra el header real -- `entry[fila][col]`, composición estándar "el de la izquierda se aplica después", coherente con `baseRotation * wobble` = wobble en el espacio local del arma seguido de la orientación de clavado).
  - Pendiente de confirmar en el juego: sin prueba visual todavía de ninguno de los dos ajustes.

### v1.8.4

- `Constants::kStickShudderMaxAngle` bajado de 35° a 20°, a petición del usuario tras probar en el juego.

### v1.8.5

- `Constants::kStickShudderFrequencyStart`/`End` bajadas de 6/30 Hz a 4/20 Hz (misma proporción), a petición del usuario para ralentizar un poco la vibración.

### v1.8.6

- `Constants::kStickShudderMaxAngle` bajado de 20° a 15°, y `kStickShudderFrequencyStart`/`End` de 4/20 Hz a 3/15 Hz, a petición del usuario tras otra ronda de prueba en el juego.

## 2026-07-21 (sonido de lanzamiento/vuelo/atrape)

### v1.9.1

- Primera pasada de sonido (mecánica nueva, sin cubrir por `Mecanica del arma.txt` -- pulido audiovisual, no un punto numerado): módulo nuevo `12.- AUDIO/FlightSound`.
  - `Audio::FlightSound`: envoltorio RAII sobre `RE::BSSoundHandle` (que no se autolibera, `~BSSoundHandle() = default`) para el silbido en bucle que sigue a la réplica durante el vuelo real (fase parabólica de la ida, curva del regreso) vía `BSSoundHandle::SetObjectToFollow` -- se para solo cuando el `shared_ptr` que lo mantiene vivo se destruye al terminar el bucle de tick, mismo mecanismo ya usado por `Animation::WeaponTrail`, sin ningún `Stop()` explícito en cada punto de salida.
  - `Audio::PlaySoundOneShot`: sonido suelto (lanzamiento, atrape), handle local de usar y tirar -- el motor sigue reproduciéndolo por su cuenta aunque el handle se destruya al momento (mismo patrón que `BSAudioManager::Play(BSISoundDescriptor*)`).
  - Enganchado en `Throw::LaunchWeapon` (silbido de lanzamiento + arranque del loop, junto a `trail->Start`) y en `Return::BeginReturnMovement` (mismo silbido al iniciar el tramo de movimiento del regreso, loop propio, y sonido de atrape en el instante de llegada a la mano).
  - Tres EditorID nuevos en `Constants.h` (`kThrowLaunchSoundEditorID`/`kFlightLoopSoundEditorID`/`kCatchImpactSoundEditorID`), placeholders sin apuntar todavía a ningún Sound Descriptor real -- pendiente de que el usuario los localice/cree en la Creation Kit; mientras tanto no suena nada y queda avisado por log, sin romper el ciclo.

### v1.9.2

- Sonido de atrape sincronizado con el instante real de llegada (`kCatchImpactSoundEditorID` = `CAP_ThorMjolnir_MarkSound_MjolnirCatch`, Sound Descriptor propio del usuario): el golpe grabado en el propio archivo de audio no está al principio, sino a `Constants::kCatchImpactSoundLeadTime` (1,127s) de su inicio -- reproducirlo justo al llegar habría dejado el golpe audible sonando tarde.
  - `Return::ComputeReturnDuration` (nuevo, `5.- RETURN/ReturnTrajectory`), inversa en forma cerrada de `ComputeTraveledDistance`: predice la duración total del regreso a partir de la distancia inicial y la aceleración ya calculadas al empezar (`T = (distancia·n·(n-1)/aceleración)^(1/n)`), sin esperar a detectarla tick a tick.
  - `Return::BeginReturnMovement` calcula una única vez, al iniciar el regreso, el instante exacto en el que hay que lanzar el sonido (`duración prevista - kCatchImpactSoundLeadTime`) y lo dispara en ese tick -- así el golpe grabado cae justo cuando la réplica toca la mano. La predicción asume que la mano no se mueve; si en la práctica la réplica llega antes de lo previsto (jugador acercándose), la propia rama de llegada dispara el sonido igualmente como salvaguarda (tarde, pero nunca se pierde).

### v1.9.3

- **Probado en el juego: el sonido de atrape no llegaba a oírse.** Causa real: el EditorID (`CAP_ThorMjolnir_MarkSound_MjolnirCatch`) resuelve a un **Sound Marker** (`RE::TESSound`), no a un **Sound Descriptor** (`RE::BGSSoundDescriptorForm`/SNDR) -- son formularios distintos en la Creation Kit, y `RE::BSAudioManager::GetSoundHandle`/`GetSoundHandleByName` exigen un `RE::BSISoundDescriptor*`, interfaz que solo implementa el segundo. Un Sound Marker simplemente referencia un Sound Descriptor propio en su campo "Sound" (`RE::TESSound::descriptor`); pasarle su EditorID directamente a esas funciones resolvía el formulario pero no llegaba a producir un handle reproducible.
  - `Audio::ResolveSoundDescriptor` (nuevo, `12.- AUDIO/FlightSound.cpp`): prueba primero `TESForm::LookupByEditorID<RE::TESSound>` (si resuelve, usa su `.descriptor`) y si no, `TESForm::LookupByEditorID<RE::BGSSoundDescriptorForm>` directamente -- así `Audio::FlightSound::Start`/`PlaySoundOneShot` funcionan sea cual sea el tipo de formulario que el usuario haya creado en la Creation Kit para cada EditorID, sin que el código que los llama tenga que saberlo. Sustituye el uso de `GetSoundHandleByName` por `GetSoundHandle` con el descriptor ya resuelto a mano.

### v1.9.4

- **Probado en el juego con `Constants::kCatchImpactSoundEditorID` ya apuntando al Sound Descriptor directamente (`CAP_ThorMjolnir_Sound_MjolnirCatch`): seguía sin oírse nada.** A petición del usuario, se retira la resolución de Sound Marker de `Audio::ResolveSoundDescriptor` (ya no se usa ningún EditorID de ese tipo) -- ahora solo resuelve `RE::BGSSoundDescriptorForm` directamente, sin la rama de `RE::TESSound`.
  - `Audio::FlightSound::Start`/`Audio::PlaySoundOneShot` ahora registran por log el resultado de cada paso (`GetSoundHandle`, `BSSoundHandle::IsValid`, `BSSoundHandle::Play`) -- hasta ahora se ignoraban esos valores de retorno, así que un fallo silencioso en cualquiera de los tres no dejaba ninguna pista. Pendiente de revisar esos logs en el juego para localizar en qué paso concreto sigue fallando.

### v1.9.5

- **Localizada la causa real de que el sonido de atrape no llegara a oírse, tras varias rondas de prueba en el juego.** No era un problema de tipo de formulario (ya descartado en v1.9.3/v1.9.4): `TESForm::LookupByEditorID` (la tabla global de EditorID que mantiene el propio motor) sencillamente no tiene ninguna entrada para el Sound Marker/Sound Descriptor de este proyecto, aunque el registro exista, esté guardado en el plugin y el plugin esté activo -- confirmado añadiendo temporalmente una llamada de diagnóstico a `RE::PlaySound` (`RE/M/Misc.h`, con toda probabilidad el mismo camino nativo que usa `Sound.Play()` en Papyrus): esa vía sí reproducía el sonido, mientras `GetSoundHandle` seguía sin encontrar el formulario en el mismo instante.
  - `Audio::ResolveSoundDescriptor` ya no depende de esa tabla global: recorre a mano los formularios ya cargados de cada tipo (`RE::TESDataHandler::GetFormArray<T>()`, nuevo `FindFormByEditorID<T>` interno) comparando `RE::TESForm::GetFormEditorID()` -- el EditorID propio de cada formulario, independiente de la tabla que falla -- con el buscado. Misma resolución dual de antes (Sound Marker → `.descriptor`, o Sound Descriptor directo), solo cambia cómo se localiza el formulario.
  - Retirada la llamada de diagnóstico a `RE::PlaySound` (y el `#undef PlaySound` que hacía falta por la colisión con la macro de `Windows.h`, `mmsystem.h`) una vez confirmada la causa -- ya no hace falta con `GetSoundHandle` funcionando, y dejarla habría sonado el golpe de atrape dos veces.
  - Pendiente de confirmar en el juego que `GetSoundHandle` ya resuelve los tres EditorID (incluidos los dos placeholders de lanzamiento/loop, que seguían sin existir como formulario real en las últimas pruebas) con este nuevo mecanismo de búsqueda.

### v1.9.6

- **Rediseño completo de la resolución y la sincronización del sonido de atrape**, a petición del usuario tras reportar dos problemas: se desincroniza si el jugador se mueve durante el regreso, y nace ya desincronizado si el lanzamiento es más corto que el preámbulo del propio audio (`Constants::kCatchImpactSoundLeadTime`, 1,127s).
  - **Pivote de resolución, de EditorID a FormID**: tres mecanismos de resolución por EditorID (`GetSoundHandleByName`, `TESForm::LookupByEditorID` tipado y sin tipar, `TESDataHandler::GetFormArray<T>()`+`GetFormEditorID()`) fallaron igual para los Sound Marker/Sound Descriptor de este proyecto -- confirmado con `RE::PlaySound`, que resuelve por otra vía interna y sí los reproducía. `Audio::ResolveSoundDescriptor` (movido a `12.- AUDIO/SoundResolver.h/.cpp`, compartido entre `FlightSound` y el nuevo `CatchSound`) ahora resuelve por `RE::TESDataHandler::LookupForm<T>(FormID local, nombre de plugin)` -- mecanismo ya verificado y en uso en este proyecto (`WeaponManager::RecoverOrReset`). `Constants.h` cambia los tres `k*SoundEditorID` (`string_view`) por `kSoundPluginName` (`"ThorMjolnir.esp"`, localizado buscando en la carpeta de mods de MO2) + un FormID local por sonido -- el de atrape ya relleno (`0x00EA61`, el Sound Descriptor `CAP_ThorMjolnir_Sound_MjolnirCatch`, confirmado parseando a mano el registro binario del `.esp` y comparando con lo que muestra la Creation Kit), lanzamiento/loop siguen en placeholder `0x000000` a falta de esos Sound Descriptor.
  - **`Audio::CatchSound`** (nuevo, `12.- AUDIO/CatchSound.h/.cpp`): sustituye por completo la predicción de un solo disparo (`Return::ComputeReturnDuration` calculado una vez al empezar el regreso, asumiendo que la mano no se mueve). En su lugar, mide cada tick la velocidad de cierre real (diferencia de la distancia arma-mano entre este tick y el anterior -- la misma distancia que `Return::BeginReturnMovement` ya usa para decidir la llegada) y estima de ahí el tiempo restante real. Arranca el sonido en cuanto ese tiempo restante cae por debajo de `kCatchImpactSoundLeadTime`, y mientras suena reajusta cada tick su velocidad de reproducción (`RE::BSSoundHandle::SetFrequency`, acotada a `[Constants::kCatchSoundMinFrequency, kCatchSoundMaxFrequency]` = `[0.5, 3.0]`) para que el preámbulo restante del propio clip encaje siempre en el tiempo real que quede hasta el atrape -- soluciona los dos síntomas a la vez: se reajusta solo si el jugador se mueve, y arranca ya a velocidad alta si el vuelo es más corto que el preámbulo, en vez de sonar tarde.
  - `Return::BeginReturnMovement` llama a `catchSound->Update(...)` cada tick con esa misma distancia arma-mano, antes de la comprobación de llegada; mantiene una salvaguarda (sonido suelto sin sincronizar) solo para el caso degenerado en que `CatchSound` nunca llegó a arrancar.
  - Pendiente de confirmar en el juego, y de que el usuario localice los FormID locales de los Sound Descriptor de lanzamiento y loop de vuelo (siguen en `0x000000`).

### v1.9.7

- **Confirmado en el juego: el sonido de atrape ya suena, sincronizado.** `Audio::ResolveSoundDescriptor` seguía sin encontrar `0x00EA61` (el FormID local "en bruto" que muestra la Creation Kit) porque `ThorMjolnir.esp` tiene el flag ESL activo (confirmado leyendo el header del propio `.esp`) -- el direccionamiento local comprimido de un plugin ligero solo usa 12 bits, así que el valor real en tiempo de ejecución es `0x00EA61 & 0xFFF = 0x0A61`, no el bruto. Localizado probando ambos automáticamente dentro del propio resolver (que ahora, si el FormID dado supera los 12 bits y no resuelve, reintenta con esa máscara) y confirmando por log cuál de los dos funcionaba.
  - `Constants::kCatchImpactSoundLocalFormID` actualizado al valor confirmado (`0x000A61`) directamente, para no depender del reintento en cada llamada.
  - El log también confirmó que el propio algoritmo de sincronización de `Audio::CatchSound` (v1.9.6) calcula bien el tiempo restante y decide arrancar en el instante correcto -- el único problema de esta ronda era la resolución del FormID, no la lógica de sincronización.
  - Pendiente: lanzamiento y loop de vuelo siguen sin sonar (`kThrowLaunchSoundLocalFormID`/`kFlightLoopSoundLocalFormID` en `0x000000`) hasta que el usuario cree esos Sound Descriptor y aporte sus FormID -- el mismo mecanismo de resolución (con el reintento de 12 bits ya incorporado) debería bastar sin más cambios de código.

## 2026-07-22 (eliminación de la estela visual)

### v1.9.8

- **Retirada por completo la estela visual del arma (mecánica v1.7.x), a petición del usuario tras varias rondas de depuración sin resultado convincente**: borrados `8.- ANIMATION/WeaponTrail.h`/`.cpp` y `9.- MATH/RotationMath.h`/`.cpp` (esta última exclusiva de la estela -- no confundir con `RE::NiMatrix3::MakeRotation`, nativa del motor, que sí sigue usando el giro del arma del punto 10 sin ningún cambio).
  - Desenganchada de `Throw::LaunchWeapon` y `Return::BeginReturnMovement`: sin creación/captura del `std::shared_ptr<Animation::WeaponTrail>` ni llamadas a `Start`/`Update`.
  - `Math::CatmullRom` (`9.- MATH/CurveMath`) y `Animation::GetVisualTransform` (`8.- ANIMATION/WeaponAnimation`) retirados también -- sin más uso que la estela, se quedaban como código muerto. `Math::EvaluateQuadraticBezier` (curva del regreso, punto 7) no se toca.
  - Constantes de `Constants.h` (`kTrailEffectPath`, `kTrailRootNodeName`, `kTrailSegmentLifetime`, `kTrailSegmentsPerSecond`, `kTrailBaseColor`/`kTrailBaseColorScaleMult`, `kTrailSegmentScale`, `kTrailAnchorLocalOffset`) eliminadas.
  - No numerada en `Mecanica del arma.txt` (pulido audiovisual propio, nunca un punto del documento), así que no hay ninguna mecánica documentada que reconsiderar tras el retiro.

## 2026-07-22 (sonido: `BSSoundHandle::Play()` no sonaba)

### v1.9.9

- **Localizada, tras una larga ronda de diagnóstico en el juego, la causa de que el sonido de atrape (v1.9.7) nunca llegara a oírse pese a que `BSAudioManager::GetSoundHandle`/`BSSoundHandle::Play()` reportaban éxito en cada paso.** Se añadieron diagnósticos sucesivos (`BSSoundHandle::IsPlaying()`/`GetDuration()` justo tras `Play()` y tick a tick después, un handle gemelo sin posición, precarga con `BSAudioManager::PrecacheDescriptor` en `kDataLoaded`, cambio del Output Model del Sound Descriptor de `SOMMono03000_verb` a uno sin reverb, comparación en vivo con `RE::PlaySound`) descartando uno a uno: carga asíncrona del recurso (`GetDuration()` sí llegaba a poblarse con un valor real, `4066`, e `IsPlaying()` a `true`, pero sin nada audible), atenuación 3D/posición (un handle sin `SetPosition` fallaba igual), el Output Model con reverb, y un problema de audio del sistema (los sonidos posicionales vanilla —pasos, impactos de arma— sonaban con normalidad en la misma partida). El propio `Play()` no funcionaba; `RE::BSSoundHandle::FadeInPlay(std::uint16_t)` -- función distinta, con su propio `RELOCATION_ID` -- sí.
  - `Audio::CatchSound`/`Audio::FlightSound::Start`/`Audio::PlaySoundOneShot` cambiados de `handle.Play()` a `handle.FadeInPlay(0)` (fundido de entrada de 0ms, equivalente a un arranque instantáneo).
  - Retirados todos los diagnósticos temporales de `CatchSound.cpp` (handle gemelo, log de seguimiento tick a tick, llamada de control a `RE::PlaySound`) una vez confirmada la causa real -- `CatchSound` vuelve a tener un único `RE::BSSoundHandle`.
  - `Audio::PrecacheAll` (`12.- AUDIO/SoundResolver.h/.cpp`), enganchado en `Events::OnSKSEMessage(kDataLoaded)`, se mantiene aunque no resultó ser la causa del silencio -- sigue siendo útil para evitar el retardo de carga asíncrona del recurso de audio en el primer uso de la partida.
  - **Corrección sobre lo anterior, tras retirar los diagnósticos**: con un único `RE::BSSoundHandle` el sonido de atrape volvió a quedarse completamente mudo (probado varios lanzamientos seguidos). Restaurados, esta vez como código de producción y no como diagnóstico: un segundo `RE::BSSoundHandle` (`CatchSound::primingHandle`) sin posición, arrancado con `FadeInPlay(0)` justo antes del handle real y mantenido vivo (sin `Stop()` hasta el destructor), y la llamada a `RE::PlaySound("CAP_ThorMjolnir_Sound_MjolnirCatch")` en el mismo instante -- confirmado en el juego (varios lanzamientos seguidos) que **las tres cosas a la vez** son necesarias para que el handle real suene; quitar cualquiera de las tres (probado una por una) lo deja mudo otra vez. Sin explicación firme de por qué -- ni `a_flags` de `BSAudioManager::GetSoundHandle` ni el pool interno de voces están documentados en `commonlibsse-ng` -- documentado como comportamiento empírico confirmado, no como código de prueba pendiente de limpiar.

## 2026-07-22 (regreso: suavizado del tramo final de llegada)

### v1.9.10

- **Suavizado del tramo final del regreso, a petición del usuario, para que el golpe/sonido de atrape no sea abrupto** -- sin invertir el perfil de aceleración creciente del punto 8 de `Mecanica del arma.txt` (confirmado con el usuario que se mantiene, solo se atenúa en el último tramo, ver `Constants::kReturnTailDistance`/`kReturnTailMinRate`).
  - `Return::BeginReturnMovement` (`5.- RETURN/ReturnManager.cpp`) ya no alimenta `Return::ComputeTraveledDistance` con el tiempo real transcurrido directamente: una vez la distancia a la mano cae por debajo de `kReturnTailDistance`, el tiempo "de progreso" avanza más despacio (curva suave tipo smoothstep hasta el suelo `kReturnTailMinRate`) -- el arma sigue el mismo perfil de aceleración de siempre, pero lo recorre más lento en tiempo real cerca de la mano, dando sensación de desaceleración hacia el atrape sin tocar la fórmula física (`Return::ComputeTraveledDistance`/`ComputeReturnAcceleration`/`ComputeReturnDuration` sin cambios).
  - Como `Audio::CatchSound` ya recalculaba su propia velocidad de cierre cada tick a partir de la distancia real (no de una predicción), este suavizado se propaga solo: más tiempo real disponible en el tramo final para el `SetFrequency` del sonido de atrape, golpe menos brusco.
  - `Constants::kReturnMaxDuration` subido de 2.0 a 2.3 segundos, para dar presupuesto de tiempo de sobra a este suavizado (que alarga la duración real del regreso un poco más allá de lo que predice `Return::ComputeReturnDuration`, que no lo conoce).
  - `Constants::kReturnTailDistance` (300 unidades) y `kReturnTailMinRate` (0.35) son placeholders sin calibrar en el juego.

## 2026-07-22 (rediseño: sonido de atrape en dos partes)

### v1.9.11

- **Sustituido por completo el sistema de sonido de atrape con ajuste continuo de velocidad (`Audio::CatchSound` + `RE::BSSoundHandle::SetFrequency` cada tick, v1.9.6-v1.9.9)**, a petición del usuario: en vez de estirar/comprimir un único clip para que su golpe grabado caiga siempre justo en la llegada, ahora son dos Sound Descriptor independientes -- `CAP_ThorMjolnir_Sound_MjolnirCatch_Start` (arranque, sonado con antelación, pequeña desincronización aceptada) y `CAP_ThorMjolnir_Sound_MjolnirCatch_End` (golpe final, disparado siempre exactamente en el instante real de la llegada, sin condición ni cálculo). Elimina de raíz el problema de fondo de la serie v1.9.6-v1.9.9 (el golpe grabado dependía de que el ajuste de frecuencia en vivo funcionara).
  - `Audio::CatchCue` (reemplaza a `Audio::CatchSound`, mismos archivos `12.- AUDIO/CatchSound.h/.cpp`): sin ningún `RE::BSSoundHandle` como miembro (cada sonido se dispara una vez y se deja sonar por su cuenta, igual que `Audio::PlaySoundOneShot`), solo lleva la cuenta de un retardo (`UpdateStart`, acumula tiempo tick a tick hasta dispararlo una sola vez) y expone `PlayEnd` (estático, incondicional).
  - `Return::BeginReturn` (`5.- RETURN/ReturnManager.cpp`) calcula el retardo del arranque **una sola vez, antes incluso del temblor de desprendimiento** (si el arma estaba clavada): `retardo = temblor (Constants::kStickShudderDuration si aplica) + duración de movimiento prevista (Return::ComputeReturnDuration) - Constants::kCatchStartSoundLeadTime`, tenido en cuenta a petición explícita del usuario para que un lanzamiento muy corto y clavado no deje el arranque sonando tarde. El `Audio::CatchCue` resultante se encadena por `std::shared_ptr` entre el bucle de tick del temblor y el del movimiento (`Return::BeginReturnMovement`, ahora con un parámetro extra), así su reloj interno cuenta de forma continua entre ambas fases.
  - Retirada toda la lógica anterior: ajuste de frecuencia, medición de velocidad de cierre tick a tick, la salvaguarda `HasStarted()`/disparo suelto sin sincronizar. `Constants::kCatchImpactSoundLeadTime`/`kCatchImpactSoundLocalFormID`/`kCatchSoundMinFrequency`/`kCatchSoundMaxFrequency` eliminadas, sustituidas por `kCatchStartSoundLeadTime` (1.066s) y `kCatchStartSoundLocalFormID`/`kCatchEndSoundLocalFormID`/`kCatchStartSoundEditorID`/`kCatchEndSoundEditorID`.
  - `Audio::PlayReliableOneShot` (función interna nueva en `CatchSound.cpp`, no expuesta): empaqueta el mecanismo de reproducción fiable ya confirmado en el juego para el diseño anterior -- un `RE::BSSoundHandle` de cebado sin posición dejado sonar por su cuenta, `RE::PlaySound(editorID)` en el mismo instante, y el handle real posicionado arrancado con `FadeInPlay(0)` (nunca `Play()`) -- documentado en `Constants::kFlightSoundHandleFlags` como comportamiento empírico confirmado, no como diagnóstico. Usado tanto para el arranque como para el golpe final.
  - FormID locales dados por el usuario desde xEdit: `0x00EA61` (Start -- mismo registro que el antiguo sonido de atrape único, ya confirmado en el juego que enmascara a `0x000A61` por el flag ESL) y `0x00F527` (End, registro nuevo, sin confirmar todavía -- se deja sin enmascarar a propósito para que `Audio::ResolveSoundDescriptor` lo confirme en el primer lanzamiento, mismo procedimiento que v1.9.7).
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos (todo el trabajo con `BSAudioManager`/`RE::PlaySound` ocurre dentro de los bucles de tick ya reencolados en el hilo principal; FormID resueltos por local+nombre de plugin, nunca absolutos; sin fugas de recursos nuevas).

## 2026-07-28 (Lanzar: sincronizado con la animación vía Open Animation Replacer)

### v1.9.12

- **El lanzamiento físico real (`Throw::LaunchWeapon`) ya no ocurre al soltar el botón, sino en el instante exacto en que la animación de lanzar suelta la mano** — investigación completa documentada en `_reference/PLAN-OAR.md`, sustituyendo por completo la vía descartada de parchear un estado/transición nuevo en `1hm_behavior.hkx` (Nemesis/Pandora, `ThorMjolnir_ReleaseThrow` nunca llegaba a disparar).
  - **Arquitectura final**: Open Animation Replacer sustituye `Animations\1HM_AttackRight.hkx` (un ataque ligero vanilla ya existente, sin tocar estados/transiciones/tabla de eventos del behavior) por `Throw.hkx`, condicionado a una graph variable propia (`Constants::kThrowTriggerGraphVariable`, `"ThorMjolnir_ThrowTrigger"`). El propio código dispara el evento vanilla de siempre (`Constants::kThrowAnimationEvent`, `"attackStart"`) para reproducirlo.
  - **La pieza que costó encontrar**: una anotación custom dentro de un `.hkx` (aquí, el instante de soltar la mano) nunca se convierte en evento real si su nombre no está ya en la tabla de eventos vanilla del behavior — ni sustituirla vía OAR ni registrarla vía AnimData (Nemesis) cambia eso. Solución: **[Payload Interpreter](https://www.nexusmods.com/skyrimspecialedition/mods/65089)** (nueva dependencia externa de mod, documentada en `CLAUDE.md`), que registra un único evento "tonto" (`PIE`) en la tabla vanilla vía su propio parche de Nemesis; el contenido real de la anotación va como *payload* (aquí, `Throw.hkx` lleva `PIE.MjolnirThrow`), no como parte del nombre del evento.
  - Nuevo estado `State::kThrowing` (`3.- WEAPON/WeaponState.h`) entre "apuntando" y "lanzada": el arma sigue físicamente en la mano mientras se reproduce `Throw.hkx`, hasta que llega la anotación de liberación.
  - `WeaponManager::OnAimButtonUp` ya no llama a `ThrowWeapon` directamente: `BeginThrowAnimation` activa la graph variable y dispara el evento; `OnThrowReleaseAnimationEvent` (llamado desde el nuevo event sink `Events::ThrowReleaseWatcher`, `RE::BSTEventSink<RE::BSAnimationGraphEvent>` registrado sobre el jugador en `kNewGame`/`kPostLoadGame` — no en `kDataLoaded`, demasiado pronto: el actor del jugador no tiene animation graph real todavía ahí) es quien de verdad llama a `ThrowWeapon`.
  - `Animation::SetThrowTrigger` (`8.- ANIMATION/WeaponAnimation.h/.cpp`) envuelve `SetGraphVariableBool`, mismo criterio que `TickSpin`/`TickShudder`: el nombre de la variable vive en un solo sitio.
  - `WeaponManager::OnLoadingScreenClosed` gestiona `kThrowing` igual que `kAiming` (el arma no se ha desequipado todavía en ninguno de los dos), apagando también la graph variable para no dejarla encendida.
  - Archivos de Open Animation Replacer del mod real (no confundir con la investigación desechable, que vivió en un mod de prueba aparte ya borrado): `Data/meshes/actors/character/animations/OpenAnimationReplacer/ThorMjolnir/Lanzar/` — dejado con `disabled: true` hasta configurar la condición real (`CompareValues` sobre `ThorMjolnir_ThrowTrigger`) desde el editor in-game de OAR, formato de condición no verificado para escribirlo a mano.

### v1.9.13

- **Ciclo completo confirmado en el juego de punta a punta**, varias veces seguidas (apuntar → soltar → `Throw.hkx` entero → lanzamiento físico sincronizado con la anotación real → clavado/regreso/reequipado) — cierre de la Fase 3 de `_reference/PLAN-OAR.md`.
- Corregido el motivo real por el que la anotación de liberación nunca llegaba al event sink en las pruebas: `Events::ThrowReleaseWatcher` se registraba en `kNewGame`/`kPostLoadGame`, pero ninguno de los dos se dispara al entrar a la partida con `coc <celda>` desde la consola del menú principal (el método de prueba habitual usado). Registro movido a perezoso e idempotente (`Events::EnsureAnimationSinksRegistered`, `static bool` guard), llamado desde `WeaponManager::BeginAiming` en vez de depender de esos mensajes.
- Corregido el tag real de Payload Interpreter: `Constants::kPayloadInterpreterTag` decía `"PIE"`, el evento real (confirmado en el Animation Event Log de OAR) es `"Pie"` — `RE::BSFixedString::operator==` compara contenido exacto, sin normalizar mayúsculas/minúsculas.
- `Animation::SetThrowTrigger` pasa a escribir la graph variable como `Float` (`SetGraphVariableFloat`), no `Bool` — para que coincida con el tipo con el que quedó montada la condición `CompareValues` en el editor de OAR.
- Restaurada la red de seguridad del lanzamiento físico (`Constants::kThrowReleaseFallbackWindow`, hilo de respaldo en `WeaponManager::BeginThrowAnimation`): el lanzamiento debe ocurrir siempre, aunque la anotación real nunca llegue (OAR/Payload Interpreter desinstalado, o el ataque se interrumpe) — decisión del usuario: solo la sincronía *visual* con la animación es best-effort, nunca el lanzamiento en sí.
- Restaurada la condición real (`CompareValues` sobre `ThorMjolnir_ThrowTrigger == 1.0`, Float) en el submod `Lanzar` — se había dejado sin condición (`conditions: []`) para aislar la prueba del mecanismo de entrega de la anotación; sin ella, cualquier ataque ligero normal (no solo soltar el botón de lanzar) disparaba `Throw.hkx` también.
- Quitado el log de diagnóstico que volcaba sin filtrar cada `BSAnimationGraphEvent` recibido por el jugador (pisadas, IK, swings normales...) — ya no hace falta con el pipeline confirmado, y era una escritura de log por cada evento de animación de toda la partida.
- **Corte visual al soltar el arma**: desequipar el arma real (`UnequipObject`) en el mismo instante de la anotación de liberación interrumpía `Throw.hkx` a mitad y saltaba a la pose de desarmado (el motor reevalúa el estado de combate en cuanto cambia el arma equipada; `attackStop` está horneado en el grafo a un tiempo fijo casi simultáneo a la propia anotación, no señala el final visual real del clip sustituido). `WeaponManager::ThrowWeapon` ahora oculta el nodo `WEAPON` del arma real (`Animation::SetEquippedWeaponHidden`, nuevo) en vez de desequiparla en el acto — la réplica toma el relevo visual de inmediato — y difiere el desequipado real `Constants::kThrowReleaseVisualHoldDuration` (placeholder, pendiente de ajustar en el juego), con guarda por si el ciclo ya se completó y reinició antes de que venza ese margen.

### v1.9.14

- **La condición real del submod `Lanzar` (`ThorMjolnir_ThrowTrigger == 1.0`) no se cumplía nunca** — confirmado en el juego que `Throw.hkx` dejaba de reproducirse por completo en cuanto se restauró la condición (v1.9.13). Causa raíz: `Actor::SetGraphVariableFloat`/`GetGraphVariableFloat` fallan en silencio sobre un nombre de variable inventado por el plugin y no declarado en la tabla interna del behavior graph — mismo problema de fondo que ya resolvió Payload Interpreter para eventos custom, esta vez para variables. Confirmado leyendo el código fuente real de Open Animation Replacer: su condición de graph variable llama a esa misma función.
  - Solución: nueva dependencia externa de mod **[BehaviorDataInjector](https://www.nexusmods.com/skyrimspecialedition/mods/78146)** (ya dependencia transitiva vía `SkipEquipAnimation`, ahora requisito directo), documentada en `CLAUDE.md`. Config `Data/SKSE/Plugins/BehaviorDataInjector/ThorMjolnir_BDI.json` (plantilla verificada contra el JSON real de `Kratos Combat` en `_reference/`) declara `ThorMjolnir_ThrowTrigger` como `kFloat` — con esto, la variable tiene almacenamiento real y tanto el propio plugin como la condición del submod de OAR la leen correctamente. Confirmado en el juego (2026-07-29).

### v1.9.15

- **Despliegue unificado con el mod de assets (nif/sonidos/ESP)**: el target de xmake pasa de `"Throwing-Weapons"` a `"ThorMjolnir"` — la regla `commonlibsse-ng.plugin` calcula `installdir` como `XSE_TES5_MODS_PATH/<nombre del target>`, así que el DLL/INI compilados caen directamente en la misma carpeta de mod (`ThorMjolnir`) que el resto del mod real, en vez de en una carpeta separada. Decisión del usuario, solo afecta al despliegue, no a la organización del código fuente en este repo.
- **Renombrados todos los nombres internos que aún decían "Throwing Weapons"/"ThrowingWeapons", a efectos de claridad** ahora que el mod es uno solo: nombre/descripción del plugin en `xmake.lua` (afecta también al nombre del log, `ThorMjolnir.log`), `Data/SKSE/Plugins/ThrowingWeapons.ini` → `ThorMjolnir.ini` (y `Constants::kInputConfigPath` a juego), semilla del `kPluginUniqueID` del cosave. Sin efecto funcional esperado salvo el propio cosave: cualquier partida guardada con este plugin a mitad de un ciclo bajo el ID antiguo dejaría de reconocerse tras este cambio (riesgo aceptado, fase de desarrollo temprana sin partidas de este tipo que preservar).

### v1.9.16

- **Moverse durante Lanzar escalaba a un power attack direccional vanilla real** (`1HM_AttackPowerFwd`/`Bwd`/`Left`/`Right`, con daño real) en vez de reproducir `Throw.hkx` — confirmado en el juego con el Animation Event Log de OAR: `Pie.MjolnirThrow` nunca llegaba a dispararse si el jugador se movía antes de la anotación de liberación (moverse después sí es seguro). Es una mecánica vanilla de Skyrim (atacar en movimiento = power attack direccional), no un bug de sustitución de OAR — se descartó una pista inicial (`MLh_1HM_AttackRight_Behavior`, otro clip del behavior) tras verificar que esa rama se selecciona por tipo de arma (`iRightHandType`), no por movimiento.
  - Solución: `Input::SetMovementLocked` (`2.- INPUT/InputManager.h/.cpp`, envuelve `RE::ControlMap::ToggleControls` sobre `USER_EVENT_FLAG::kMovement`) bloquea el movimiento del jugador durante todo `State::kThrowing` — desde `WeaponManager::BeginThrowAnimation` hasta `OnThrowReleaseAnimationEvent` (cubre tanto la anotación real como la red de seguridad), con desbloqueo también en los caminos de recuperación (`OnLoadingScreenClosed`, `ResetToInHand`).
  - **Insuficiente por sí solo, confirmado en el juego**: bloquear input *nuevo* no frena el movimiento/momentum que el jugador ya llevaba en el instante exacto de soltar el botón, así que la escalada a power attack seguía ocurriendo (en ambas direcciones, adelante y atrás, no solo una — confirmado con el Animation Event Log que ambas mostraban `PowerAttack_Start_end` + golpes reales antes de, si acaso, llegar `Pie.MjolnirThrow`).
  - **Solución definitiva, confirmada en el juego (2026-07-29)**: `Animation::SetAnimationDriven` (`8.- ANIMATION/WeaponAnimation.h/.cpp`) activa `Constants::kAnimationDrivenGraphVariable` (`"bAnimationDriven"`, variable **vanilla**, ya existente en la tabla de variables de `1hm_behavior.hkx` — no hace falta BehaviorDataInjector para esta) junto con `Input::SetMovementLocked` durante todo `State::kThrowing`. Con las dos combinadas, moverse en cualquier dirección durante Lanzar ya no escala a power attack.

### v1.9.17

- **Arma real duplicada visualmente junto a la réplica tras soltar el proyectil, confirmado en el juego** — `WeaponManager::ThrowWeapon` ocultaba el nodo equivocado. `Actor::GetNodeByName("WEAPON")` da el hueso de enganche del esqueleto, no la malla del arma: tiene un único hijo, un `BSFadeNode` (la malla real) — confirmado con un log de diagnóstico añadido para esto. Ocultar el propio hueso (primero con `SetAppCulled`, luego con el flag `kHidden`) no ocultaba nada visualmente; el flag hay que ponerlo en el `BSFadeNode` hijo. `Animation::SetEquippedWeaponHidden` corregido para iterar `GetChildren()` del nodo `WEAPON` y ocultar cada hijo directamente. Hallazgo documentado en `CLAUDE.md` (sección de APIs verificadas) para reutilizarlo en Llamada/Catch.
- **Desenvainado en dos pulsaciones, igual que el ataque cuerpo a cuerpo vanilla con el arma envainada**: pulsar el botón de apuntar con el arma envainada solo la desenvaina ahora (sin empezar a apuntar); hace falta una segunda pulsación, ya desenvainada, para entrar de verdad en `State::kAiming`. Antes, la primera pulsación desenvainaba y apuntaba a la vez, y el arma salía disparada sin llegar a reproducir `Throw.hkx` (el propio desenvainado, con su propia animación real, no había terminado todavía en el instante de disparar `attackStart`). `WeaponManager::OnAimButtonDown` comprueba `Actor::AsActorState()->IsWeaponDrawn()` antes de decidir; `BeginAiming` ya no fuerza el desenvainado (redundante ahora, y resuelve el riesgo de no-idempotencia ya anotado en el código: llamar `DrawWeaponMagicHands` con el arma ya desenvainada podía dar un blip visual).

## 2026-08-03 (Llamada: segunda animación sincronizada vía Open Animation Replacer)

### v1.10.1

- **El regreso físico real (`WeaponManager::BeginReturn`) ya no ocurre al pulsar el botón de recuperar, sino en el instante en que la animación de Llamada llega al chasquido de dedos** — mismo patrón que Lanzar, con una diferencia clave: `Call.hkx` no lleva ninguna anotación de Payload Interpreter, solo un evento vanilla nativo `SoundPlay.CAP_ThorMjolnir_MarkSound_FingerSnap` (decisión tomada con el usuario: usar ese evento, que el motor ya procesa sin depender de Payload Interpreter/BehaviorDataInjector, en vez de un retardo fijo sin calibrar).
  - Nuevo estado `State::kCalling` (`3.- WEAPON/WeaponState.h`) entre `kThrown`/`kStuck` y `kReturning`: el arma sigue fuera de la mano (mismo estado físico que `kThrown`/`kStuck`) mientras se reproduce `Call.hkx`, hasta que llega el evento de chasquido (o la red de seguridad, `Constants::kCallReleaseFallbackWindow`, mismo criterio que `kThrowReleaseFallbackWindow`).
  - `WeaponManager::OnAimButtonDown` (casos `kThrown`/`kStuck`) ya no llama a `BeginReturn` directamente: `BeginCallAnimation` activa `Constants::kCallTriggerGraphVariable` (`"ThorMjolnir_CallTrigger"`, ya declarada en `ThorMjolnir_BDI.json`) y dispara el mismo evento vanilla que Lanzar (`Constants::kLightAttackAnimationEvent`, renombrado de `kThrowAnimationEvent` porque ahora lo comparten Lanzar y Llamada, ambos sobre el mismo slot `1HM_AttackRight` con condiciones mutuamente excluyentes); `OnCallReleaseAnimationEvent` (llamado desde el nuevo event sink `Events::CallReleaseWatcher`) es quien de verdad llama a `BeginReturn`.
  - `BeginReturn` pasa a recibir `a_wasStuck` por parámetro en vez de recalcularlo de `weaponState.GetState()` — ya no es fiable una vez el estado pasa por `kCalling` (que no es `kStuck`). Capturado en `BeginCallAnimation` antes de cambiar de estado (`wasStuckBeforeCalling`, nuevo miembro de `WeaponManager`) y en el propio callback `onAutoRecall` de `ThrowWeapon` (que sigue llamando a `BeginReturn` directamente, sin pasar por Llamada).
  - `Animation::SetCallTrigger` (`8.- ANIMATION/WeaponAnimation.h/.cpp`) envuelve `SetGraphVariableFloat`, mismo patrón que `SetThrowTrigger`.
  - `WeaponManager::OnLoadingScreenClosed` gestiona `kCalling` apagando la graph variable y `bAnimationDriven` y desbloqueando el movimiento, pero llamando a `RecallWeapon` (no un simple cambio de estado como `kThrowing`) — el arma sigue fuera de la mano en este estado, a diferencia de `kThrowing`/`kAiming`.
  - `CaptureSaveData` incluye `kCalling` en `cycleActive` (mismo motivo que `kThrown`/`kStuck`/`kReturning`: la réplica sigue en el mundo, se perdería si se guarda/carga a mitad sin recuperarla).
  - `ResetToInHand` apaga también `ThorMjolnir_ThrowTrigger`/`CallTrigger` (hueco preexistente, cerrado de paso: antes solo apagaba `bAnimationDriven` y desbloqueaba el movimiento).
  - Archivos de Open Animation Replacer del mod real: `Data/meshes/actors/character/animations/OpenAnimationReplacer/ThorMjolnir/Call/` (mismo `1HM_AttackRight.hkx` renombrado, condición `CompareValues` sobre `ThorMjolnir_CallTrigger == 1.0` ya puesta y habilitada). El submod `Catch` queda con la misma estructura y condición sobre `ThorMjolnir_CatchTrigger`, pero deshabilitado y sin código C++ que lo active todavía.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.2

- **El evento `SoundPlay.CAP_ThorMjolnir_MarkSound_FingerSnap` de v1.10.1 no llegó a sustituir la animación vanilla en el juego** — confirmado por el usuario: con el arma en mano quieto o en movimiento, sonaba/se veía siempre el ataque ligero vanilla (mano derecha), nunca `Call.hkx`, y el regreso solo arrancaba al terminar esa animación vanilla (la red de seguridad por tiempo, no el evento real). Sustituido por completo el mecanismo de sincronía de Llamada, a petición del usuario: misma anotación de Payload Interpreter que Lanzar (tag `"Pie"` + payload separado), payload propio `Constants::kCallReleasePayload` (`"ThorMjolnirCall"`) en vez de `SoundPlay.*`.
  - `Events::CallReleaseWatcher` compara `a_event->tag == Constants::kPayloadInterpreterTag && a_event->payload == Constants::kCallReleasePayload`, igual que `ThrowReleaseWatcher`.
  - El sonido del chasquido de dedos, al dejar de depender de un `SoundPlay` vanilla, se dispara ahora desde el propio `WeaponManager::OnCallReleaseAnimationEvent` en el mismo instante que el regreso físico real -- `Audio::PlayReliableOneShot` (movida de una función privada de `12.- AUDIO/CatchSound.cpp` a `12.- AUDIO/SoundResolver.h/.cpp` para compartirla, `CatchCue` ahora la reutiliza en vez de su propia copia) en vez del más simple `Audio::PlaySoundOneShot` -- este último nunca se ha confirmado fiable en el juego, mientras que el mecanismo triple ya está probado repetidas veces con los sonidos de atrape. FormID local dado por el usuario desde xEdit: `0x011579` (del FormID completo `0x01011579`, con el byte de índice de carga `01` descartado). Añadido a `Audio::PrecacheAll`.
  - De paso, igualada la prioridad del submod `Call` en OAR (`1` → `1000`, misma que `Throw`) -- posible causa real de que la animación vanilla ganara en vez de `Call.hkx`, si algún otro mod del modlist ya reemplaza el mismo slot `1HM_AttackRight` con prioridad mayor. Sin confirmar todavía si esta era la causa de fondo o si bastaba con el cambio de anotación.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.3

- **Ningún sonido del arma sonaba en el juego (ni el nuevo del chasquido, ni los de atrape ya confirmados en v1.9.7) -- confirmado en el log, `Audio::ResolveSoundDescriptor` fallaba para *todo* FormID, incluidos los ya funcionando antes.** Causa: `Constants::kSoundPluginName` seguía apuntando a `"ThorMjolnir.esp"`, pero el ESP realmente activo en esta partida (la carpeta de mod `ThorMjolnir_OAR`, duplicada de la original) es `ThorMjolnirOAR.esp` -- `RE::TESDataHandler::LookupForm` busca por nombre de plugin exacto, así que no encontraba nada bajo ningún FormID. Cambiado `kSoundPluginName` a `"ThorMjolnirOAR.esp"` -- **exclusivo de esta rama** (`oar`), mismo motivo que el target de `xmake.lua` ya renombrado en su día (v1.9.15/rama oar): la rama `behavior`/`main` sigue con el ESP original `ThorMjolnir.esp`, sin tocar.
  - Sin relación con el problema de que la animación vanilla siga ganando en vez de `Call.hkx` (sistemas independientes: resolución de audio vs. selección de clip en OAR) -- ese sigue sin explicación, pendiente de revisar el Animation Log en vivo de OAR en el instante de pulsar recuperar.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.4

- **Localizada la causa de que `Call.hkx` nunca llegara a reproducirse, vía el Animation Event Log de OAR** (confirmado en el juego, escribe también a `OpenAnimationReplacer.log` con `bAnimationEventLogWriteToTextLog`): las dos pulsaciones de Llamada probadas dispararon `PowerAttack_Start_end` (con la secuencia completa de un ataque cargado vanilla real -- `preHitFrame`/`weaponSwing`/`HitFrame`/`AttackWinStart`/`attackStop`), nunca la secuencia del ataque ligero que sustituye nuestro submod de OAR, y `Pie.ThorMjolnirCall` no aparece en ningún punto del log (mientras que `Pie.MjolnirThrow`, de Lanzar, sí). Confirmado con el usuario que el personaje estaba quieto en ambas pruebas -- descarta el mismo bug de movimiento ya resuelto para Lanzar en v1.9.16.
  - Causa real: `WeaponManager::BeginCallAnimation` disparaba `Constants::kLightAttackAnimationEvent` (`"attackStart"`) desde `OnAimButtonDown`, es decir, en el instante de **pulsar** el botón -- a diferencia de `BeginThrowAnimation`, que lo dispara desde `OnAimButtonUp`, al **soltar**. El motor decide ataque ligero vs. cargado según si el botón real sigue "pulsado" en el instante de `attackStart`; disparándolo al soltar (como ya hacía Lanzar) se evita esa ambigüedad por construcción.
  - `WeaponManager::OnAimButtonDown` ya no gestiona `kThrown`/`kStuck` (cae al `default`, sin efecto, mismo criterio que `kThrowing`); `OnAimButtonUp` pasa a gestionar ambos casos (`kAiming` → `BeginThrowAnimation`, `kThrown`/`kStuck` → `BeginCallAnimation`). Ningún cambio de UX perceptible para una pulsación normal (pulsar y soltar siguen sintiéndose instantáneos), solo cambia en qué transición del mismo evento de botón se dispara `attackStart` internamente.
  - Sin confirmar todavía en el juego -- pendiente de que el usuario vuelva a probar.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.5

- **La hipótesis de v1.10.4 (pulsar vs soltar) era incorrecta -- probado en el juego, seguía reproduciéndose exactamente la misma secuencia vanilla tras el cambio.** Causa real, esta vez localizada con certeza vía el panel "Animation Log" de OAR (distinto del Animation Event Log, muestra el archivo real que se activa en cada instante): el arma real ya está desequipada en el momento de Llamada (se desequipó al lanzarla, mucho antes) -- punto 4 de `Mecanica del arma.txt`, el jugador golpea a puños mientras el arma está fuera de la mano. El motor, con las manos vacías, no reproduce `1HM_AttackRight.hkx` en absoluto al recibir `attackStart` -- reproduce `H2H_AttackRight.hkx` (clip `H2H_RightHook`, combate cuerpo a cuerpo), confirmado en el propio Animation Log sin ninguna etiqueta `[Replaced]` (a diferencia de la entrada de Lanzar, que sí la lleva: `Mod: ThorMjolnir Submod: Lanzar`). El submod de Llamada sustituía el archivo equivocado desde el principio -- no era un problema de prioridad, de anotación, ni de tiempo de pulsación, los tres cambios anteriores (v1.10.2 a v1.10.4) no tenían ninguna posibilidad de arreglarlo.
  - Renombrado el `.hkx` dentro de los submods `Call` y `Catch` (carpeta de mod, no el repositorio de código) de `1HM_AttackRight.hkx` a `H2H_AttackRight.hkx` -- `Catch` incluido de una vez aunque siga sin implementar en C++, mismo motivo (el atrape también ocurre con el arma real todavía fuera de la mano, hasta el reequipado final). Descripciones de ambos `config.json` actualizadas a juego.
  - Cambio puramente de datos (carpeta de mod), sin ningún cambio de código -- no hace falta recompilar.
  - Pendiente de confirmar en el juego.

### v1.10.6

- **Confirmado en el juego: `Call.hkx` ya sustituye al ataque desarmado correctamente con el jugador quieto (v1.10.5 arregló el problema de fondo).** Pero moviéndose durante Llamada, el personaje no se detiene y a veces vuelve a reproducirse el ataque desarmado vanilla en vez de `Call.hkx` -- mismo síntoma que el power attack de Lanzar (v1.9.16: moverse cambia la variante del ataque), pero esta vez en la rama de combate cuerpo a cuerpo, no la de una mano. Sospecha: `Animation::SetAnimationDriven` (`bAnimationDriven`) es lo que de verdad evitaba la escalada para Lanzar, pero esa variable se comprueba dentro de la rama de combate a una mano -- puede que no tenga el mismo efecto en la rama de cuerpo a cuerpo, dejando `Input::SetMovementLocked` solo (que ya se sabe, desde v1.9.16, que no basta por sí solo: no frena el movimiento/momentum ya en marcha).
  - Experimento: `WeaponManager::BeginCallAnimation` dispara `Constants::kMoveStopAnimationEvent` (`"moveStop"`, el mismo evento vanilla que decide Standing vs Locomotion en el grafo, ver `PLAN-animaciones.md`) justo antes de `attackStart`, a ver si fuerza la variante quieta del ataque desarmado sin más cambios.
  - Sin confirmar todavía en el juego.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.7

- **Probado en el juego (moveStop de v1.10.6 sí ayudó en parte): moviéndose hacia delante/atrás durante Llamada, `Call.hkx` ya se reproduce (con un ligero deslizamiento visual del personaje, sin resolver todavía) -- pero moviéndose lateralmente sigue reproduciéndose el ataque desarmado vanilla.** Diagnosticado por el usuario, mismo patrón que las variantes direccionales de power attack de Lanzar (v1.9.16): moverse a los lados selecciona un archivo vanilla *distinto* del que sustituye nuestro submod (`H2H_AttackRight.hkx`, solo la variante estática/de frente).
  - Añadidos `H2H_RunLeftAttackRight.hkx`/`H2H_RunRightAttackRight.hkx` (copias de la misma `Call.hkx`) dentro del mismo submod `Llamada` en la carpeta de mod -- un submod de OAR puede sustituir varios archivos vanilla a la vez bajo la misma condición, no hace falta un submod ni un `config.json` por variante. Nombres dados por el usuario (patrón de nomenclatura vanilla ya conocido de las variantes de `1HM_Run*AttackRight`, aplicado a la rama desarmada `H2H_*`).
  - Cambio puramente de datos, sin código -- no hace falta recompilar.
  - Pendiente de confirmar en el juego; el deslizamiento hacia delante/atrás sigue sin investigar (posible equivalente de `bAnimationDriven` para la rama de cuerpo a cuerpo, sin identificar todavía).

### v1.10.8

- **Sustitución de animación de Llamada confirmada como resuelta en las variantes probadas hasta ahora (de frente, lateral izquierda/derecha) -- queda el deslizamiento del personaje al moverse (y, según v1.10.9, alguna variante direccional más sin cubrir).** A petición del usuario ("¿vale la pena mantener el atributo desarmado, o hay que afrontarlo de otra manera?"): se acuerda seguir intentando arreglarlo dentro de la rama desarmada actual antes de explorar alternativas de arquitectura (p. ej. mantener el personaje "armado" a nivel de grafo con un arma señuelo durante Llamada/Atrape, reutilizando la rama de una mano ya fiable de Lanzar) -- documentado como opción a futuro si esto no basta.
  - Verificado contra los headers reales (`skill verify-commonlibsse-api`): `RE::Actor::GetCharController() const` (`Actor.h:557`, directo en la clase, sin riesgo de offset) y `RE::bhkCharacterController::SetLinearVelocityImpl(const hkVector4&)` (`bhkCharacterController.h:89`, virtual pura). `RE::hkVector4{}` (constructor por defecto, `hkVector4.h:11`) ya vale como vector cero.
  - `WeaponManager::BeginCallAnimation` fuerza la velocidad física a cero (`controller->SetLinearVelocityImpl(RE::hkVector4{})`) justo al bloquear el movimiento -- `Input::SetMovementLocked` por sí solo bloquea el input *nuevo*, no el momentum físico ya en marcha (mismo motivo documentado para Lanzar en v1.9.16, pero `bAnimationDriven` no parece bastar aquí para la rama de cuerpo a cuerpo). Un solo tirón al empezar el gesto, no repetido por tick -- a confirmar en el juego si con eso basta o hace falta repetirlo mientras dura Llamada.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Sin confirmar todavía en el juego.

### v1.10.9

- **Moviéndose hacia atrás durante Llamada, a veces se reproducía el ataque desarmado vanilla en vez de `Call.hkx`** -- confirmado en el Animation Log de OAR: `H2H_RunBackAttackRight.hkx` sin etiqueta `[Replaced]` (mientras que, en el mismo log, `H2H_RunRightAttackRight.hkx` y `H2H_AttackRight.hkx` sí la llevaban, `Mod: ThorMjolnir Submod: Llamada`). Mismo patrón que las variantes lateral (v1.10.7): un archivo vanilla más que nuestro submod no cubría todavía.
  - Añadido `H2H_RunBackAttackRight.hkx` (copia de `Call.hkx`) al submod `Llamada`, mismo criterio que las anteriores.
  - Que fuera intermitente ("algunas veces") encaja con una carrera de tiempos entre el `moveStop`/velocidad a cero de v1.10.6/v1.10.8 (que a veces sí llega a forzar la variante quieta a tiempo) y el muestreo de dirección del motor -- con este archivo cubierto, ya no importa cuál de las dos variantes gane esa carrera.
  - Cambio puramente de datos, sin código -- no hace falta recompilar.
  - Sin confirmar todavía en el juego; no se ha visto (todavía) una variante equivalente para adelante (`H2H_RunAttackRight`/similar) -- si aparece, mismo tratamiento.

### v1.10.10

- **No se puede empezar a apuntar/lanzar el arma estando agachado** -- decisión del usuario, no cubierta por `Mecanica del arma.txt` (no menciona el sigilo). `WeaponManager::BeginAiming` comprueba `Actor::AsActorState()->IsSneaking()` (verificado en `lib/commonlibsse-ng/include/RE/A/ActorState.h:199`, mismo patrón ya usado para `IsWeaponDrawn()`) y no hace nada si el jugador está agachado -- centralizado ahí (no en `OnAimButtonDown`) para que aplique igual desde la primera pulsación que desde la resincronización de `kAiming` (pulsación repetida por botón de soltar perdido).
  - El arma sigue pudiendo desenvainarse en sigilo (ese paso no se toca) -- solo se bloquea el apuntado/lanzamiento en sí.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.11

- **Abandonada la rama de combate cuerpo a cuerpo (H2H) para Llamada, tras agotar el margen razonable de parches (v1.10.1 a v1.10.9: cuatro variantes direccionales cubiertas una a una, `moveStop`, velocidad física a cero) sin eliminar el deslizamiento del personaje al moverse durante el gesto.** Decisión del usuario: en vez de seguir ahí, mantener al personaje "armado" a nivel de animation graph solo durante el gesto (Llamada, y Atrape cuando se implemente) con un arma señuelo equipada y oculta, reutilizando la rama de una mano ya fiable de Lanzar -- sin ningún cambio visible para el jugador (sigue viendo `Call.hkx`, y sigue genuinamente desarmado el resto del ciclo -- volando, clavado, volviendo -- tal como pide el punto 4 de `Mecanica del arma.txt`).
  - `WeaponManager::EquipGestureWeapon`/`UnequipGestureWeapon` (nuevos, pensados para reutilizarse en Atrape): equipan/desequipan la misma arma ya trackeada por `weaponState` (no un arma señuelo nueva, evita depender de un FormID vanilla externo), suprimiendo la animación de equipar con `SkipEquipAnimation` (mismo truco que `ReequipAndReset`). Ver v1.10.12 para el ocultado (`Animation::SetEquippedWeaponHidden`, mismo truco que `ThrowWeapon`), que se movió fuera de `EquipGestureWeapon`.
  - `Events::EquipGuard` (que desequipa automáticamente cualquier cosa equipada mientras el ciclo está en marcha, para cumplir el punto 4) ahora respeta un nuevo `WeaponManager::IsEquipGuardSuppressed()`, activado por `EquipGestureWeapon` mientras dura su propio equipado -- sin esto, el propio guard habría deshecho el equipado del señuelo de inmediato.
  - `BeginCallAnimation` llama a `EquipGestureWeapon()` antes de disparar `attackStart`; `OnCallReleaseAnimationEvent` llama a `UnequipGestureWeapon()` antes de `BeginReturn` (vuelve a desarmado genuino para el vuelo de regreso, el "armado" solo dura el gesto).
  - Quitados los band-aids específicos de H2H, ya sin motivo en la rama de una mano: `Constants::kMoveStopAnimationEvent` y el bloque `GetCharController()->SetLinearVelocityImpl(...)` en `BeginCallAnimation`.
  - Carpeta de mod (datos, no código): revertido el submod `Call` de OAR a apuntar otra vez a `1HM_AttackRight.hkx` (como Lanzar), borradas las variantes `H2H_RunLeftAttackRight`/`H2H_RunRightAttackRight`/`H2H_RunBackAttackRight` (ya no aplican). Mismo revertido en `Catch` para no dejarlo inconsistente de cara a cuando se implemente.
  - Logs de diagnóstico añadidos en `EquipGestureWeapon`/`UnequipGestureWeapon` y en `Events::EquipGuard` (cuándo se deja pasar un equipado por supresión, distinto del camino normal de `kInHand`), para poder verificar toda la secuencia leyendo `ThorMjolnir_OAR.log` sin depender solo del Animation Log en vivo de OAR.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Sin confirmar todavía en el juego.

### v1.10.12

- **Probado en el juego: v1.10.11 no funcionó -- al pulsar el botón de recuperar seguía reproduciéndose el ataque desarmado (H2H), y además con el arma real visible.** Confirmado en el log (`ThorMjolnir_OAR.log`): `EquipGestureWeapon`/`Animation::SetEquippedWeaponHidden` se ejecutaban sin ningún aviso (nodo `WEAPON` encontrado, con hijos), pero el arma seguía viéndose y el ataque seguía siendo desarmado -- mismo motivo ya documentado para `ReequipAndReset` en otro contexto: `EquipObject` no aplica el equipado real (ni la selección de rama de combate, ni el nodo 3D final) en el mismo instante en que se llama.
  - Reestructurado `BeginCallAnimation`: `EquipGestureWeapon()` ya no oculta el arma -- solo equipa. Un tick real de espera (`Constants::kTickInterval`, mismo patrón hilo-que-duerme-y-reencola de todo el proyecto) antes de ocultar (`Animation::SetEquippedWeaponHidden`), activar `ThorMjolnir_CallTrigger` y disparar `attackStart` -- ahora sí con el equipado ya aplicado de verdad.
  - Sin confirmar todavía en el juego si un solo tick basta, o si hace falta esperar más.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.10.13

- **Probado en el juego: v1.10.12 (un solo tick de espera) funcionaba a veces sí, a veces no -- comportamiento dispar.** Confirma que el retardo de `EquipObject` hasta aplicar el equipado real varía de una vez a otra (no es un número fijo de fotogramas), así que un único tick de margen no es fiable.
  - `Animation::SetEquippedWeaponHidden` pasa a devolver `bool` (antes `void`) -- `true` si encontró el nodo `WEAPON` con hijos y pudo ocultarlos, `false` si no (arma señuelo aún no lista). `WeaponManager::ThrowWeapon` sigue llamándola igual, ignorando el valor de vuelta (sin cambio de comportamiento ahí).
  - Nuevo `WeaponManager::PollGestureWeaponReady(int a_attemptsLeft)`: sustituye el tick único fijo por un **sondeo** -- reintenta `SetEquippedWeaponHidden` cada `Constants::kTickInterval` hasta que devuelva `true` o se agoten `Constants::kGestureWeaponReadyMaxAttempts` (nuevo, placeholder de 10 intentos ≈160ms, con aviso en el log si se agotan sin éxito). Solo entonces activa `ThorMjolnir_CallTrigger`, dispara `attackStart` y arranca la red de seguridad -- ese tramo final se movió aquí desde `BeginCallAnimation`, que ahora solo equipa el señuelo y arranca el sondeo.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos (sondeo acotado a 10 intentos como máximo, ~160ms, no es un bucle sin límite ni trabajo pesado por fotograma).
  - Sin confirmar todavía en el juego.

### v1.10.14

- **Probado en el juego: v1.10.13 seguía fallando -- volvía el ataque desarmado con el arma visible.** Localizada la causa real en el log: `Animation::SetEquippedWeaponHidden` devolvía `true` **en el mismo instante** que `EquipObject`, sin ningún reintento, en las tres pruebas -- el nodo `WEAPON` ya tenía hijos y se dejaba ocultar de inmediato, pero ese nodo no reflejaba el equipado real todavía (se sustituye más tarde por uno nuevo, visible, cuando el equipado real termina de aplicarse). El sondeo de v1.10.13 no esperaba nada de verdad: la condición que comprobaba nunca fue una señal fiable de "listo".
  - Quitado `WeaponManager::PollGestureWeaponReady` y `Constants::kGestureWeaponReadyMaxAttempts` (el sondeo basado en esa condición fallida).
  - `BeginCallAnimation` pasa a esperar una espera fija de `Constants::kSkipEquipAnimationWindow` (500ms) antes de ocultar el señuelo y disparar `attackStart` -- reutiliza la misma ventana que `ReequipAndReset` ya usa (y ya tenía calibrada en este proyecto) para el mismo fenómeno de asentamiento del equipado, en vez de inventar una señal de "listo" que no existe. Introduce un margen perceptible entre soltar el botón y ver `Call.hkx` -- aceptado a cambio de fiabilidad frente al comportamiento intermitente anterior.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Sin confirmar todavía en el juego.

### v1.10.15

- **Rechazado por el usuario, sin llegar a probarse formalmente: los 500ms de v1.10.14 no son viables** -- durante esa espera el arma real se ve equipada en pose de cuerpo a cuerpo (parpadeo visual), y sumados al resto del ciclo (~3s ya para recuperar el arma) es demasiado lento para la mecánica de juego. Se decide no volver a la rama desarmada (H2H) -- buscar una alternativa distinta, más rápida e invisible, para hacer que el personaje esté "armado" a nivel de grafo sin pasar por el equipado real lento de `RE::ActorEquipManager`.

### v1.10.16

- **Experimento: escribir directamente la graph variable vanilla `iRightHandType` (Int), sin equipar nada de verdad.** `iRightHandType` es una variable real del propio motor (no inventada, no necesita BehaviorDataInjector) que decide qué rama de combate usa el grafo según el arma en la mano derecha -- confirmada como tal en documentación pública de la comunidad de modding (usada también por `Actor::GetEquippedItemType`, que documenta 1 = espada a una mano), aunque el mapeo exacto de valores para esta variable de grafo en concreto no está confirmado con certeza en fuentes públicas.
  - `WeaponManager::BeginCallAnimation` ya no llama a `EquipGestureWeapon` -- en su lugar, `player->SetGraphVariableInt("iRightHandType", 1)` directamente, sin ningún equipado real de por medio. Si el grafo respeta el valor, el cambio de rama es instantáneo (nada que "asentar") y el arma real nunca se toca (nunca visible). `OnCallReleaseAnimationEvent` la revierte a `0` (desarmado) en vez de llamar a `UnequipGestureWeapon`.
  - `EquipGestureWeapon`/`UnequipGestureWeapon` (el arma señuelo) se dejan definidas sin usar, de reserva, por si este experimento no funciona en el juego -- evita tener que reconstruirlas si hace falta volver atrás.
  - Log de diagnóstico añadido en `WeaponManager::BeginThrowAnimation` (con el arma real todavía en mano en ese punto) que lee `iRightHandType` -- para confirmar con datos reales de esta partida si el valor `1` (adivinado a partir de `GetEquippedItemType`) es correcto, en vez de asumirlo.
  - Nuevas constantes `Constants::kRightHandTypeGraphVariable` (`"iRightHandType"`) y `Constants::kRightHandTypeOneHanded` (`1`, sin confirmar).
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Sin confirmar todavía en el juego.

### v1.10.17

- **Confirmado en el juego: el experimento de `iRightHandType` de v1.10.16 funciona -- Llamada se reproduce siempre (`Pie.ThorMjolnirCall` llega en todas las pruebas, la red de seguridad nunca salta), sin deslizamiento, sin arma visible, sin ningún retardo perceptible.** El log de diagnóstico reveló que el valor real de `iRightHandType` con el arma equipada en esta partida es **3**, no el `1` adivinado por analogía con `Actor::GetEquippedItemType()` -- probado en el juego que `1` "funcionaba" igual (el ataque ligero básico parece rutear por el mismo `1HM_AttackRight` sea cual sea el subtipo de arma a una mano), pero se corrige `Constants::kRightHandTypeOneHanded` al valor confirmado en vez de depender de esa coincidencia.
  - Quitado el log de diagnóstico de `BeginThrowAnimation` (ya cumplió su propósito).
  - `EquipGestureWeapon`/`UnequipGestureWeapon` (el arma señuelo) siguen definidas sin usar, de reserva -- con el experimento ya confirmado, se podrían quitar del todo, pero se dejan por ahora mientras se termina de implementar Atrape con el mismo mecanismo.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.11.1

- **Implementado Atrape (`WeaponManager::BeginCatchAnimation`/`OnCatchReleaseAnimationEvent`), con el mismo mecanismo `iRightHandType` ya confirmado en Llamada (v1.10.16/v1.10.17) -- ninguna variante H2H ni arma señuelo de por medio.**
  - Nuevo estado `State::kCatching` (`WeaponState`): se entra al llegar el arma a la mano (callback `onArrived` de `Return::BeginReturn`, antes llamaba directamente a `ReequipAndReset`), no al pulsar un botón.
  - `BeginCatchAnimation` escribe `iRightHandType` a `Constants::kRightHandTypeOneHanded`, activa `Animation::SetCatchTrigger` y dispara `attackStart` para que el submod de OAR de Atrape reproduzca `Catch.hkx`; arranca también la red de seguridad por tiempo (`Constants::kCatchReleaseFallbackWindow`).
  - `OnCatchReleaseAnimationEvent` (llamado desde el nuevo `Events::CatchReleaseWatcher`, sink de `RE::BSAnimationGraphEvent` que ya reconoce la anotación `PIE.ThorMjolnirCatch` -- venía horneada en `Catch.hkx` desde el principio, no hizo falta reexportar el `.hkx`) revierte `iRightHandType` a `0` y llama a `ReequipAndReset` -- a diferencia de `OnCallReleaseAnimationEvent`, aquí el regreso físico ya ha terminado, lo que sigue es el reequipado real.
  - `CaptureSaveData`/`OnLoadingScreenClosed` cubren `kCatching` igual que `kCalling`: la réplica sigue en el mundo y el arma real no se ha reequipado todavía en este estado, así que una pantalla de carga a mitad del gesto usa `RecallWeapon` (no un simple cambio de estado) para no dejarla huérfana.
  - `Catch/config.json` (OAR): descripción actualizada al mecanismo real, prioridad subida de 999 a 1000 (consistente con Lanzar/Llamada). Se corrige también la descripción de `Call/config.json`, que había quedado desactualizada describiendo el arma señuelo ya abandonado.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos (el nuevo callback `onArrived`/`BeginCatchAnimation` se confirma en el mismo hilo principal reencolado por `Physics::StartTickLoop`, mismo patrón ya usado por el resto del ciclo).
  - Pendiente de confirmar en el juego (mismo protocolo que Llamada: revisar `ThorMjolnir_OAR.log` y el Animation Log de OAR).

### v1.11.2

- **Corregidos dos bugs reportados por el usuario en Atrape (v1.11.1): la animación se reproducía después de que el arma ya hubiera llegado a la mano, y el arma se quedaba flotando quieta delante del personaje esperando a que el gesto terminara para "aparecer" en la mano.** Causa raíz: `BeginCatchAnimation` se disparaba en `onArrived` (llegada física real, réplica ya parada) y `ReequipAndReset` no se llamaba hasta la anotación de liberación del propio `Catch.hkx` -- la animación siempre iba un paso por detrás del arma, que se quedaba flotando el tiempo que tardaba en reproducirse el gesto.
  - **Rediseño: la animación y el reequipado físico se desacoplan por completo, cada uno con su propio disparador.** `Return::ReturnCallbacks` gana un nuevo callback `onApproaching`, disparado con antelación (`Constants::kCatchAnimationLeadTime`, mismo cálculo que ya existía para `Constants::kCatchStartSoundLeadTime`/`Audio::CatchCue`) sobre la llegada física real -- `WeaponManager::BeginReturn` lo conecta a `BeginCatchAnimation` (arranca el gesto visual) y deja `onArrived` conectado de nuevo directamente a `ReequipAndReset` (reequipado real, exactamente igual que antes de que existiera Atrape).
  - Nuevo `Return::ApproachTrigger` (`ReturnManager.cpp`, mismo patrón que `Audio::CatchCue::UpdateStart`: reloj propio que cuenta desde el principio del regreso, temblor de desprendimiento incluido si lo hay, dispara una sola vez al alcanzar el retardo) -- con red de seguridad propia: si el regreso resulta más corto de lo previsto y el retardo nunca se alcanza por su cuenta, se fuerza el disparo justo antes de `onArrived`, para que la animación arranque siempre antes (o, como muy tarde, en el mismo instante) que la llegada física, nunca después.
  - Como consecuencia, `State::kCatching` deja de existir por completo (`WeaponState`): el ciclo principal del arma vuelve a transitar `kReturning -> kInHand` directamente vía `onArrived`, indiferente al gesto visual. El gesto se trackea aparte con un nuevo flag `WeaponManager::catchAnimationActive`, independiente de `weaponState`, porque el arma real ya puede estar reequipada (estado `kInHand`) mientras `Catch.hkx` todavía se sigue reproduciendo de fondo.
  - `OnCatchReleaseAnimationEvent` ya no llama a `ReequipAndReset` (eso ya pasó en `onArrived`) -- solo apaga los flags del grafo (`CatchTrigger`/`AnimationDriven`/`iRightHandType`/bloqueo de movimiento) y `catchAnimationActive`.
  - `OnLoadingScreenClosed`/`ResetToInHand` limpian `catchAnimationActive` de forma incondicional (fuera del switch de estados, en el caso de `OnLoadingScreenClosed`), para no dejar esos mismos flags encendidos para siempre si una pantalla de carga o una partida nueva interrumpe el gesto a mitad -- posible ahora que el gesto puede seguir vivo con `weaponState` ya en `kInHand`.
  - `Constants::kCatchAnimationLeadTime` (`0.5f`): placeholder sin calibrar en el juego, a diferencia de `kCatchStartSoundLeadTime` (medido a oído sobre un sonido ya grabado) -- depende de cuánto tarda `Catch.hkx` en llegar a su propio gesto de agarre, hay que ajustarlo probando en el juego.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Pendiente de confirmar en el juego y calibrar `kCatchAnimationLeadTime` según se vea la sincronía real.

### v1.11.3

- **Corrección sobre v1.11.2: el reequipado real vuelve a estar gatillado por la anotación `PIE.ThorMjolnirCatch`, no por la llegada física.** El usuario aclaró que esa anotación, ya horneada en `Catch.hkx` desde el principio, existe precisamente para marcar el instante exacto en que la mano debe cerrarse sobre el arma en el clip -- v1.11.2 la había dejado sin uso (el reequipado pasaba a disparase en la llegada física, `onArrived`), lo cual habría quedado desincronizado del gesto real de la mano.
  - `Return::ReturnCallbacks::onArrived` vuelve a su comportamiento original (previo a Atrape): ya no reequipa nada, solo deja la réplica quieta -- el bucle de tick se detiene solo al llegar. `WeaponManager::BeginReturn` lo conecta a un no-op explícito.
  - `WeaponManager::OnCatchReleaseAnimationEvent` vuelve a llamar a `ReequipAndReset()` (como en Lanzar/Llamada), gatillado por la anotación real (o su red de seguridad) -- ya no por la llegada física.
  - `WeaponManager::BeginCatchAnimation` recupera el fallback `ReequipAndReset()` directo para el caso sin jugador (sin animación que reproducir, no tiene sentido esperar a ninguna anotación).
  - **`Constants::kCatchAnimationLeadTime` corregido de `0.5f` (placeholder sin calibrar) a `0.9f`** -- valor medido por el usuario directamente sobre el clip: 27 fotogramas a 30 FPS desde el arranque de `Catch.hkx` hasta su propia anotación. Como el reequipado depende de esa anotación y no de la distancia física, esta antelación debe coincidir con esos mismos 0.9s para que el gesto de la mano y la llegada física de la réplica queden sincronizados de verdad.
  - Documentada la relación entre `kCatchAnimationLeadTime` (0.9s) y `Constants::kCatchReleaseFallbackWindow` (1.5s, sin cambios): la red de seguridad debe seguir siendo mayor con margen de sobra, o podría dispararse antes de que la réplica llegue de verdad a la mano.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.

### v1.11.4

- **Corregido un bug no determinista de Atrape con el arma muy cerca al llamarla: a veces no arrancaba ninguna animación, a veces se repetía Call.hkx, a veces se reproducía un ataque cuerpo a cuerpo.** Causa raíz: con el arma muy cerca, el regreso físico es tan corto que `Constants::kCatchAnimationLeadTime` (0.9s) por sí solo daba un retardo casi nulo -- `BeginCatchAnimation` (con su propio `NotifyAnimationGraph("attackStart")`) se disparaba casi en el mismo instante en que `OnCallReleaseAnimationEvent` acababa de resolver el `attackStart` de Llamada. Dos disparos de ese mismo evento vanilla demasiado seguidos confundían al grafo de animación de forma no determinista, según en qué punto exacto de la resolución del primero le pillara el segundo.
  - Nueva constante `Constants::kMinCatchAnimationDelay` (`0.5f`, placeholder sin calibrar): retardo mínimo, independiente de `kCatchAnimationLeadTime`, antes de que `onApproaching` pueda dispararse -- le da margen al grafo para asentar `Call.hkx` antes de pedirle una animación nueva, a costa (solo en este caso extremo) de recortar la antelación real sobre la llegada física.
  - **Rediseño del mecanismo de disparo de `onApproaching`**: en vez de ir enganchado al bucle de tick de retorno (`Return::ApproachTrigger`, introducido en v1.11.2, ahora eliminado), `Return::BeginReturn` arranca un temporizador de hilo aparte (mismo patrón que el resto de redes de seguridad del proyecto: duerme el retardo calculado, luego `AddTask`) independiente de si el bucle de tick sigue vivo o no -- necesario porque, con el arma muy cerca, el bucle podía terminar (réplica ya llegada) antes de que el retardo (ahora con el mínimo de por medio) se cumpliera.
  - Como consecuencia, se retira la garantía anterior de "`onApproaching` siempre antes que `onArrived`" (v1.11.2): con este mínimo, en un regreso muy corto puede dispararse *después* de la llegada física -- deliberado, la réplica se queda quieta un instante esperando en ese caso extremo, preferible a la corrupción del grafo.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Pendiente de confirmar en el juego con el arma a distintas distancias (lejos, cerca, muy cerca) y calibrar `kMinCatchAnimationDelay` si el problema persiste o si se nota demasiado la espera en el caso extremo.

### v1.11.5

- **Rediseño completo de la sincronización de Atrape, corrigiendo dos bugs de v1.11.4: el arma podía reequiparse antes de terminar de recorrer visualmente el trayecto (con el arma "todavía lejos"), y con el arma muy cerca al llamarla seguían saliendo animaciones equivocadas de forma no determinista.** El usuario señaló la causa de fondo: usar temporizadores precalculados de antemano (v1.11.2 a v1.11.4) rompe la sincronización real en cuanto la trayectoria real diverge de la predicción -- y el suavizado del tramo final (`Constants::kReturnTailDistance`/`kReturnTailMinRate`, que alarga la duración real más allá de lo que predice `Return::ComputeReturnDuration`) garantiza que siempre diverge algo. La instrucción explícita: nunca desacoplar animación y física -- acelerar o ralentizar el vuelo según convenga, pero siempre sincronizado de verdad.
  - **Valores exactos, medidos por el usuario sobre los clips ya recortados** (sustituyen a los placeholders/estimaciones de v1.11.3/v1.11.4): `Constants::kCatchAnimationLeadTime` de `0.9f` a `0.5f` (Catch.hkx: 30 fotogramas a 30 FPS, anotación en el fotograma 15). `Constants::kMinCatchAnimationDelay` confirmado en `0.5f`, ahora medido de verdad en vez de adivinado (Call.hkx: 25 fotogramas a 30 FPS, anotación en el fotograma 10 -> cola de 15 fotogramas = 0.5s tras su propia anotación).
  - **`Return::ReturnCallbacks::onApproaching` ya no lo dispara ningún temporizador** (ni el `Return::ApproachTrigger` de v1.11.2 ni el hilo aparte de v1.11.4) -- se dispara desde dentro del propio bucle de tick de `Return::BeginReturnMovement`, midiendo la velocidad real de la réplica en ese mismo tick (distancia cerrada / tiempo) para estimar en segundos reales cuánto falta de verdad hasta la llegada, comparado contra `kCatchAnimationLeadTime`. Al venir de una medición en vivo, absorbe automáticamente el suavizado del tramo final y cualquier desviación por que el jugador se haya movido durante el regreso -- ya no hay predicción que pueda quedarse desfasada.
  - También exige que hayan pasado al menos `kMinCatchAnimationDelay` segundos reales desde que empezó el regreso (temblor de desprendimiento incluido si lo hubo) antes de poder disparar -- mismo propósito que el mínimo de v1.11.4 (evitar disparar el `attackStart` de Atrape demasiado pegado al de Llamada), pero ahora comprobado con el reloj real del propio bucle, no con un temporizador aparte que podía dispararse sin que el bucle siguiera vivo.
  - **Nueva función `Return::ComputeReturnAccelerationForDuration(distancia, duración objetivo)`** (`ReturnTrajectory.h`/`.cpp`, inversa de `ComputeReturnDuration` despejando la aceleración en vez de partir de ella -- mismo despeje algebraico que ya usaba internamente `ComputeReturnAcceleration` para su límite superior, `kReturnMaxDuration`, aquí expuesto para el caso contrario). `BeginReturnMovement` la usa para **ralentizar el vuelo (nunca acelerarlo)** cuando la distancia es tan corta que la aceleración natural haría que el regreso terminara antes de darle tiempo a los dos márgenes de sincronización de Atrape (`kMinCatchAnimationDelay` + `kCatchAnimationLeadTime`) -- la física real, no solo la animación, se ajusta para que la sincronización sea real y no aproximada.
  - Como consecuencia, ya no existe ningún camino en el que `onApproaching`/el reequipado puedan dispararse por delante de lo que la réplica ha recorrido de verdad (bug de "arma todavía lejos, ya reequipada") ni por detrás de un margen de asentado insuficiente tras Llamada (bug de animaciones no deterministas) -- ambos quedan resueltos por construcción, no por ajuste de constantes a ciegas.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Corregido un error de compilación durante el propio desarrollo: `std::numeric_limits<float>::max()` choca con la macro `max` de Windows.h (mismo problema ya documentado en el proyecto para `std::min`/`std::max`) -- sustituido por un literal centinela propio.
  - Pendiente de confirmar en el juego a distintas distancias (lejos, cerca, muy cerca) -- con datos ya medidos en vez de estimados, no debería hacer falta recalibrar nada, pero queda por confirmar en la práctica.

### v1.11.6

- **Corregido un bug residual de v1.11.5, reportado por el usuario tras probarlo: en todos los casos (lejos o cerca) la réplica se detenía un instante corto pero visible justo antes de llegar a la mano, y justo después se reenganchaba de golpe.** Causa: la estimación de tiempo hasta la llegada (`Return::BeginReturnMovement`) apuntaba a `distanceToHand == 0`, pero la condición de llegada real que de verdad congela el bucle de tick usa `Constants::kReturnArrivalDistance` (30 unidades) como umbral, no distancia cero -- el bucle congelaba la réplica *antes* de que la estimación considerara "llegado", así que `onApproaching` se disparaba tarde, dejando una réplica visiblemente quieta esperando el resto del margen hasta que la anotación de Catch.hkx por fin disparaba el reequipado.
  - La estimación ahora resta `Constants::kReturnArrivalDistance` antes de dividir por la velocidad medida (`remainingDistance = distanceToHand - kReturnArrivalDistance`), apuntando al mismo umbral que de verdad usa la condición de llegada, en vez de a una llegada "completa" que el propio bucle nunca espera.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Pendiente de confirmar en el juego que la pausa visible ha desaparecido.

### v1.11.7

- **Reducido (según el usuario, tras probar v1.11.6) pero no eliminado del todo el hueco visible entre "la réplica se para" y "el arma real reaparece".** La estimación de velocidad de `onApproaching` (v1.11.5/v1.11.6) mide un único tick y no puede predecir con precisión perfecta cuánto va a seguir decelerando el suavizado del tramo final después de esa medición -- siempre puede quedar un margen residual pequeño entre que `Catch.hkx` empieza demasiado tarde/pronto y la llegada física real.
  - En vez de seguir persiguiendo una estimación perfecta, se ataca el síntoma directamente: **el bucle de tick de `Return::BeginReturnMovement` ya no se detiene al cruzar `Constants::kReturnArrivalDistance`** (antes llamaba a `onArrived()` y devolvía `false`, dejando la réplica congelada en la última posición de la curva, hasta 30 unidades de la mano). Ahora sigue vivo, pegando la réplica a la posición exacta de la mano cada tick (siguiéndola si el jugador se sigue moviendo) hasta que `WeaponManager::ReequipAndReset` (gatillado por la anotación de `Catch.hkx`, no por este bucle) lo cancela desde fuera.
  - Con esto, cualquier margen residual entre `onApproaching` y la llegada real dejó de ser un salto de posición (réplica flotando quieta a 30 unidades de la mano) -- como mucho es un cambio de malla en el sitio correcto (réplica -> arma real equipada), mucho menos perceptible.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos (el bucle ya se cancela de forma garantizada desde `ReequipAndReset`, cuyo disparo ya estaba garantizado por la red de seguridad de `BeginCatchAnimation` -- sin riesgo de fuga).
  - Pendiente de confirmar en el juego.

### v1.11.8

- **Implementado el punto 10 (segunda mitad, hasta ahora pendiente a propósito, ver `CHANGELOG.md` v1.6.1): "justo antes de... volver a la mano del jugador, se endereza para que... el mango quede orientado para que el jugador pueda agarrarla".** El usuario identificó la causa real del hueco visual que sobrevivía a v1.11.7: no era (solo) un problema de posición, sino de **rotación** -- el arma seguía girando sobre sí misma hasta el último instante, así que el cambio a la pose de agarre real (sin rotación extra) se notaba como un salto brusco, no como una espera.
  - Nueva función `Animation::TickSpinStraighten` (`WeaponAnimation.h`/`.cpp`), sustituye a `Animation::TickSpin` durante la ventana de enderezado: recalcula el ángulo que tenía el giro justo al empezar esa ventana y lo interpola (curva suave, smoothstep) hacia 0 -- la orientación de reposo del nodo, la misma que ya tiene el arma real equipada.
  - `Return::BeginReturnMovement` arranca esa ventana en el mismo instante que `onApproaching` (mismo disparador, misma duración de momento: `Constants::kSpinStraightenDuration`, 0.5s placeholder) -- así el giro llega frenado y alineado justo cuando toca, en paralelo con el gesto de Atrape.
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Pendiente de confirmar en el juego, y de implementar la otra mitad del punto 10 (enderezar la cabeza/filo hacia el objetivo antes de clavarse en la ida) -- fuera de alcance de este bug de Atrape, sigue pendiente como estaba.

### v1.11.9

- **Corregido un bug arrastrado desde varias versiones atrás: tras Atrape, el personaje se quedaba en pose de cuerpo a cuerpo hasta realizar una acción (atacar, etc.), momento en el que saltaba de golpe a la pose de una mano -- a pesar de que el arma ya estaba genuinamente reequipada.** Causa: tanto `WeaponManager::OnCatchReleaseAnimationEvent` como el bloque de limpieza de `OnLoadingScreenClosed` volvían `iRightHandType` a `0` (desarmado) justo *antes* de disparar el reequipado real (`ReequipAndReset`/`RecallWeapon`, que sí incluye una llamada real a `RE::ActorEquipManager::EquipObject`) -- el reequipado real no parece disparar por sí solo un refresco de la pose de reposo, así que el personaje se quedaba mostrando la última pose evaluada (cuerpo a cuerpo) hasta que otra acción forzaba al grafo a releer la graph variable.
  - Quitada esa escritura a `0` en ambos sitios -- `iRightHandType` ya está en el valor correcto (`Constants::kRightHandTypeOneHanded`, puesto en `BeginCatchAnimation`) para lo que va a ser cierto de verdad en cuanto `EquipObject` se aplique, así que tocarlo ahí sobraba y además rompía la pose de reposo.
  - Sigue reseteándose a `0` en `WeaponManager::OnCallReleaseAnimationEvent` (correcto: ahí el jugador se queda genuinamente desarmado, sin ningún reequipado real a continuación) y en `ResetToInHand` (red de seguridad de sesión, sin reequipado real garantizado justo después).
  - Revisado con la skill `skse-plugin-pitfalls`: sin hallazgos.
  - Pendiente de confirmar en el juego.

## 2026-08-05 (Lanzar/Llamada/Atrape: golpe vainilla real contra NPCs cercanos, experimento `kIgnoreFriendlyHits`)

### v1.11.10

- **Diagnosticado y en pruebas: al disparar `attackStart` (evento usado para que OAR sustituya Lanzar/Llamada/Atrape), si hay un NPC cerca del jugador se dispara la detección de golpe real vainilla contra él (sonido, sangre, daño) -- independientemente de qué clip haya sustituido OAR.** Causa: `AttackRight_State` (el estado vanilla al que pertenece `attackStart`, ver `1hm_behavior.xml`) pone la graph variable `iRegularAttack` a `1` mediante un `hkbEvaluateExpressionModifier` propio (`EvaluateiRegularAttackTrue`) -- casi con toda seguridad leída por el pipeline nativo (no expuesto en `commonlibsse-ng`) de detección de golpe cuerpo a cuerpo de Bethesda, de forma independiente al contenido real del clip sustituido.
  - Se descartaron explícitamente dos vías alternativas: un parche de Nemesis/Pandora propio que registre un evento custom nuevo (más limpio arquitectónicamente, pero el usuario prefiere no mantener un parche de comportamiento aparte); y probar otros eventos vanilla como disparador (`recoilStart`, `IdleTauntA`/`IdleTauntB`, tres gestos de diálogo del DLC01) -- todos descartados tras pruebas reales en el juego por sus propios problemas (remanencia de animación vainilla tras `recoilStart` por ser una capa aditiva sin más salida automática que `recoilStop`; fallo determinista de `NotifyAnimationGraph` a la tercera repetición del mismo evento con `IdleTauntA`/`B`; pérdida de la pose de "listo para atacar" tras los gestos de diálogo, sin arreglo satisfactorio). Ver el histórico completo de estas pruebas en las entradas previas de este mismo archivo antes de esta fecha si hace falta retomarlas.
  - Nuevo experimento: `Combat::SetIgnoreFriendlyHits` (`7.- COMBAT/DamageManager.h`/`.cpp`) activa/desactiva sobre el jugador el mismo flag de referencia que `Actor.SetIgnoreFriendlyHits` en Papyrus (`RE::TESForm::RecordFlags::kIgnoreFriendlyHits`, bit `1<<20` de `formFlags` -- miembro público y de offset fijo, `TESForm` es la primera clase base de `TESObjectREFR`, sin el problema de offset por versión de juego que sí afecta a otras bases de `Actor`). `WeaponManager` lo activa justo antes de disparar `attackStart` en `BeginThrowAnimation`/`BeginCallAnimation`/`BeginCatchAnimation`, y lo desactiva en el punto simétrico de cada ciclo (el desequipado real diferido de `ThrowWeapon`, `OnCallReleaseAnimationEvent`, `OnCatchReleaseAnimationEvent`) -- además de en los tres caminos de aborto/reset (`OnLoadingScreenClosed` para `kThrowing`/`kCalling`/Atrape a medias, y `ResetToInHand` como red de seguridad general).
  - Limitación conocida, sin confirmar todavía en el juego: el propio nombre del flag ("amistoso") sugiere que podría solo suprimir el golpe contra objetivos de la misma facción/alianza que el jugador, no contra cualquier NPC hostil cercano -- a diferencia de `iRegularAttack`, no hay forma de verificar esto leyendo los headers de `commonlibsse-ng` (el pipeline que lo consume no está expuesto), solo probándolo en el juego.
  - Pendiente de confirmar en el juego: si el golpe/sonido/sangre real desaparece contra NPCs neutrales/hostiles cercanos, o solo contra aliados.

### v1.11.11

- **Sustituido `kIgnoreFriendlyHits` (v1.11.10) por la solución de raíz: la opción propia de Open Animation Replacer "Only use triggers from annotations".** El usuario encontró y confirmó en el juego que esta opción (activable por submod en el modo Author de la UI de OAR, junto con "Ignore 'Don't Convert Annotations To Triggers' flag", que su propio tooltip pide activar a la vez) hace que OAR ignore por completo los triggers horneados en el clip vainilla original sustituido -- solo se procesan los que vengan de anotaciones dentro de nuestro propio archivo (nuestra única `PIE.xxx`). Con esto, `AttackRightModifierList`/`EvaluateiRegularAttackTrue` (la causa raíz de v1.11.10, ver ahí) deja de ejecutarse en absoluto para nuestros clips, así que `iRegularAttack` nunca se pone a `1` y el golpe/sonido/sangre real vainilla contra NPCs cercanos desaparece de raíz -- sin necesitar ningún flag de mitigación por nuestra parte. `Combat::SetIgnoreFriendlyHits` y todas sus llamadas se han quitado (`7.- COMBAT/DamageManager.h`/`.cpp`, `WeaponManager.cpp`).
  - Activar esa opción introdujo un bug nuevo: Llamada y Atrape se quedaban congelados sin salir de `AttackRight_State` (Lanzar no se veía afectado). Rastreadas las 18 transiciones reales de `AttackRight_State` en `1hm_behavior.xml` (11 locales + 7 wildcard, ver `#0813`/`#2213`): ninguna es un simple "ataque terminado, vuelve a idle" -- todas encadenan a otro ataque (power attacks direccionales, combos), condicionadas a ventanas de tiempo ancladas a anotaciones del clip vainilla original (`AttackWinStart`/`AttackWinEnd`/`weaponSwing`) que nuestros clips no llevan. Añadir esas anotaciones a los nuestros no habría arreglado el atasco -- solo habría reabierto el sistema de combos vainilla (el mismo riesgo de escalar a un power attack real ya documentado y blindado para Lanzar, ver `CHANGELOG.md` v1.9.16 y siguientes).
  - Hipótesis (no verificada a fondo, pero consistente con el comportamiento observado): Lanzar no se atasca porque su propio desequipado real del arma, más adelante en el ciclo (`ThrowWeapon`), fuerza al motor a reevaluar la rama de combate por fuera de cualquier transición del grafo -- un reset "externo" a la máquina de estados. Llamada/Atrape, a propósito, nunca tocan el equipo real durante el gesto (para no disparar la animación de desenvainar vía `SkipEquipAnimation`), así que nunca reciben ese reset.
  - **Solución aplicada, confirmada por el usuario en el juego**: `Constants::kAttackStopAnimationEvent = "attackStop"` (evento vanilla, opuesto de `kLightAttackAnimationEvent`/`attackStart`, ya existente en la tabla de eventos de `1hm_behavior.hkx`) disparado a mano vía `NotifyAnimationGraph` desde `WeaponManager::OnCallReleaseAnimationEvent`/`OnCatchReleaseAnimationEvent`, justo en el mismo instante en que ya se limpia el resto de flags del gesto. Desatasca el grafo tanto fuera de combate (antes: Atrape no llegaba a reproducirse en absoluto, congelado en el último fotograma de Llamada) como en combate (antes: congelado justo en el fotograma de la anotación `PIE.ThorMjolnirCall`). No hizo falta tocar ningún `.hkx`.
  - Lanzar no dispara `attackStop` (no lo necesita, ver hipótesis arriba) -- sin confirmar si sería inofensivo añadirlo también por consistencia, no se ha hecho por no tocar lo que ya funciona.

### v1.11.12

- **Payload Interpreter eliminado como dependencia del mecanismo de detección de liberación de Lanzar/Llamada/Atrape -- sustituido por la API de Functions de Open Animation Replacer.** El usuario encontró que OAR tiene su propio sistema de anotaciones nativo (`OAR.<payload>`, sin necesitar ningún parche de Nemesis) y pidió cambiar a él. Dos intentos, el primero descartado tras confirmarlo en el juego:
  - **Intento 1 (descartado)**: anotaciones renombradas de `PIE.xxx` a `OAR.xxx`, config.json de cada submod con un bloque `functionsOnTrigger` ligando el trigger a la función nativa `SendAnimEvent` de OAR (que internamente llama a `NotifyAnimationGraph(eventName)`). Confirmado con el propio Animation Event Log de OAR que el evento sí se disparaba (`[Notify] ThorMjolnirCall`/`ThorMjolnirCatch`) -- pero nuestro `BSTEventSink<BSAnimationGraphEvent>` (`Events::ThrowReleaseWatcher`/`CallReleaseWatcher`/`CatchReleaseWatcher`) nunca lo recibía; la "red de seguridad" por tiempo seguía disparándose en las tres animaciones, en dos sesiones de prueba distintas. Causa: `NotifyAnimationGraph()` disparado por código es una inyección de evento *hacia* el grafo, no algo que el motor redifunda *hacia fuera* a sinks externos -- a diferencia de una anotación procesada de forma nativa durante la reproducción real del clip (el mecanismo real de Payload Interpreter), que sí se redifunde.
  - **Intento 2 (aplicado, confirmado en el código fuente real de OAR antes de implementar)**: la API de Functions para plugins externos (`OAR_API::Functions`, github.com/ersh1/OpenAnimationReplacer, `src/API/`). En vez de escuchar un evento, nuestro propio plugin registra tres funciones custom (`ThorMjolnirThrowRelease`/`CallRelease`/`CatchRelease`) directamente en OAR (`OAR_API::Functions::AddCustomFunction<T>()`, llamado en `kPostLoad`) -- OAR las llama a nosotros por función (`IFunction::Run`/`RunImpl`), en el mismo proceso, sin ningún evento de por medio. Patrón verificado contra el plugin de ejemplo oficial (`ersh1/OpenAnimationReplacer-ExamplePlugin`, que hace lo mismo para Conditions -- Functions duplica la misma estructura a propósito, según el propio comentario del código fuente).
  - Nuevo directorio `src/13.- EXTERNAL/OpenAnimationReplacer/`: copia literal, sin modificar, de los headers/.cpp de `src/API/` de OAR (`OpenAnimationReplacer-SharedTypes.h`, `OpenAnimationReplacer-FunctionTypes.h`/`.cpp`, `OpenAnimationReplacerAPI-Functions.h`/`.cpp`) -- el propio header lo pide así ("Copy this file into your own project if you wish to use this API"). Primera dependencia de **compilación** de este proyecto sobre un mod externo (hasta ahora todas eran solo dependencias de mod instalado, ver CLAUDE.md) -- si OAR no está instalado o es una versión sin esta API, `GetAPI()` devuelve null, el registro falla con un aviso por log, y el ciclo sigue funcionando solo con la red de seguridad por tiempo, sin sincronía fina.
  - `src/10.- EVENTS/OARFunctions.h`/`.cpp`: las tres clases custom (heredan de `Functions::CustomFunction`, patrón "bare minimum" del propio ejemplo oficial) y `RegisterAll()`, llamado desde `Events::OnSKSEMessage` en `kPostLoad`.
  - Quitados por completo: `Events::ThrowReleaseWatcher`/`CallReleaseWatcher`/`CatchReleaseWatcher` (la clase `BSTEventSink<BSAnimationGraphEvent>`), `Events::EnsureAnimationSinksRegistered()` y su llamada perezosa desde `WeaponManager::BeginAiming`, y las constantes `Constants::kPayloadInterpreterTag`/`kThrowReleasePayload`/`kCallReleasePayload`/`kCatchReleasePayload` (del mecanismo original de Payload Interpreter) y `kThrowReleaseEventName`/`kCallReleaseEventName`/`kCatchReleaseEventName` (del Intento 1, `SendAnimEvent`).
  - `pch.h`: añadido `using namespace std::literals;` (necesario para los literales `"..."sv` que usan los headers vendorizados de OAR, que no lo declaran ellos mismos -- asumen que el proyecto consumidor ya lo tiene).
  - **Confirmado en el juego por el usuario**: los tres eventos de liberación llegan correctamente vía la API de Functions, sin depender de Payload Interpreter.

### v1.11.13

- **`BehaviorDataInjector` eliminado como dependencia -- sustituido por `TESGlobal` reales de la Creation Kit.** BDI solo hacía falta para registrar almacenamiento de graph variables custom (`ThorMjolnir_ThrowTrigger`/`CallTrigger`/`CatchTrigger`) en la tabla interna del behavior de Havok; un `TESGlobal` es almacenamiento nativo del motor y no tiene ese problema en absoluto -- ni falta registrar nada en tiempo de ejecución.
  - `Animation::SetThrowTrigger`/`SetCallTrigger`/`SetCatchTrigger` (`8.- ANIMATION/WeaponAnimation.cpp`) ya no llaman a `Actor::SetGraphVariableFloat` -- ahora escriben `TESGlobal::value` directamente, sobre un puntero cacheado tras la primera búsqueda por EditorID (`RE::TESForm::LookupByEditorID<RE::TESGlobal>`, patrón perezoso ya usado en el resto del proyecto). `Constants::kThrowTriggerGraphVariable`/`kCallTriggerGraphVariable`/`kCatchTriggerGraphVariable` renombradas a `k*TriggerGlobalEditorID`, mismo valor de cadena reutilizado como EditorID del Global (`CAP_GlobalVariable_ThorMjolnir_ThrowTrigger` y análogas, con el prefijo `CAP_GlobalVariable_` que usa el usuario para este tipo de registro en la Creation Kit).
  - `config.json` de los tres submods: `Value A` de la condición `CompareValues` pasa de `{"graphVariable": "...", "graphVariableType": "Float"}` a `{"form": {"pluginName": "ThorMjolnirOAR.esp", "formID": "..."}}` -- formato verificado directamente en el código fuente de OAR (`SharedTypes.cpp`, `NumericValue::Parse`/`Serialize`) antes de escribirlo, no adivinado.
  - **Trampa real, ya documentada de forma permanente en CLAUDE.md** ("Errores comunes a vigilar"): `ThorMjolnirOAR.esp` tiene el flag ESL activo, así que el FormID local que muestra la Creation Kit (`01203E`/`01203F`/`012040`) no es el valor real a usar -- un ESL solo tiene 12 bits de espacio de FormID local, hay que enmascarar (`& 0xFFF`, quedarse con los últimos 3 dígitos hex: `03E`/`03F`/`040`). Detectado porque el propio editor de OAR mostraba la condición como "(Not found)" con el valor sin enmascarar; confirmado y arreglado tras aplicar la máscara. Mismo problema, ya visto antes en este proyecto, con `Constants::kCatchStartSoundLocalFormID` (`0xEA61` → `0xA61`).
  - Simplificados los logs de diagnóstico de `BeginThrowAnimation`/`BeginCallAnimation`/`BeginCatchAnimation` (`WeaponManager.cpp`): ya no releen la graph variable de vuelta (`GetGraphVariableFloat`) para confirmar la escritura -- esa comprobación existía por si la escritura fallaba en silencio (el problema real de las graph variables, motivo original de BDI), y un `TESGlobal` no puede fallar así.
  - **Confirmado en el juego por el usuario.** `Data/SKSE/Plugins/BehaviorDataInjector/ThorMjolnir_BDI.json` ya no es necesario -- BehaviorDataInjector deja de ser dependencia del mod (ver CLAUDE.md, actualizado).

### v1.11.14

- **Bug de versión del plugin ante la API de Functions de OAR, detectado en una modlist grande (LoreRim) donde el editor de OAR marcaba Lanzar/Llamada/Atrape como `! INVALID !` ("Missing required plugin ThorMjolnir_OAR version 1.0.0.0") pese a que el registro de las tres funciones se confirmaba correcto en `ThorMjolnir_OAR.log`.** Causa encontrada en el propio código de xmake, no en OAR ni en la modlist: `set_version("0.0.0")` (raíz de `xmake.lua`) nunca se había subido de `0.0.0` -- ese valor es el que `lib/commonlibsse-ng/xmake.lua` vuelca en `SKSE::PluginDeclaration::GetVersion()`, y es literalmente lo que `AddCustomFunction` envía a OAR como versión de nuestro propio plugin. Nuestras tres funciones (`OARFunctions.cpp`) declaran `GetRequiredVersion() { return {1, 0, 0}; }` -- valor que quedó grabado en el `config.json` de cada submod al exportarlas (`"requiredVersion": "1.0.0.0"`). Con el plugin registrándose a sí mismo como `0.0.0.0`, la propia OAR compara esa versión contra el `1.0.0.0` que la config exige y la marca como no satisfecha, aun con el registro correcto.
  - Arreglo: `set_version("1.0.0")` en `xmake.lua`. Confirmado tras el rebuild que el DLL ya reporta `FileVersion 1.0.0.0`.
  - No explica por sí solo por qué la modlist de pruebas del usuario no mostraba el aviso con el mismo binario en `0.0.0.0` -- probablemente esa comprobación de versión en el editor de OAR es solo informativa y no bloqueaba la ejecución real de la función ahí; de cualquier forma, el desajuste era real y había que corregirlo.

### v1.11.15

- **Compensación balística de la puntería en Lanzar (`Throw::ComputeAimedDirection`/`SolveLowArcPitch`, `4.- THROW/ThrowManager.cpp`), corrigiendo que el martillo caía muy por debajo de la mirilla a corta/media distancia.** Causa: la dirección de lanzamiento apuntaba en línea recta al punto de la mira, y la gravedad hacía caer el arma por debajo de esa línea recta durante el vuelo -- sin ninguna compensación entre ambas cosas. Con las constantes de antes (`kThrowInitialSpeed`/`kThrowGravity`), la caída ya superaba el metro a ~20 unidades de Skyrim por segundo de vuelo (~20 m de distancia).
  - Fórmula cerrada de ["Solving Ballistic Trajectories"](https://www.forrestthewoods.com/blog/solving_ballistic_trajectories/) (forrestthewoods.com), caso de velocidad fija / objetivo estático: dada la distancia horizontal y la diferencia de altura hasta el punto de la mira, resuelve el ángulo de tiro (pitch) para el que la parábola completa termina exactamente ahí, en vez de solo la línea recta inicial. Se usa la solución de arco bajo (más directa); la de arco alto se descarta por quedar visualmente exagerada a media/larga distancia. La velocidad de lanzamiento (`kThrowInitialSpeed`) no cambia -- coherente con el punto 3 de `Mecanica del arma.txt` ("siempre se lanza con la misma fuerza"), solo varía el ángulo.
  - Sin solución real (objetivo fuera del alcance balístico de esa velocidad) se cae al comportamiento anterior (apuntar en línea recta) -- no ocurre en la práctica dentro de `kMaxThrowDistance` con las constantes actuales.
  - **Retirada la rampa de gravedad de arranque** (Mejora Kratos #1, `kThrowGravityRampDuration`, introducida en v1.11.x anteriores): la fórmula de arriba asume gravedad constante desde el instante cero; con la rampa activa habría quedado un pequeño residuo (el arma llegando algo por encima de la mirilla en tiros muy cortos). Decisión del usuario: preferible gravedad constante sin más que añadir un segundo paso de refinamiento iterativo sobre la fórmula cerrada.
  - Pendiente de probar en el juego a corta/media/larga distancia.

## 2026-08-06 (Punto 10: transición continua de rotación en vuelo -- sin saltos al lanzar, impactar o atrapar)

### v1.12.1

- **Diagnosticado y corregido el salto visual de rotación reportado por el usuario (bocetos a mano) al soltar el arma y al recuperarla en la mano.** Causa raíz: `TESObjectREFR::SetAngle` nunca se llamaba sobre la réplica en todo el proyecto -- su rotación "lógica" quedaba fija a lo que le asignara `PlaceObjectAtMe` por defecto y no se tocaba en toda la ida/vuelta; el giro visible (`Animation::TickSpin`) se escribía siempre como rotación *absoluta* (`elapsed = 0` -> identidad) sobre un nodo hijo, sin relación con la pose real que tenía el arma en la mano un frame antes.
  - Descartada la corrección obvia (capturar la rotación real y volcarla a `SetAngle`): `NiMatrix3::ToEulerAnglesXYZ` no es la inversa de `EulerAnglesToAxesZXY` (la convención que ya usa `Physics::SyncHavok` para la réplica) -- comparadas fórmula a fórmula en `NiMatrix3.cpp`, son dos convenciones de Euler distintas; ya hay precedente de este mismo problema en este proyecto (ver v0.2.1, dirección de cámara). Se mantiene el diseño ya existente y documentado ("puramente visual, no toca el ángulo lógico de `a_refr` ni Havok") en vez de romperlo.
  - **Nuevos helpers de rotación** (`9.- MATH/RotationMath.h`/`.cpp`): `SlerpRotation` (interpolación esférica entre dos `NiMatrix3`, vía cuaternión), `LocalRotationFromWorld` (rotación local que reproduce una rotación mundial deseada dado el mundial constante del nodo raíz, `Transpose(root) * deseada`) y `ShortestArcRotation` (rotación de ángulo mínimo que lleva un vector a coincidir con otro). Ninguno depende de la conversión a Euler.
  - **Lanzamiento** (`Throw::LaunchWeapon`): captura la rotación mundial real de la malla equipada (`Animation::GetEquippedWeaponWorldRotation`, mismo nodo que `SetEquippedWeaponHidden`) justo antes de que `WeaponManager::ThrowWeapon` la oculte, y funde (`Animation::TickSpin`, nueva firma con `a_blendFromLocal`) desde esa pose exacta hacia el giro calculado, a lo largo de la rampa ya existente (`kSpinRampDuration`). El giro en sí es un objetivo que también se mueve cada tick, así que nunca hay un ángulo fijo al que saltar.
  - **Impacto** (segunda mitad del punto 10, "el filo/cabeza apunte hacia el objetivo" -- hasta ahora sin implementar, el giro se congelaba tal cual al golpear): nuevo `Animation::ComputeImpactAlignment` alinea `Constants::kImpactAxisLocal` (placeholder, eje local del modelo hacia la cabeza/filo) con la dirección de vuelo en el instante del golpe, y `Animation::TickSpinStraighten` (nueva firma, con objetivo explícito en vez de "hacia el reposo") funde hacia ahí en `kImpactStraightenDuration`. Implementado en los dos casos: superficie (bucle de enderezado propio y corto en `Throw::LaunchWeapon`) y actor (integrado en el bucle de seguimiento ya existente de `Combat::BeginEmbeddedEffect`, que ahora recibe la dirección de vuelo).
  - **Regreso** (`Return::BeginReturnMovement`): mismo fundido al *empezar* a moverse (arreglaba un salto equivalente no reportado explícitamente por el usuario pero de la misma naturaleza, al desprenderse de un objetivo clavado o al iniciar un auto-recall a media parábola) partiendo de lo que llevara el nodo de giro en ese instante (`Animation::GetSpinLocalRotation`, nuevo). En la ventana de aproximación a la mano, el objetivo del enderezado ya no es la pose de reposo del nodo (asumía que la rotación de la réplica ya coincidía con la mano, falso): ahora es la rotación real y *en vivo* del hueso `"WEAPON"` del jugador (`Animation::GetHandBoneWorldRotation`, recalculada cada tick), así que si el jugador sigue girando durante la ventana, el arma le sigue.
  - **Limitación conocida, pendiente de confirmar en el juego**: el objetivo del enderezado del regreso usa el hueso `"WEAPON"`, no la malla equipada -- el arma real ya está desequipada del todo durante esa ventana (ver `CLAUDE.md`), así que no hay malla que muestrear. Si el NIF tiene un offset de agarre no nulo entre hueso y malla, quedará un pequeño residuo de rotación en el atrape. Añadido un log de diagnóstico de un solo uso por sesión (`Throw::LaunchWeapon`, "offset de agarre malla/hueso") que compara ambas rotaciones en el único instante en que las dos están disponibles a la vez (justo antes de lanzar) -- a petición del usuario, para introducir ese offset como corrección fija más adelante si las pruebas en el juego lo confirman necesario.
  - Pendiente de probar en el juego.

### v1.12.2

- **Primera prueba del usuario en el juego: regreso "muy buena aproximación" (sin cambios), pero dos problemas reales en v1.12.1 -- el giro de vuelo se aplanaba momentos después de salir de la mano, y el impacto contra un enemigo dejaba el arma clavada por el mango en vez de por la cabeza.**
  - **Log revisado** (`Documents/My Games/Skyrim Special Edition/SKSE/ThorMjolnir_OAR.log`): offset de agarre malla/hueso = `(-0.00, -0.00, 0.00)` grados XYZ -- despreciable en este NIF, confirma que el objetivo de enderezado del regreso (hueso `"WEAPON"`, ver limitación de v1.12.1) no necesita corrección. Log de diagnóstico retirado (`Throw::LaunchWeapon`), ya cumplió su función.
  - **Causa raíz del giro que se aplanaba**: `Animation::TickSpin` (v1.12.1) solo fundía desde la pose real capturada (`a_blendFromLocal`) durante `kSpinRampDuration` (0.3s) y, pasada la rampa, escribía el ángulo de giro calculado *tal cual* -- anclado al nodo raíz (una orientación casi horizontal, ver `CLAUDE.md`), descartando la pose capturada por completo. El usuario lo describió con precisión: "es el valor inicial de lanzamiento quien debe marcar su rotación posterior", no una referencia horizontal fija. **Arreglo**: `TickSpin` ya no funde temporalmente -- compone la pose capturada como base permanente durante todo el tramo de vuelo (`local = a_baseLocal * ComputeSpinRotation(elapsed)`, parámetro renombrado a `a_baseLocal`). El giro sigue acelerando igual (`kSpinRampDuration` sigue rampeando la velocidad angular, eso no cambia), pero ahora gira siempre relativo a cómo salió de la mano, nunca respecto al nodo raíz. Mismo arreglo aplicado al inicio del tramo de regreso (`Return::BeginReturnMovement`), que usa la misma función -- sin bug visible ahí solo porque la base capturada ya solía estar cerca de la horizontal (arrastrada del propio bug de la ida), no porque el mecanismo fuera distinto.
  - **Causa probable de "clavada por el mango"**: `Animation::ComputeImpactAlignment` (v1.12.1) calculaba hacia dónde apunta `Constants::kImpactAxisLocal` asumiendo el nodo de giro en reposo (`local == identidad`), en vez de con su rotación local real en ese instante -- con el arreglo de arriba, el nodo de giro casi nunca está en identidad durante el vuelo, así que la referencia de partida para alinear con la dirección de vuelo estaba sistemáticamente equivocada. **Arreglo**: nuevo parámetro `a_currentLocal` -- alinea desde hacia dónde apunta la cabeza *ahora mismo* (`rootWorld * currentLocal * kImpactAxisLocal`), no desde una referencia de reposo asumida. `Constants::kImpactAxisLocal` (el propio eje, `{0,1,0}`) se deja sin tocar a propósito para este cambio -- si tras la corrección de referencia el impacto sigue apuntando al lado contrario, ahí sí hará falta invertir el signo, pero mezclar ambos cambios a la vez habría impedido saber cuál de los dos arreglaba el problema.
  - Pendiente de volver a probar en el juego: giro en vuelo (ya no debería aplanarse) y orientación del impacto contra enemigo/superficie (a falta de confirmar si el eje en sí necesita además invertirse).
